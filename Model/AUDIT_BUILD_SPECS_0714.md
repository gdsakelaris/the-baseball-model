# Feature-gap audit — approved build specs (2026-07-14)

Working document for the audit build wave joining the finish batch. Produced by the
feature-gap-audit workflow (11 finders -> dedup -> 49 adversarial verifications ->
synthesis); all four scope decisions taken by the user 07-14 evening:
**all 33 ranked items + backlog #22 rebuild + opp_penhl_share rider (share only) +
park_vmr column with 2025-only gated pricing fit.** Prune this file post-batch
(HANDOFF_0714.md precedent).

Line-number anchors in specs refer to the 07-14 post-finish-batch tree and drift as
edits land — re-grep rather than trusting offsets late in the build.

## Coverage verdict (synthesis, verbatim)

Direct answer: NO — but narrowly. No head family is missing a first-order axis; what remains is a finite, enumerable set of ~46 verified gaps (33 build items after merging shared tables), 2 measured-empty proposals, and 1 gray. Per family: BATTER CONTACT/POWER (hr/hit/tb2-4/double/triple/xtb/xh/hrr): ~95% saturated — 385 cols spanning a 4-point EV tail, pull-side spray+park physics, platoon, exposure products, MiLB priors. Genuine holes: the oppo-side spray bin + its fence interaction (spray is one-sided today), pitcher spray/shape-allowed exposure, per-hit-type rate isolation, and three raw-archive cause-axes (perceived velo, release-slot separation, breaking movement). The TRIPLE head is the under-covered outlier: 1.21% base with only speed proxies + park factor until hit-type rates ship. DISCIPLINE (k/bk family, bb/xbb, sb): K-side is the best-covered surface in the model (zone/velo-band/class/count crosses, ump, exposure); holes are the entire PASSIVITY axis (no zone-swing or two-strike-take rate anywhere — called-K path invisible), the two-strike-by-class putaway cell, and the single most conspicuous gap of the whole audit: the #1 feature family (xpa_x_*, top on 13/24 heads) has NO bb or sb member — the new xbb head shipped with zero exposure coverage, and sb (the flagged repair head) lacks both the opportunity decomposition and any rookie MiLB steal prior (bat_sprint is NaN exactly at debut). PRODUCTION (rbi/run/hrr families): deepest context machinery in the repo, but the 07-14 heads rbi2/run2/hrr4 shipped WITHOUT the direct threshold-history features their hrr2/hrr3 siblings have; the neighbor matrix is asymmetric (no ahead-SLG, behind-OBP, or behind-GIDP despite the intermediates sitting merged in the frame); LOB is loaded with zero consumers (realized traffic vs conversion invisible); team cluster-luck (#20) never reached the batter frame. STARTER heads (k/outs/pbb/pha/per): stuff and workload near-saturated (pd_* incl. movement/bands/sequencing, #16/#17/#25-27), but the STARTS FRAME is the systematically shortchanged grain — it never received the environment/context carriers every other frame got: no lineup damage/OBP view (whiff-only lu_*), no own-bullpen state, no contact-shape-allowed beyond one xwOBA scalar, no realized hits-allowed rate at all (no hit-luck mirror), no wind carry, no run-game signal, no unearned-run axis (per's label is literally defined by that split), no bio, no pitch-economy ratio. Largest gap count of any family; nearly all are cheap cross-frame ports of already-shipped mechanisms. GAME heads (total/team_total/winner): means well covered post-07-14; structural holes are the starter-vs-pen INNINGS SPLIT (quality of both units priced, exposure weight absent), a completely absent walk channel, handedness at team grain (real encoding gap, weak measured value), and venue run-DISPERSION for tail pricing (a pricing-layer item, not a mean feature). Bridge-pen quality measured ~95% redundant (GRAY). The winner frame's compactness is principled and correctly left untouched; linescores were measured EMPTY for GBM features (every inning-grain trait fails persistence testing) and stay a grading/sim-validation asset. FLAG (verified by me with fresh greps): backlog #22 (HL arm availability) carries a BUILD verdict in FEATURE_BACKLOG.md:211 but was never built — no availability column in features.py, absent from FINISH_PLAN's Phase-1 list — contradicting the batch's all-built premise; reconciliation is a gray item below. Bottom line: after this build list the engineered-feature surface over the existing base data would be essentially complete; the remaining headroom lives in the sim and pricing layers (hazard v2, dispersion pricing), not in new GBM columns.

## Ranked build items — full specs

### [1] bb_sb_exposure_products  (cost S, value high)
- **Frames:** batter
- **Heads:** bb, xbb, sb (selection votes per head)
- **Rationale:** Completes the model's documented #1 feature class: bb and sb are the ONLY batter heads with no rate-x-opportunity member, the new xbb head has zero exposure coverage, and sb is the top-priority repair head under the dynamic inverse-performance rule (sim steal layer earned w=0, so feature lift is the live route). Trees cannot multiply xpa_slot by the rates; no existing column carries the product.
- **Synthesis spec:** features.py add_batter_derived: (a) at :2337 xpa_x_bb = xpa_slot*c_bb_pct_sh and xpa_x_sb = xpa_slot*c_sb_pa_sh; (b) at :2375 decayed siblings xpa_x_bb_d/xpa_x_sb_d from d_*_sh (the #31 pattern); (c) pen walk rate as RAW cum ratio mirroring pen_k_bf — NOT shrunk (spec correction): _attach_context :2027 pen_bb_bf = pen_cum_BB/pen_cum_BF plus the same key in Stores.bullpen (:4726) and its None-fallback; then xpen_bb = xpa_pen*(pen_bb_bf - ps_bb_bf) at :2368. Expose all in batter_feature_cols (exposure block ~:3104/:3137, decayed ~:3141); pen_bb_bf optionally as a main beside pen_k_bf. ~8 lines across shared train/serve functions; all inputs already serve.

<details><summary>Verifier record: bb_sb_exposure_products (BUILD)</summary>

- refined_spec: All in Model/features.py add_batter_derived (shared train/serve, parity-safe). (a) At :2337 add: df["xpa_x_bb"] = df["xpa_slot"] * df["c_bb_pct_sh"]; df["xpa_x_sb"] = df["xpa_slot"] * df["c_sb_pa_sh"]. (b) At :2375 (per #31 pattern) add: df["xpa_x_bb_d"] = df["xpa_slot"] * df["d_bb_pct_sh"]; df["xpa_x_sb_d"] = df["xpa_slot"] * df["d_sb_pa_sh"]. (c) pen walk rate — CORRECTION to candidate: raw cum ratio, NOT shrunk, mirroring pen_k_bf exactly: in _attach_context at :2027 add rows["pen_bb_bf"] = rows["pen_cum_BB"] / rows["pen_cum_BF"]; in Stores.bullpen (:4726) add f"{prefix}_bb_bf": row["cum_BB"] / row["cum_BF"] plus the same key in the None-fallback dict (:4724). Then at :2368 add df["xpen_bb"] = df["xpa_pen"] * (df["pen_bb_bf"] - df["ps_bb_bf"]). (d) Expose: append xpa_x_bb, xpa_x_sb, xpen_bb to the exposure block of batter_feature_cols (~:3104/:3137) and xpa_x_bb_d, xpa_x_sb_d to the decayed block (~:3141); pen_bb_bf may also be exposed alongside pen_k_bf (:2989) as a main. Inputs all in built frames from Data/mlb_game_batting.csv (BB, SB, PA) and Data/mlb_game_pitching.csv (BB, BF, GS==0). SHAP selection remains the decider — these enter the superset and the vote keeps per-head. No new shrinkage needed: c_sb_pa_sh already carries K=200, c_bb_pct_sh K=120.
- evidence: features.py:2332-2337 = xpa_x_{hr,hit,tb,k,rbi,r} only; :2373-2375 decayed {hr,tb,k}_d only; :2366-2368 xpen_{k,hr,h} only — no bb/sb member exists. Exposed batter bb/sb columns exhaustively enumerated from exposed_cols.txt line 2: c_bb_pct(_sh), c_sb_pa_sh, c_sb_succ, d_bb_pct_sh, d_sb_pa_sh, r7/15/30_sb_pa_sh, s_bb_pct(_sh), s_sb_pa_sh, mix_bb, p_kbb, pc/ps_bb_bf, psb_sb27, psb_stop, tsb_sb_g, tsb_stop, ump_bb_pct, ump_bb_x_pbb, lg_bb_pa, lg_sb_pa, milb_bb, toff_bb_pct, c_ibb_pa — all plain rates, environment, or rate-x-rate collisions; zero PA-scaled. pen_bb_bf never derived (features.py:2025-2027 does HR/SO/H only) though _bullpen_table (:618-621) cumulates BB and pen_cum_BB/pen_cum_BF sit in the frame (frames_columns.txt line 3). FEATURE_BACKLOG #15 shipped K/HR/H pen deltas only, #31 shipped hr/tb/k decayed only; Part 1 bench + Decline ledger contain nothing bb/sb-exposure related. Data: mlb_game_batting.csv 617,988 rows 2015-2026, PA/BB/SB 100% non-null, league BB/PA=0.0840 and SB/PA=0.0151 match SHRINK priors (0.085/0.016) exactly; mlb_game_pitching.csv GS==0 rows 174,795, BF/BB 100% non-null, pen BB/BF=0.0933. Serving: add_batter_derived (features.py:2200) is called by predict.py:513 on serving rows; c_bb_pct_sh already consumed there (mix_bb :2342); ps_bb_bf served via Stores.starter_feats (:4627); Stores.bullpen (:4721-4729) has cum_BB in pen_tab, needs one new key.
- notes: Family-completion of the model's documented #1 feature class (xpa_x_* top on 13/24 heads): bb and sb are the ONLY batter heads with no rate-x-opportunity member, and the new xbb count head (H6) has zero exposure coverage. sb is a flagged weak head (standing sb-repair monitor; dynamic rule = inverse-performance priority). One spec fix applied: existing pen_*_bf rates are raw cum ratios, not shrunk — the candidate's 'shrunk' wording would break idiom parity. Trees cannot multiply xpa_slot by the rates, and no existing column carries the product (mix_bb is rate-x-rate with no PA scaling; ump_bb_x_pbb is umpire-x-starter). Cost is ~8 lines across two shared-path functions plus cols-list appends; rides the next full chain per FINISH_PLAN, no separate retrain.

</details>

### [2] deep_threshold_hist  (cost S, value medium)
- **Frames:** batter
- **Heads:** rbi2, run2, hrr4 primary; xrbi/xrun, hrr2/hrr3 secondary
- **Rationale:** The 07-14 batch shipped these three heads WITHOUT the direct threshold-history features hrr2/hrr3 have (all six siblings survived selection). RBI is overdispersed (var/mean 1.64) so mean rates cannot price the tail; strongest for rbi2, run2 rides free (R near-Poisson). Watch head ECE in the paired read — the _HRR_HIST family was once benched for hrr2_ece; the standing paired-eval process is the designed arbiter.
- **Synthesis spec:** features.py load_raw after :251: RBI2=(RBI>=2), RUN2=(R>=2), HRR4=(H+R+RBI>=4) as float indicators; add all three to BAT_STATS (:441) and DECAY_STATS (:52). Generalize HRR_SHRINK (:1615) to THRESH_SHRINK with priors MEASURED on <=2024 logs (hrr4 0.139, rbi2 0.085, run2 0.064 — corrected from the candidate's 2025 rates), K=40 by convention. Generalize hrr_hist_feats (:1793) to an iterable (sum_key,out_stem) map and extend the four call sites (vectorized :2822; inference :4154/:4172/:4209) -> 9 cols c/s/d_{rbi2,run2,hrr4}_g_sh. Serving automatic via the shared helper; frames.joblib invalidated.

<details><summary>Verifier record: deep_threshold_hist (BUILD)</summary>

- refined_spec: In features.py load_raw (after line 251): gb["RBI2"]=(pd.to_numeric(gb["RBI"])>=2).astype("float64"); gb["RUN2"]=(pd.to_numeric(gb["R"])>=2).astype("float64"); gb["HRR4"]=(_hrr>=4).astype("float64"). Add "RBI2","RUN2","HRR4" to DECAY_STATS (features.py:51-52) and BAT_STATS (features.py:441 list). Extend HRR_SHRINK (features.py:1615) to a general THRESH_SHRINK: {"hrr2_g":(0.395,40.0),"hrr3_g":(0.250,40.0),"hrr4_g":(0.139,40.0),"rbi2_g":(0.085,40.0),"run2_g":(0.064,40.0)} — priors measured <=2024 on the full game-batting log (same population as the HRR priors), K=40 by convention. Generalize hrr_hist_feats (1793-1801) to iterate a (sum_key, out_stem) map: {"HRR2":"hrr2_g","HRR3":"hrr3_g","HRR4":"hrr4_g","RBI2":"rbi2_g","RUN2":"run2_g"}, emitting {pre}_{stem}_sh = (sums[key]+k*prior)/(den+k). Extend the sums dicts at the four call sites: vectorized (2822: add HRR4/RBI2/RUN2 from {src}_ cols — they exist automatically once in BAT_STATS/DECAY_STATS), inference zero-sums (4154), c/s (4172, frame[BAT_STATS].sum() already covers them), decayed (4209, dks covers DECAY_STATS). Exposes 9 cols on the batter frame: c/s/d_rbi2_g_sh, c/s/d_run2_g_sh, c/s/d_hrr4_g_sh. Inputs: Data/mlb_game_batting.csv RBI, R, H (100% non-null, 2015-2026). Serving: automatic via shared helper + Stores (selftest parity by construction). Routing: PROP_EXCLUDE={} so SHAP selection decides per-head; watch rbi2/run2/hrr4 ECE in the paired read given the hrr2_ece/xhrr-MAE precedent for this family. Frames.joblib cache invalidated (feature-construction change).
- evidence: EXISTS: no — frames_columns.txt has only y_rbi2/y_run2/y_hrr4 (targets); no *_rbi2/_run2/_hrr4_g_sh anywhere. Exposed nearest: c/s/d_hrr{2,3}_g_sh (sibling thresholds, all 6 confirmed in exposed batter list) + mean-only c/s/d/r7/r15/r30_{r,rbi}_pa_sh, rbi_conv, run_opp, xpa_x_rbi. FEATURE_BACKLOG H1 (line 300) / H4 (line 382) built heads+targets+wiring only; not in Decline ledger (4 items, unrelated). CODE: indicator block features.py:249-251; cum lists BAT_STATS:441 + DECAY_STATS:52 contain HRR2/HRR3; hrr_hist_feats:1793-1801 shared helper; vectorized call 2819-2824; inference calls 4154 (zero-sums), 4172 (c/s), 4209 (decayed) — parity by shared helper. train.py:413 PROP_EXCLUDE={} (curated routing overridden; SHAP selection sole decider), so new cols route via selection. DATA (pandas run): mlb_game_batting.csv 617,988 rows 2015-2026, H/R/RBI 100% non-null; <=2024 rates rbi2 0.0851, run2 0.0643, hrr4 0.1393 (candidate's 0.093/0.072 were 2025 rates); RBI var/mean 1.64 (overdispersed), R var/mean 1.06 (near-Poisson). Caveat precedent: _HRR_HIST historically benched for hrr2_ece and routed off xhrr (train.py:222-227, 397-400) — own-target threshold history has caused calibration harm before; paired eval arbitrates.
- notes: Strongest for rbi2 (no RBI threshold history exists at any line; var/mean 1.64 means the mean rate cannot price the tail). hrr4 moderate (hrr2/hrr3 shares already trace the tail at 2 and 3; the 4-line adds curvature only). run2 weakest (R var/mean 1.06 near-Poisson, so E[R] + xpa_bat largely determines P(R>=2)) — but it rides the same 3-line change for free. Candidate's prior values corrected: 0.085/0.064/0.139 (<=2024 full log), not the 2025 rates it quoted. Risk is not existence/leakage/serving but head-level calibration: this exact family was benched once for hrr2_ece and routed off xhrr for CI-clear MAE harm — the standing paired-eval + selection process is the designed arbiter, and MIXED blocks per accept-policy.

</details>

### [3] hit_type_rates_plus_park_products (merged: hit_type_shrunk_rates + park_x_2b_and_park_x_3b)  (cost S, value medium)
- **Frames:** batter
- **Heads:** triple, double, single, tb3, tb4, hits2, xh, xtb, tb2
- **Rationale:** The new triple head (1.21% base, thinnest binary on the board) currently has NO realized-triples history — 3B/AB is a stable skill (y2y r=0.451) unrecoverable by axis-aligned trees from the fused iso/xbh/tb blends. The park products extend the proven hand-built park_x_hr template to the two hit types that never got it; the backlog's x1b/x2b/x3b decline covered count HEADS only, not these features. Products hard-depend on the rates — one merged build.
- **Synthesis spec:** Three SHRINK entries (features.py:1583): '1b_ab':(0.160,460) with composite numerator H-2B-3B-HR special-cased in BOTH shared helpers (:1776,:1911, iso precedent); '2b_ab':(0.049,300,'2B','AB'); '3b_ab':(0.0046,500,'3B','AB') — priors measured on the file, Ks by convention. Auto-yields 6 cols (c_/s_ siblings) via SHRINK_COLS registration; serving automatic (BAT_STATS already sums 2B/3B, shrunk_from_sums at :4171). Do NOT add rolling/decayed variants. Then two products cloning park_x_hr (:2352) in the shared derived block: park_x_2b = park_2b_pg*c_2b_ab_sh; park_x_3b = park_3b_pg*c_3b_ab_sh (park factors already exposed as-of mains).

<details><summary>Verifier record: hit_type_shrunk_rates (BUILD)</summary>

- refined_spec: Add three entries to SHRINK (features.py:1583): "1b_ab": (0.160, 460, None, "AB") with composite numerator H-2B-3B-HR special-cased next to the iso branch in BOTH shared helpers (shrunk_from_sums:1776 and _bat_rates_shrunk:1911 — numer = sums["H"]-sums["2B"]-sums["3B"]-sums["HR"]); "2b_ab": (0.049, 300, "2B", "AB"); "3b_ab": (0.0046, 500, "3B", "AB") (Ks by convention like BIP_SHRINK/PVLOC, not swept). Yields six columns c_1b_ab_sh/c_2b_ab_sh/c_3b_ab_sh + s_ siblings automatically: SHRINK_COLS (1751) registers them, _bat_rates_shrunk builds training-path values from the existing c_/s_ cumsums (c_2B/c_3B/c_H/c_HR/c_AB confirmed in built frame), serving path is automatic via batter_feats:4171 out.update(shrunk_from_sums(sums, pre)) since BAT_STATS already includes 2B/3B; cold-start (4140) lands on priors. Leakage identical to c_xbh_ab (same inclusive cumsums, allow_exact_matches=False). Do NOT add to SHRINK_ROLL or DECAY_SHRINK (no rolling/decayed variants — rolling sums track no 2B/3B). Frames: batter. Route to heads via SHAP selection as usual; most direct consumers: triple, double, single, tb3/tb4, hits2, xh, xtb.
- evidence: exposed_cols.txt batter frame (385): nearest cols = c_iso/s_iso, c_iso_sh/s_iso_sh, c_xbh_ab/s_xbh_ab, c_h_ab(_sh), c_tb_ab(_sh), c_hr_pa(_sh), park_2b_pg/park_3b_pg, milb_b1/b2/b3 (MiLB priors only), bat_sprint/bat_hp1b/bat_leg_hits/bat_brr — no MLB-realized 1B/2B/3B rate in any encoding (ROLL_STATS/DECAY_STATS exclude 2B/3B). frames_columns.txt batter frame carries c_2B/c_3B/c_H/c_HR/c_AB + s_ siblings as intermediates. features.py: SHRINK dict at 1583 with iso composite-numerator precedent at 1776/1911; both shared helpers (shrunk_from_sums:1769, _bat_rates_shrunk:1906) iterate SHRINK generically; SHRINK_COLS:1751 auto-registers c_/s_ names; serving batter_feats:4157-4171 sums BAT_STATS (includes 2B/3B, line 439-441) and calls shrunk_from_sums — zero serving code needed; cold-start zero-sums path at 4140 lands on priors. Pandas on Data/mlb_game_batting.csv: 617,988 rows 2015-2026, AB/H/2B/3B/HR 100% non-null; pooled priors 1B/AB=0.1599, 2B/AB=0.0492, 3B/AB=0.00456 (match proposal); y2y r (1,859 AB>=250 pairs): 3B/AB 0.451, 2B/AB 0.252, 1B/AB 0.598; 0 negative game-grain singles. FEATURE_BACKLOG decline ledger (line 426-427) declines x1b/x2b/x3b COUNT HEADS only — not these input features; no Part-1 bench found.
- notes: Every quantitative claim in the proposal verified exactly (priors, y2y correlations, frame intermediates, serving-path sums). The triple head (1.21% base, thinnest binary on the board) currently has NO realized-triples history — only speed proxies + park_3b_pg — and 3B/AB is unrecoverable by trees (3-term linear combo across differently-shrunk, differently-denominated exposed cols; hr exposed per-PA with no AB/PA ratio). Value is concentrated on the thin hit-type heads (triple/double/single) with marginal spillover to tb3/tb4/hits2/xh/xtb; not a game-head mover, hence medium not high. Backlog decline of x1b/x2b/x3b is heads-only and does not cover these features. Rare-event thinness is handled by the proposal's own strong-K shrinkage (500 for 3B).

</details>

<details><summary>Verifier record: park_x_2b_and_park_x_3b (BUILD)</summary>

- refined_spec: Two lines cloning features.py:2352 in the same derived block: df["park_x_2b"] = df["park_2b_pg"] * df["c_2b_ab_sh"]; df["park_x_3b"] = df["park_3b_pg"] * df["c_3b_ab_sh"]. HARD DEPENDENCY: c_2b_ab_sh/c_3b_ab_sh must first be added via the hit_type_shrunk_rates sibling (two SHRINK dict entries at features.py:1583 — "2b_ab": (0.049, ~300, "2B", "AB"), "3b_ab": (0.0046, ~500+, "3B", "AB"); measured league priors above; 3B needs a strong K, it is the thinnest rate on the board). Inputs: mlb_game_batting.csv 2B/3B/AB (already in BAT_STATS line 440) for the batter rates; park_cum_2B/park_cum_3B as-of venue tables (mlb_games.csv+game batting, ≥30-game gate, features.py:2030-2040) for the factors. Frame: batter only. Serving: automatic — the derived block is shared train/serve, ServingStore.park() already emits park_2b_pg/park_3b_pg, shrunk rates serve via batter-history sums; new-venue NaN propagates like park_x_hr. Route to heads double/triple/tb2/tb3/tb4/xtb via the same keep-list mechanism as train._PARK_OFF. Schedule with the next frame rebuild (frames.joblib cache invalidated per HANDOFF_0714).
- evidence: features.py:2352 df["park_x_hr"]=df["park_hr_pg"]*df["c_hr_pa_sh"] confirmed as the shipped template, inside the shared train/serve derived block (same block as mix_*/xpa_x_*/rbi_conv). No park_x_2b/park_x_3b in features.py or either scratchpad inventory. Dependency cols c_2b_ab_sh/c_3b_ab_sh do NOT exist: SHRINK dict (features.py:1583-1600) has no 2b_ab/3b_ab entries; frames carry only raw cum counts c_2B/c_3B/s_2B/s_3B and combined UNSHRUNK c_xbh_ab/s_xbh_ab (features.py:4167 sums 2B+3B+HR — no split). FEATURE_BACKLOG decline ledger covers hit-type COUNT HEADS (x1b/x2b/x3b), not rate features — no bench applies; H3 spec explicitly added park_3b_pg as a main for the triple head but never the product. Park factors: as-of cum per-game venue rates over PRIOR games, ≥30-game gate (features.py:2030-2040); served by ServingStore.park() features.py:~4804-4820 (both 2B and 3B in the serve loop). Exposed batter mains verified in exposed_cols.txt: park_2b_pg, park_3b_pg, park_x_hr, c_hr_pa_sh all present. Data check: mlb_game_batting.csv 617,988 rows 2015-2026, 2B/3B 100% non-null; league 2B/AB=0.0492, 3B/AB=0.0046 (2025: 0.0038).
- notes: Verdict is BUILD conditional on the hit_type_shrunk_rates dependency shipping — if that sibling is rejected there is no valid fallback multiplicand (c_xbh_ab is unshrunk and conflates HR; c_iso_sh conflates HR power). Combined cost of dependency + products is still S (~4 lines in existing idioms). Value capped at medium: park 2B/3B factors are less venue-skewed than HR factors, and shrinkage will pull most batters' 3B rate to league (triple base rate 1.21%), so the 3B product mostly separates true speed/gap outliers in triple-friendly parks — which is precisely the geometry the triple/tb4 heads lack. Selection (SHAP votes) remains the arbiter per standing policy.

</details>

### [4] sb_opportunity_chain  (cost S, value medium)
- **Frames:** batter
- **Heads:** sb primary; run, run2 spillover
- **Rationale:** Intent (attempts per time-on-first) is a triple ratio trees cannot carve from the fused per-PA steal rates, and nothing crosses the batter's chain with tonight's starter's traffic/permissiveness. sb is top priority under the dynamic rule. Expect credit-splitting with xpa_x_sb (rank 1) — deliberately separate encodings; that is the vote working.
- **Synthesis spec:** From existing cum sums in the shared helper sites (batch ~features.py:2826, serve ~:4176): c_1B = c_H-c_2B-c_3B-c_HR; ob1_sh = (c_1B+c_BB+c_HBP + 100*0.24)/(c_PA+100); sb_att1 = (c_SB+c_CS + 30*0.09)/(c_1B+c_BB+c_HBP+30) — prior 0.09 per the post-2023 regime (corrected from 0.07). Collisions post-merge: sb_chain = xpa_slot*ob1_sh*sb_att1; sb_chain_env = sb_chain*(psb_sb27 - AS-OF league mean) (as-of centering, never full-sample). Optional ob1_x_st = centered(ob1_sh)*centered(ps_h_bf+ps_bb_bf) for run heads. Expose all; SHAP routes.

<details><summary>Verifier record: sb_opportunity_chain (BUILD)</summary>

- refined_spec: Batter frame + serve helper shared by both paths (house convention, same sites as c_sb_succ: batch ~features.py:2826, serve ~:4176). From existing as-of cum sums in Data/mlb_game_batting.csv (c_H, c_2B, c_3B, c_HR, c_BB, c_HBP, c_SB, c_CS, c_PA — all already in frame and in serve sums dict): (1) c_1B = c_H - c_2B - c_3B - c_HR; ob1_sh = (c_1B + c_BB + c_HBP + K*prior)/(c_PA + K), prior=0.24 (measured 0.2376), K~100. (2) sb_att1 = (c_SB + c_CS + K*prior)/(c_1B + c_BB + c_HBP + K), prior=0.09 (recent-era measured; NOT 0.07 — 2023 rules shifted 0.067->0.10), K~30. Collisions (post-merge where xpa_slot/psb_sb27 live): (3) sb_chain = xpa_slot * ob1_sh * sb_att1. (4) sb_chain_env = sb_chain * (psb_sb27 - as-of lg mean) — center with the lg_sb_pa-idiom as-of roll, never full-sample. Optional 5th: ob1_x_st = (ob1_sh - lg) * ((ps_h_bf + ps_bb_bf) - lg) for run/run2. Expose all in batter frame; SHAP selection routes heads. Targets: sb primarily (currently soft, standing-monitor repair item), run/run2 spillover.
- evidence: Exposed batter frame: all steal rates are SB/PA (c/s/r7/r15/r30/d_sb_pa_sh, successful steals only) + c_sb_succ (SB/(SB+CS)) + psb_sb27/psb_stop/tsb_* env; no reach-first rate, no attempt-per-opportunity, no sb product in the xpa_x_*/park_x_*/ump_k_x_* product family. features.py: sb_pa shrink (0.016, K=200) line 1590; c_sb_succ batch line 2826, serve line 4176 where sums dict already dereferences SB/CS/H/2B/3B/HR/BB/HBP/PA (lines 4158-4181) — serving inputs confirmed in scope. FEATURE_BACKLOG: only sb entry in Decline ledger is the xsb COUNT HEAD (line 426), not this feature; no Part-1 bench, no #15-32 overlap. Data (mlb_game_batting.csv, pandas): 617,988 rows 2015-2026, 100.0% non-null on all 9 input cols; measured league ob1=0.2376, attempt/opportunity=0.0825 overall with 2023 rule-change regime shift (0.067 in 2021 -> 0.104 in 2024, 0.094 in 2026); SB/PA=0.0147.
- notes: Genuinely new axis: intent (attempts per time-on-first) is underivable from existing mains — it equals (sb_pa/sb_succ)/ob1, a triple ratio trees cannot carve; ob1 also not exactly derivable (obp per-PA vs xbh_ab per-AB denominator mismatch). Upside capped by sb being one thin head (~6.5% base) — hence medium not high — but sb is top priority under the dynamic inverse-performance rule and the sim steal layer earned w=0, so feature-level lift is the live route. Expect credit-splitting with d_sb_pa_sh and with sibling proposal xpa_x_sb (bb_sb_exposure_products) if both build — acceptable, vote-friendly. Spec corrections applied: attempt prior 0.09 not 0.07 (post-2023 regime), ob1 prior 0.24 not 0.245, env centering must be as-of.

</details>

### [5] pbip_shape_exposure_and_collisions (merged: expose_pbip_pullair_plus_mix + starts_frame_contact_profile_widening + gb_pitcher_air_dp)  (cost S, value medium)
- **Frames:** batter + starts
- **Heads:** pha, per, outs, k (starts); hr, tb2-4, xtb, hrr2-4, double (batter)
- **Rationale:** The starter heads' own frame is the one grain with zero batted-ball-shape-allowed information (only an xwOBA scalar) while batter/team frames both carry the GB axis — the GBM literally cannot carve air-x-GB or GB-x-GIDP for per/pha/outs today. Pitcher pullair-allowed is a 07-12 exposure oversight (pulled air = 18.4% vs 5.1% HR per air ball). pbip_gb is the load-bearing add; everything shares one existing table so it merges into one build.
- **Synthesis spec:** All exposure/derivation of the ALREADY-BUILT pitcher-BIP as-of table. (a) Batter frame: add pbip_pull, pbip_pullair, pbipd_pullair to batter_feature_cols (~:3036) + mix_pullair = bip_pullair*pbip_pullair beside mix_gb (:2345); columns already computed and served — pick the career-form product only. (b) Starts frame: widen the two bip_feats calls (:3273-3279) to CUM=(xwoba,ev,brl,gb,flyd,pullair) and DK=(xwoba,gb,brl); NaN-mirror the else-branch; expose 7 new pbip_/pbipd_ names; ZERO serving change (predict.py:555 already spreads the full bip_pitcher dict). (c) On top of (b): p_gb_air = (AIR_RHO0 - air_dens)*(pbipd_gb - 0.43) in add_starter_derived (missing-col guard per :3201-3203); lu_gidp=('c_gidp_pa_sh','mean') in the lu groupby (:3338) + 'lu_gidp':'c_gidp_pa_sh' in predict _LU_COLS; gb_dp_x = (pbipd_gb-0.43)*(lu_gidp-0.018). Do not build pbipd_gb twice.

<details><summary>Verifier record: expose_pbip_pullair_plus_mix (BUILD)</summary>

- refined_spec: (1) Add "pbip_pull", "pbip_pullair", "pbipd_pullair" to batter_feature_cols in the pbip block (features.py ~3036-3038, next to pbip_gb/pbipd_gb). (2) In add_batter_derived (features.py:2200 function, next to mix_gb at 2345) add: df["mix_pullair"] = df["bip_pullair"] * df["pbip_pullair"], and add "mix_pullair" to batter_feature_cols beside mix_air/mix_gb (~3098). No new inputs: columns already built by _bip_day_sums(bip, "PitcherId") -> _cum_decay_table -> as-of merge (allow_exact_matches=False); serving already returns them via Stores.bip_pitcher/_bip_entity, and add_batter_derived is shared train/serve (predict.py:513). Frames: batter only. Heads: hr, tb2/tb3/tb4, xtb, hrr2-4, double — SHAP selection votes per head as usual.
- evidence: exposed_cols.txt batter frame: bip_pull/bip_pullair/bipd_pullair exposed, pbip_pull/pbip_pullair/pbipd_pullair ABSENT (pitcher mirrors exposed only for gb/ld/pu/flyd); frames_columns.txt: all three present in built frame. features.py: _bip_day_sums:1196-1197 computes pull_n/pullair_n for both BatterId/PitcherId tables; BIP_SHRINK:998-999 (priors 0.436/0.166, K=60/80); pullair in BIP_DECAYED:1011; mix_air:2273 = (1-bip_gb)*(1-pbip_gb) direction-free; mix_gb template:2345; exposure block 3032-3098 confirms asymmetry; add_batter_derived:2200 shared with predict.py:513; serving Stores.bip_pitcher:4295 -> _bip_entity computes all BIP_SHRINK names incl pull/pullair (4279-4280). FEATURE_BACKLOG.md: no bench/decline of pitcher spray mirrors; 07-12 notes (L500/507) added ld/pu/flyd mirrors, skipped spray. Pandas on mlb_statcast_bip.csv: 1,376,160 rows 2015-2026; HcX 98.37% / Stand+BBType+PitcherId 100% non-null; reproduced _spray_flags (PULL_DEG=15): pull 43.3%, pullair 15.9% (match priors); 63.1% of 62,778 HRs pull-side; HR rate 18.40% on pulled-air vs 5.08% other air; pitcher pullair-allowed (n>=300, 1113 pitchers) p10/p50/p90 = 11.9/15.7/19.9%, sd 3.12pp, residual sd 2.03pp after regressing out air share (corr 0.761 with 1-gb) — not recoverable from pbip_gb.
- notes: Proposal's 64.8% pull-HR figure measures 63.1% on the actual file — directionally exact. Value is real but capped by 0.76 corr between pullair-allowed and existing air-share-allowed (residual sd 2.03pp on an 18.4%-vs-5.1% HR-rate channel); shrinkage already set (K=80, same as exposed batter side). Spec choice of career-form product (bip_pullair*pbip_pullair) follows mix_gb; a decayed variant (bipd_pullair*pbipd_pullair, mix_ld form) is an equally valid alternative — pick one, not both. Four new cols total; per-day incremental compute ~zero.

</details>

<details><summary>Verifier record: gb_pitcher_air_dp (BUILD)</summary>

- refined_spec: (a) In build_starts_frame next to features.py:3275: st["pbipd_gb"] = bip_feats(lambda c: st[f"pb_dk_{c}"] * wdn_bip, "t", ("gb",))["t_gb"] (optionally st["pbip_gb"] from pb_cum_* the same way); add to starts_feature_cols (features.py:3460 block). Serving is already emitted by Stores.bip_pitcher — zero serving code. (b) In add_starter_derived (shared, near p_conv_c features.py:3204): df["p_gb_air"] = (AIR_RHO0 − df["air_dens"]) * (df["pbipd_gb"] − BIP_SHRINK["gb"][0]); guard with the same if-missing-NaN pattern as features.py:3201-3203. (c) Add lu_gidp=("c_gidp_pa_sh","mean") to the lu agg at features.py:3338 and "lu_gidp": "c_gidp_pa_sh" to predict.py:516 _LU_COLS; then in add_starter_derived: df["gb_dp_x"] = (df["pbipd_gb"] − 0.43) * (df["lu_gidp"] − SHRINK["gidp_pa"][0]). Expose pbipd_gb, p_gb_air, lu_gidp, gb_dp_x as starts mains; selection votes per head (per/pha get (a)+(b); outs gets (a)+(c)). Inputs: Data/mlb_statcast_bip.csv BBType/PitcherId/Date (already aggregated by _bip_table into pb_* sums on sf); mlb_weather-derived air_dens (existing starts main); Data/mlb_game_batting.csv GIDP/PA via existing c_gidp_pa_sh on the batter frame. Note: if the sibling proposal starts_frame_contact_profile_widening ships, part (a) is subsumed and this reduces to (b)+(c) on top of it — do not build pbipd_gb twice.
- evidence: exposed_cols.txt starts frame (134 cols): only pbip_xwoba/pbipd_xwoba for contact allowed — no gb/shape col, no lineup batted-ball lu_* (all K/whiff/chase-side). frames_columns.txt sf (53274x448): pb_cum_gb_n, pb_dk_gb_n, pb_cum_n, pb_dk_n ALL already merged as intermediates (features.py:3265-3270). Idiom precedents verified: BIP_SHRINK["gb"]=(0.43,60,"gb_n","n") features.py:997; the exact bip_feats-gb-over-pb_dk call already ships at game-frame level for ps_gb_d (features.py:3525-3529); air_fly=(1.165−air_dens)×bip_pullair features.py:2247 (AIR_RHO0=1.165, line 2142); centered-product idiom chase_matchup features.py:2278; SHRINK["gidp_pa"]=(0.018,200,"GIDP","PA") features.py:1598; lu groupby features.py:3337-3347. Serving: predict.py:555 spreads **s.bip_pitcher(pid,date) into starter rows and Stores._bip_entity (features.py:4286-4287, BIP_DECAYED includes "gb" line 1011) ALREADY emits pbip_gb/pbipd_gb at serve time — currently dropped; _LU_COLS predict.py:516 is the lu_gidp hook; F.add_starter_derived shared at predict.py:579 gives parity. Backlog: no bench of gb-allowed/gidp anywhere (Part 1 = pull_fence/porch_margin/d_ps_xwcon_d/lg_* only; #28 took xwoba only per comment features.py:3261-3264). Pandas: mlb_statcast_bip.csv 1,376,160 rows 2015-2026, BBType 100% non-null, ground_ball share 0.4415, PitcherId 100%; mlb_game_batting.csv 617,988 rows 2015-2026, GIDP 100% non-null, GIDP/PA 0.0188.
- notes: Strongest audited claim held up end-to-end: the starter heads' own frame is the one grain with zero batted-ball-shape-allowed information (only xwoba damage), while batter/team frames both carry the GB axis — the GBM literally cannot carve air×GB or GB×GIDP for per/pha/outs today. Part (a) is the load-bearing piece and is near-free (training = 2 lines, serving already computes it). (b)/(c) are cheap shipped-idiom centered products; value concentrated in per (HR/ER insulation) and outs (DP conversion). Priors in the proposal match measured data exactly (gb 0.4415 vs 0.43 prior; GIDP/PA 0.0188 vs 0.018 prior). Frames cached 12:30 predate this — adding these columns is a feature-construction change, so frames.joblib must be rebuilt (per HANDOFF_0714 reuse rule).

</details>

<details><summary>Verifier record: starts_frame_contact_profile_widening (BUILD)</summary>

- refined_spec: features.py build_starts_frame, lines ~3273-3279: widen the two bip_feats calls from ("xwoba",) to CUM = ("xwoba","ev","brl","gb","flyd","pullair") and DK = ("xwoba","gb","brl") — e.g. `for k,v in bip_feats(lambda c: st[f"pb_cum_{c}"], "pbip", CUM).items(): st[k]=v` and `for k,v in bip_feats(lambda c: st[f"pb_dk_{c}"] * wdn_bip, "pbipd", DK).items(): st[k]=v` (bip_feats prefixes names itself, so "pbip"/"pbipd" tags yield the exact batter-frame names with identical shrink constants). Mirror the else-branch (3277-3279) to NaN-fill all 9 emitted cols. Add 7 new names to starts_feature_cols: pbip_gb, pbipd_gb, pbip_brl, pbipd_brl, pbip_ev, pbip_flyd, pbip_pullair. Inputs: pb_cum_*/pb_dk_* sums already as-of-joined at features.py:3270 from _bip_table(raw["bip"], "PitcherId") — no new reads, no new joins. Serving: NO change — predict.py:555 already spreads the full s.bip_pitcher(pid,date) dict (features.py:4286-4287 returns every name); parity by construction. Selection votes per starter head (pha/per/outs/k). Note: pbip_pullair is a first exposure in any frame (batter frame only has batter-side bip_pullair) but serves identically.
- evidence: exposed_cols.txt starts (134): only BIP-derived cols are pbip_xwoba/pbipd_xwoba; no gb/brl/ev/flyd/pullair shape anywhere on the frame (pd_*=discipline, pc_/ps_/p5_hr_bf=outcome rates, pars_*=arsenal whiff). Team frame DOES expose opp_ps_gb_d/opp_ps_brl_d for the same pitcher — asymmetry claim verified. frames_columns.txt sf (53,274 x 448): all needed raw sums already joined (pb_cum/dk_{gb_n, brl_n, ev_sum, ev_n, fld_sum, fld_n, pullair_n, hc_n, n}). features.py:3265-3279 confirms the xwoba-only bip_feats call (scoped by backlog #28's p_conv spec, not a bench — FEATURE_BACKLOG Part 1 (5 items) and Decline ledger (4 items) contain nothing about this). Serving: predict.py:555 already spreads **s.bip_pitcher(pid,date) into the starts row; Stores._bip_entity (features.py:4244-4290) returns ALL BIP_SHRINK+BIP_DECAYED names as pbip_/pbipd_ — extra keys computed today and discarded by the col filter. Data: mlb_statcast_bip.csv = 1,376,160 rows, 2015-04-05 to 2026-07-12; PitcherId/BBType 100% non-null, LSA/ExitVelo 98.8%, HcX 98.4%, xwOBA 98.5%; 2,713 pitchers; BBType = ground_ball 607,572 / line_drive 338,596 / fly_ball 332,757 / popup 97,235. All 7 names exist in BIP_SHRINK (features.py:990-1009) with fixed priors/Ks; gb/brl/ev/flyd/pullair all in BIP_DECAYED where needed.
- notes: Strongest single add is pbip_gb (carried by nothing on the frame; GB share drives BABIP/GIDP/HR-suppression, orthogonal axes of the xwcon scalar the GBM cannot decompose). pbip_brl/pbip_flyd are partially proxied by pc_/ps_/p5_hr_bf outcome rates but stabilize far faster (K=80/40 BIP vs rare HR events), so value is real but incremental — hence medium, with feature_select voting per head as the arbiter. Early-career pitchers NaN until first tracked BIP (same as existing pbip_xwoba; 2015+ window fully covered). If this ships, the separate gb_pitcher_air_dp candidate gets its pbipd_gb input for free, as the proposal notes. This is a training-frame + cols-list change only; it must ride the next full chain per FINISH_PLAN (tree currently mid-batch, LGBM_ONLY_TEMP=True) — not a standalone retrain.

</details>

### [6] own_pen_leash_state  (cost S, value medium)
- **Frames:** starts
- **Heads:** outs primary; per, pha, k secondary
- **Rationale:** Proposed independently by two auditors. The starts frame exposes ZERO bullpen columns, yet the manager's pull decision runs on his OWN pen's fatigue/quality — team_st_outs_pg is static policy, this is the dynamic day-of input. Targets the outs head's acknowledged headroom (hazard v2 queued). Pure re-key + exposure of leakage-vetted tables.
- **Synthesis spec:** Re-key existing pen tables to the starter's OWN team in build_starts_frame (~features.py:3253): (1) pen_tab renamed cum_* -> openp_cum_* (distinct prefix — plain pen_cum_* is taken by opponent-keyed intermediates), _asof_merge by [Team,Season] -> own_pen_era = openp_cum_ER*27/openp_cum_Outs; (2) _bullpen_hl_table renamed openhl_ -> own_pen_hl_era; (3) _pen_fatigue_table renamed pen_np_l3 -> own_pen_np_l3, exact-date merge (table pre-shifted). Expose 3 cols. Serving in predict._starter_rows (~:556): s.bullpen(team,...,prefix='own_pen'), s.bullpen_hl(...), s.pen_fatigue(team,date) — all stores exist.

<details><summary>Verifier record: own_pen_leash_state (BUILD)</summary>

- refined_spec: Training, in build_starts_frame after the team_st_outs_pg block (features.py:~3253), reusing the pen_tab already built at line 3223: (1) own = pen_tab.rename cum_* -> openp_cum_* (distinct prefix — plain pen_cum_* names are already taken by the opponent-keyed intermediates from _attach_context); st = _asof_merge(st, own, by=["Team","Season"]); st["own_pen_era"] = openp_cum_ER*27/openp_cum_Outs. (2) hl = _bullpen_hl_table(gp) renamed cum_* -> openhl_cum_*; _asof_merge by ["Team","Season"]; st["own_pen_hl_era"] = openhl_cum_ER*27/openhl_cum_Outs. (3) fat = _pen_fatigue_table(gp).rename(columns={"pen_np_l3":"own_pen_np_l3"}); st = st.merge(fat, on=["Team","Date"], how="left") (table is pre-shifted, exact-date merge safe). (4) Append ["own_pen_era","own_pen_hl_era","own_pen_np_l3"] to starts_feature_cols. Serving, in predict.py _starter_rows (~line 556, team in scope): row["own_pen_era"] = s.bullpen(team, season, date, prefix="own_pen")["own_pen_era"]; row.update(s.bullpen_hl(team, season, date, prefix="own_pen_hl")) (yields own_pen_hl_era; drop or ignore own_pen_hl_k_bf); row["own_pen_np_l3"] = s.pen_fatigue(team, date). Inputs: Data/mlb_game_pitching.csv relief rows (GS==0: NP, ER, IP->Outs, BF, SV, HLD, GF, Date, Team) via existing _bullpen_table/_bullpen_hl_table/_pen_fatigue_table. Frame: starts. Heads: outs primary; per/pha/k secondary — selection votes per head as usual.
- evidence: exposed_cols.txt starts list (134 cols): zero pen_* columns; nearest = team_st_outs_pg, p_short_share, p_outs_sd, p_np_l3, is_dh/dh_game2 (all static policy or own-history, no day-of pen state). frames_columns.txt sf (53,274x448): pen_cum_BF/HR/SO/BB/ER/Outs/H/n + pen_hr_bf/k_bf/h_bf/era present as OPPONENT-keyed intermediates only — features.py:2003-2006 (_attach_context keys pen_tab by Opponent) and features.py:3226 (build_starts_frame calls _attach_context WITHOUT pen_hl_tab/pen_fat_tab), so no own-pen, no HL, no fatigue in the starts frame at all. FEATURE_BACKLOG Part 1 bench table (items 1-5) and Decline ledger: no own-pen-for-starts entry; #15/#16/#22 confirmed distinct (batter-frame exposure / static leash / opponent HL arms). Leakage: _asof_merge allow_exact_matches=False (features.py:1461); _pen_fatigue_table rolling(3D).shift(1) (features.py:649). Serving: Stores builds pen_tab/pen_hl_tab/_pen_fat (features.py:4019-4023); bullpen/bullpen_hl/pen_fatigue take arbitrary team+prefix (features.py:4721-4746); predict.py:_starter_rows has team in scope and already calls team-keyed s.team_st_outs(team,...) at line 556. Data (pandas run): mlb_game_pitching.csv 228,069 rows 2015-2026, 174,795 relief rows, NP/ER/BF/SV/HLD/GF 100% non-null every year (Outs derived from IP at load, features.py:253); HL subset 80,436 rows (46% of relief).
- notes: Confirmed real gap, not a bench: the unexposed pen_* intermediates in the starts frame are the OPPONENT's pen (irrelevant byproduct of _attach_context reuse), and no backlog/ledger item covers own-pen state for starter heads. All three tables and serving stores already exist and are leakage-vetted for other frames — this is purely a re-key (Team instead of Opponent) plus exposure. Naming must avoid the existing opponent-keyed pen_cum_* intermediates in the starts frame (use openp_/openhl_ intermediate prefixes). own_pen_hl_era is NaN until a team's first SV/HLD/GF relief appearance each season — matches existing pen_hl_era behavior, GBM-imputable, no shrinkage needed (dense team-level cums). Value rated medium not high: mechanism is real and targets the outs head's acknowledged headroom (hazard v2 queued), but manager behavior is noisy and team_st_outs_pg/dh flags already carve a coarse static version; the dynamic fatigue/quality deltas are the genuinely new geometry.

</details>

### [7] bd_passivity_axis (stage 1 only)  (cost S, value medium)
- **Frames:** batter
- **Heads:** bb, xbb, bk/bk2/bk3, xbk, hit, xh
- **Rationale:** Two-auditor convergence — strongest in the batch. The passivity axis (zone-swing rate, two-strike take) is the standard third discipline dimension, entirely unexposed while its intermediates already sit in the frame; called strikeouts are ~25% of K's and walks are drawn by taking. Free: ratios of sums the GBM never sees.
- **Synthesis spec:** features.py only, zero pipeline cost: (1) PD_SHRINK (~:1016) add 'zsw':(0.672,120,'z_sw','z_n') and 'tssw':(0.604,100,'ts_sw','ts_n') — priors measured on the full 544,604-row file; (2) PD_BATTER (~:1122) += (zsw,tssw) -> bd_zsw_c/d, bd_tssw_c/d auto-built in BOTH vectorized and serving-Store paths from existing unexposed sums; (3) MUST hand-add the 4 names to batter_feature_cols (~:3044) — spec gap fixed; (4) collision called_k_matchup = (0.604 - bd_tssw_d)*(p_edge_d - PD_SHRINK['edge'][0]) in the ts_matchup block (~:2310); optional ump_k_x_take = ump_k_pct*(1-bd_tssw_d). Stage 2 heart-band DEFERRED (corr 0.925 with zsw; needs a re-agg).

<details><summary>Verifier record: bd_passivity_axis (BUILD)</summary>

- refined_spec: Stage 1 only (zero pipeline cost, features.py-only). (1) PD_SHRINK (~L1016) add: "zsw": (0.672, 120, "z_sw", "z_n") and "tssw": (0.604, 100, "ts_sw", "ts_n") — priors MEASURED 2015-2026 full file (0.6719/0.6041, 544,604 rows, 100% coverage). (2) PD_BATTER (~L1122) += ("zsw", "tssw") — this alone auto-builds bd_zsw_c/d, bd_tssw_c/d in BOTH the vectorized frame path (L2572) and the serving Store (pd_batter_feats L4574) from existing bd_cum/dk_z_sw|z_n|ts_sw|ts_n sums; z_sw NaN-gates on pre-schema files exactly like zwsw (L344). (3) batter_feature_cols() (~L3044, next to bd_wsw_c/bd_chase_c): add "bd_zsw_c","bd_zsw_d","bd_tssw_c","bd_tssw_d". (4) Collision in the ts_matchup derived block (~L2310): df["called_k_matchup"] = (PD_SHRINK["tssw"][0] - df["bd_tssw_d"]) * (df["p_edge_d"] - PD_SHRINK["edge"][0]); add to cols; optional ump_k_x_take = ump_k_pct * (1 - bd_tssw_d). Heads: bb/xbb, bk/bk2/bk3/xbk, hit/xh; selection (SHAP) decides per head as usual; selftest parity run after. Stage 2 (heart-band re-agg, corr 0.925 with zsw): DEFER — only revisit if selection keeps bd_zsw and a raw re-agg is already scheduled for other reasons.
- evidence: Exposed bd_ set (exposed_cols.txt grep) = exactly wsw/chase/zwsw/fb95wh/brkwh/offwh/fbwh/fpsw/fblowh/fbmidwh/tswh/f32b x {_c,_d} — no zone-swing, overall-swing, or two-strike-swing rate, and no bd_*_n counts exposed, so GBM cannot ratio the intermediates. frames_columns.txt confirms bd_cum_z_sw/bd_dk_z_sw, bd_cum_z_n/bd_dk_z_n, bd_cum_ts_sw/bd_dk_ts_sw, bd_cum_ts_n/bd_dk_ts_n exist unexposed (carried as zwsw/tswh denominators — NOT a bench: FEATURE_BACKLOG Part 1 lists only pull_fence/porch_margin/batter-fatigue/d_ps_xwcon_d/lg_*; zero grep hits for zsw/passivity/take-rate in backlog or Decline ledger). features.py: PD_SHRINK L1016 has no zsw/tssw entry; PD_BATTER L1122 is the shared tuple driving vectorized path L2572-2577 AND serving Store pd_batter_feats L4570-4587 via _pd_sums L4522 (which already sums z_sw/z_n/ts_sw/ts_n for serving zwsw/tswh); z_sw derived with NaN gating at L344. Data (pandas, full file): mlb_pitch_daily_batters.csv 544,604 rows, 100% non-null on n/sw_n/oz_sw/oz_n/z_n/oz_wh/ts_n/ts_sw every year 2015-2026; measured zone-swing prior 0.6719 (proposal ~0.67 exact), two-strike swing 0.6041 (~0.6 exact); per batter-season median z_n=235, ts_n=144 so K=120/100 proportionate to siblings (chase K=150, tswh K=100). p_edge_d and ump_k_pct both already exposed in batter frame for the collision; existing centered products (ts_matchup L2310) prove the idiom serves.
- notes: Every factual claim in the proposal verified exactly (priors 0.672/0.604 vs claimed ~0.67/~0.6; intermediates present-but-unexposed; not benched/declined). One spec gap fixed: the proposal omits the required batter_feature_cols() additions — PD_BATTER membership creates the columns but exposure is hand-listed. Value rated medium not high: the walk/K heads already carry dense OUTCOME encodings (c/s/d bb_pct, k_pct, ump_bb/k, p_f32b), so the process-stat lift should be real but modest and concentrated in bb/bk heads; the discipline-triad completeness argument and two-auditor convergence are legitimate. Stage 2 heart-band should not ship with stage 1 (redundant at r=0.925 and needs a schema bump).

</details>

### [8] team_walk_channel  (cost S, value medium)
- **Frames:** team (winner untouched — already has ps_bb_bf both sides)
- **Heads:** team_total, total
- **Rationale:** Walk rate exists in NO encoding on either side of the 64-col team frame — the largest run-scoring input the frame cannot see (max corr 0.36 with any exposed col). total/team_total are exactly the heads the oracle-ceiling memo names as real headroom. Serving unusually cheap: both stores already return the values.
- **Synthesis spec:** features.py: (1) build_game_frame side ratios (~:3582): {side}_bb_pct = {side}_cum_BB/{side}_cum_PA; (2) build_team_game_frame dict: off_bb_pct, opp_ps_bb_bf (already computed per side, :3718), lg_bb_pa as carrier; (3) patience_wild = (off_bb_pct - lg_bb_pa)*(opp_ps_bb_bf - lg_bb_pa) in the shared add_team_game_derived (:3876 + predict.py:697); (4) expose the 3 in team_game_feature_cols. Serving: two dict picks — team_offense store already returns off_bb_pct (:4685), stf ps_bb_bf already fetched; lg_bb_pa arrives via **env; collision computes in the shared helper.

<details><summary>Verifier record: team_walk_channel (BUILD)</summary>

- refined_spec: Three team-frame columns, all from existing as-of intermediates. TRAINING (features.py): (1) in build_game_frame next to the existing side ratios (lines 3582-3587) add g[f"{side}_bb_pct"] = g[f"{side}_cum_BB"] / g[f"{side}_cum_PA"]; (2) in build_team_game_frame's dict add "off_bb_pct": gf[f"{side}_bb_pct"], "opp_ps_bb_bf": gf[f"{opp}_ps_bb_bf"], "lg_bb_pa": gf["lg_bb_pa"] (gf verified to carry all inputs); (3) compute the collision inside add_team_game_derived (shared train/serve helper, called at features.py:3876 and predict.py:697): d["patience_wild"] = (d["off_bb_pct"] - d["lg_bb_pa"]) * (d["opp_ps_bb_bf"] - d["lg_bb_pa"]) — the ump_bb_x_pbb/mix_bb centered-product idiom; (4) append off_bb_pct, opp_ps_bb_bf, patience_wild to team_game_feature_cols() (lg_bb_pa stays an unexposed carrier unless selection wants it). SERVING (predict.py _team_rows): pick "off_bb_pct": toff["off_bb_pct"] (already in the team_offense store return, features.py:4685) and "opp_ps_bb_bf": stf["ps_bb_bf"] (already fetched via starter_feats); lg_bb_pa already present via **env; patience_wild then computes for free in the shared add_team_game_derived — parity covered by the existing team-row selftest. Inputs: Data/mlb_game_batting.csv (BB, PA per team-game via _team_offense cums), Data/mlb_game_pitching.csv (BB, BF via _starter_asof), _league_env_table (lg_bb_pa). Frames/heads: team frame only (total + team_total); winner untouched (already has ps_bb_bf both sides + d_ps_kbb). NaN behavior: patience_wild NaN when any input NaN (~92% coverage, bounded by ps_bb_bf — same as existing opp_ps_* cols; GBM-native).
- evidence: EXISTS check: team frame's 64 exposed cols have zero walk encoding (offense=off_hr_pa/off_r_pg/off_k_pct/off_xwcon/off_brl_con; starter=opp_ps_era/k_bf/hr_bf/h_bf/xwcon*). Walk rate lives only on OTHER frames: toff_bb_pct (batter, features.py:2024/2987), away/home_ps_bb_bf + d_ps_kbb (winner, features.py:3784 — proposal understates: ps_bb_bf is directly exposed on winner, not just via d_ps_kbb). gf[{side}_obp] (features.py:3586) is an unexposed intermediate for lu_obp_gap only; FEATURE_BACKLOG Part 1 bench table + Decline ledger contain nothing walk/OBP-level. DATA: mlb_game_batting.csv 617,988 rows 2015-2026, BB/PA 100% non-null, league 0.084; mlb_game_pitching.csv 228,069 rows, BB/BF 100% non-null, 0.084 (same scale as lg_bb_pa — one centering constant coherent). Cached gf (26,400 games): cum_BB/cum_PA 99.3% non-null, ps_bb_bf 92.3% (same class as exposed opp_ps_era), lg_bb_pa 98.4%; off bb_pct p5-p95 .065-.107, ps_bb_bf p5-p95 .034-.136, lg_bb_pa .071-.103. VALUE: corr(off_bb_pct, off_r_pg)=0.36, off_k_pct=0.06, off_hr_pa=0.24, off_obp(unexposed)=0.53; corr(ps_bb_bf, ps_era)=0.24, ps_k_bf=-0.10, ps_h_bf=-0.18 — mostly unique variance. SERVING: s.team_offense already returns off_bb_pct (features.py:4685) unpicked by _team_rows (predict.py:647); stf["ps_bb_bf"] already fetched (_WIN_ST predict.py:716); lg_bb_pa already in serving row via **env (ENV_COLS features.py:827, league_env 4764-4771).
- notes: Genuine absent channel on the team frame, not a re-encoding: walk rate appears in no encoding on either side of the 64-col list, and correlation checks confirm low redundancy with everything exposed (max 0.36 vs off_r_pg). Serving is unusually cheap because both stores already return the needed values — two dict-key picks plus one shared-helper line. Minor correction to the candidate's claim: {side}_ps_bb_bf is exposed directly on the winner frame (features.py:3784), not only through d_ps_kbb — immaterial to the team-frame gap. Value capped at medium rather than high because off_r_pg already embeds walks' run consequences (the addition is compositional/interaction info) and the oracle-ceiling memo cautions that game-grain batches move totals modestly — but total/team_total are exactly the heads that memo names as having real headroom, so this is among the better-justified team-grain adds. Goes through the standard superset -> SHAP selection -> paired-CI ship gate like any other column.

</details>

### [9] opp_starter_len_pen_split  (cost S, value medium)
- **Frames:** team (winner via runs blend only)
- **Heads:** team_total, total
- **Rationale:** The team frame prices starter quality and pen quality as separate mains with NO column for how tonight's 9 innings divide between them — a 4.2-IP opener team and a 6.2-IP workhorse team look identical (corr(IP/start, ERA) = -0.43, so quality cannot proxy length). Same exposure-product geometry as the #1 batter family, pointed at the totals headroom; the GBM is the blind .80 of SIM_BLEND.
- **Synthesis spec:** features.py: (1) stf['p_ip_per_start'] = ps_Outs/3/p_starts_season (the :3243 formula) appended to ST_COLS (:3575) -> opp_ps_len per side (0/0 -> NaN, debut precedent); (2) per-side _asof_merge of the existing _team_starter_outs_table (:662) -> opp_st_outs_pg (manager leash); (3) xpen_r_gap = clip(9-opp_ps_len,0,9)*(opp_pen_era-opp_ps_era)/9 post-assembly. Expose 3 in team_game_feature_cols; win_feature_cols NOT widened. Serving: starter_feats already returns p_ip_per_start (:4655); s.team_st_outs exists (:4714, already serving the starts frame at predict.py:556).

<details><summary>Verifier record: opp_starter_len_pen_split (BUILD)</summary>

- refined_spec: Three team-frame columns (total + team_total heads; win_feature_cols NOT widened — winner benefits only via the runs blend). TRAIN (features.py build_game_frame): (1) stf['p_ip_per_start'] = stf['ps_Outs']/3/stf['p_starts_season'] (identical to line 3243; 0/0->NaN for debut/first-start-of-season, GBM imputes — existing precedent), append 'p_ip_per_start' to ST_COLS (line 3575) so each side gets {side}_p_ip_per_start; (2) per-side as-of merge of the existing lea table: lea = _team_starter_outs_table(gp) (features.py:662), rename Team->team_col, _asof_merge by [team_col,'Season'], g[f'{side}_st_outs_pg'] = cum_Outs/cum_n (pen_tab idiom at line 3597-3600). In build_team_game_frame (line 3805) add: 'opp_ps_len': gf[f'{opp}_p_ip_per_start'], 'opp_st_outs_pg': gf[f'{opp}_st_outs_pg'], then after frame assembly 'xpen_r_gap' = np.clip(9 - opp_ps_len, 0, 9) * (opp_pen_era - opp_ps_era) / 9 (expected extra runs from the handoff; both ERAs are per-9 so units are runs; NaN propagates). Add all three to team_game_feature_cols (line 3881). SERVE (predict.py _team_rows, line 615): 'opp_ps_len': stf['p_ip_per_start'] (already returned by the existing starter_feats call at line 641), 'opp_st_outs_pg': s.team_st_outs(opp, season, date) (method already exists, features.py:4714, already serving starts frame at predict.py:556), and compute xpen_r_gap from the row's opp_pen_era/opp_ps_era after assembly. Inputs: Data/mlb_game_pitching.csv (GS, IP->Outs, ER) — no new data, no scrape. Selection votes per head as usual.
- evidence: EXISTS: team_game_feature_cols (64, exposed_cols.txt) has NO length/volume column — nearest are opp_ps_era/opp_ps_k_bf (rates), opp_pen_np_l3 (fatigue), opp_ps_tto_decay (within-start decay); p_ip_per_start exposed only in batter+starts frames, team_st_outs_pg only in starts frame, xpa_pen/xpen_* only in batter frame; frames_columns.txt shows no unexposed intermediate in game/team frames; Decline ledger (FEATURE_BACKLOG.md:568+) has nothing relevant; #15 targeted batter heads, #16 starter heads. PLUMBING: ST_COLS at features.py:3575 merges per-side stf cols into gf exactly as spec'd (ps_tto_decay precedent); stf=_starter_asof(gp) carries ps_Outs+p_starts_season (features.py:532-542, cumsum-minus-row, leakage-safe); build_team_game_frame (features.py:3805) maps gf[f'{opp}_*'] per side. SERVING: predict._team_rows (predict.py:615) already calls starter_feats (line 641) which already returns p_ip_per_start (features.py:4655); Stores.lea_tab already exists (features.py:4028) with team_st_outs method (features.py:4714) already serving starts frame at predict.py:556 — no new Stores mirror needed. DATA (mlb_game_pitching.csv): 228,069 rows 2015-2026, IP 100% non-null, 53,274 starter rows, mean 5.30 IP/start; season IP/start p10/p50/p90 = 4.33/5.25/6.03; corr(IP/start, ERA) = -0.433 (era is a weak confounded proxy); team-season penERA-stERA gap p10/p90 = -0.92/+0.53 (sign-varying); team-year leash 14.29-17.38 outs/gm.
- notes: Proposal survives all adversarial checks; serving is even cheaper than claimed (the 'small Stores mirror' in the serving_note is already built — lea_tab at features.py:4028 + team_st_outs at 4714). Genuinely missing axis, not a re-encoding: the 64-col team frame prices starter and pen quality separately but has no innings-split variable, so the tree cannot carve the exposure weighting; corr(IP/start, ERA)=-0.433 confirms opp_ps_era cannot substitute. Same exposure-product geometry that is the #1 feature family on 13/24 batter heads, pointed at the documented total/winner headroom. Value capped at medium (not high) because: 3 columns on one frame, and the PA-sim starter-hazard v1 already partially carries handoff dynamics on the sim side of SIM_BLEND (total sim weight .20) — the GBM incumbent (.80) is the blind component this fixes. Early-season NaN (p_starts_season=0) follows the existing p_ip_per_start precedent; clip guards the collision. Correctly avoids widening the winner frame (small-n overfit rule).

</details>

### [10] p_hit_luck_starts  (cost S, value medium)
- **Frames:** starts
- **Heads:** pha primary; per, outs
- **Rationale:** Stronger than proposed: the starts frame exposes NO realized hits-allowed rate at all, so the GBM lacks BOTH halves of the BABIP-luck residual. The identical formula shipped 07-12 on the batter frame and survived SHAP selection — pre-validated signal, cross-frame port with parity by shared helper.
- **Synthesis spec:** features.py: (1) in build_starts_frame's bip block beside :3275: st['pbipd_xba'] = bip_feats(lambda c: st[f'pb_dk_{c}']*wdn_bip,'t',('xba',))['t_xba']; NaN in the else-branch; (2) formula in add_starter_derived (the SHARED parity function — NOT build_starts_frame — so predict.py:579 serves it free): _pcon = 1 - p5_k_bf - p5_bb_bf; p_hit_luck = where(_pcon>0, p5_h_bf/_pcon, NaN) - pbipd_xba — verbatim batter twin (:2259-2261), with the :3201-3203 missing-col guard; (3) expose p_hit_luck (optionally pbipd_xba) in starts_feature_cols.

<details><summary>Verifier record: p_hit_luck_starts (BUILD)</summary>

- refined_spec: Two-part, matching the #28 p_conv idiom exactly. (1) build_starts_frame, inside the existing `if raw.get("bip") is not None:` block next to line 3275: st["pbipd_xba"] = bip_feats(lambda c: st[f"pb_dk_{c}"] * wdn_bip, "t", ("xba",))["t_xba"]; add st["pbipd_xba"] = np.nan to the else branch (3277-3279). (2) add_starter_derived (the SHARED parity function — put the formula here, NOT in build_starts_frame, so predict.py:579 serves it for free): guard `for c in ("pbipd_xba", "p5_h_bf", "p5_k_bf"): if c not in df.columns: df[c] = np.nan` (mirroring 3201-3203), then _pcon = 1.0 - df["p5_k_bf"] - df["p5_bb_bf"]; df["p_hit_luck"] = np.where(_pcon > 0, df["p5_h_bf"] / _pcon, np.nan) - df["pbipd_xba"] — the verbatim batter-frame formula (2259-2261). (3) Append "p_hit_luck" (and optionally "pbipd_xba" as a main) to starts_feature_cols (3400-3464). Inputs: mlb_game_pitching.csv H/SO/BB/BF via the existing p5_ shift(1).rolling(5) construction; mlb_statcast_bip.csv xBA via the existing pb_ _bip_table asof merge (3265-3270). Frames: starts only. Heads: pha primary, per, outs. Serving: no new code — starter_feats already emits p5_h_bf/p5_k_bf/p5_bb_bf and bip_pitcher already emits pbipd_xba; add_starter_derived placement gives train/serve parity by construction.
- evidence: EXISTS: p_hit_luck only in batter frame (features.py:2259-2261; exposed batter list). Starts exposed list (134 cols, dumped in full) has ZERO realized hits-allowed encodings — no pc_h_bf/ps_h_bf/p5_h_bf, no xba-allowed; nearest = pc_era/ps_era, pbip_xwoba/pbipd_xwoba, and p_conv_r=(p5_h_bf+p5_bb_bf)*pbipd_xwoba (features.py:3205) where h-rate is fused in an uninvertible product vs xwoba not xba. sf frame (448 cols) has p5_h_bf + pb_dk_xba_n/sum computed-but-unexposed; pbipd_xba never computed for sf (only the xwoba slice, 3273-3276). FEATURE_BACKLOG: no bench/decline of a starts mirror (p_hit_luck appears only as the shipped 07-12 batter feature, line 510-512, and team-grain sibling #20). LEAKAGE: p5_*=shift(1).rolling(5) (544-545); pb_ tables merged _asof_merge allow_exact_matches=False (1457-1462, D-1 contract); serving Date<date strict (4254, 4621). SERVING: predict.py:541-579 already assembles starter_feats (returns p5_h_bf/p5_k_bf/p5_bb_bf at 4650-4654) + bip_pitcher (returns pbipd_xba — _bip_entity line 4287 iterates BIP_DECAYED which includes 'xba', line 1011) then calls shared F.add_starter_derived. DATA (pandas): mlb_statcast_bip.csv 1,376,160 rows 2015-2026, xBA 97.5% non-null (95.5-98.4%/season), PitcherId 100%; mlb_game_pitching.csv GS==1 = 53,274 rows (matches sf shape), BF/H/SO/BB 100% non-null; realized h-per-contacted-BF 0.3221 vs xBA-on-contact mean 0.325 (BIP_SHRINK xba prior 0.323) — residual centers ~0.
- notes: Stronger than proposed: the starts frame exposes NO realized hits-allowed rate at all (not even pc_h_bf/ps_h_bf), so the GBM lacks both halves of the residual, not just the xba side. The identical formula was accepted 07-12 on the batter frame and survived SHAP selection there (p_hit_luck in the exposed batter list), so the signal is pre-validated; realized/expected sides are on the same scale (0.322 vs 0.325). Denominator nuance (contacted-PA includes HBP/ROE; xBA is per tracked BB) is inherited from the accepted batter twin — keep the formulas verbatim-identical for cross-frame consistency. One caveat: p_hit_luck rides last-5 rates, so early-season rows are thin — same NaN-propagation behavior as the shipped batter version, no extra shrinkage added there either.

</details>

### [11] p_short_share_batter  (cost S, value medium)
- **Frames:** batter
- **Heads:** hr, tb2-4, hrr2-4, bk family, bb, hit, count means (exposure-sensitive)
- **Rationale:** xpa_pen is a convex clip of starter length, so outing DISPERSION shifts expected bullpen exposure at fixed mean (corr with mean only 0.146 — near-orthogonal). Refines the #1 exposure family for literally two list entries; the candidate's serving worry was verified moot.
- **Synthesis spec:** Add 'p_outs_sd' and 'p_short_share' to the st_feats select list in build_batter_frame (features.py:2408-2414, beside pvloc_* at :2412) and to batter_feature_cols (~:2986). Both already computed on the shared _starter_asof table; ZERO serving change — the batter serving row already receives both keys via **st_feats (predict.py:414; Stores.starter_feats :4658-4665 incl. NaN fallback).

<details><summary>Verifier record: p_short_share_batter (BUILD)</summary>

- refined_spec: Add "p_outs_sd", "p_short_share" to the st_feats select list in build_batter_frame (features.py:2408-2414 — insert alongside "pvloc_era","pvloc_k_bf","pvloc_hr_bf" at line 2412; both already computed on the _starter_asof(gp) table the select reads) and add both names to batter_feature_cols (near features.py:2986 where p_ip_per_start sits). Inputs: mlb_game_pitching.csv starter history via _starter_asof (Outs from ip_to_outs(IP); _c_Outs2 cum + SHORT_START_OUTS=12/SHORT_SHARE_WIN=10 constants, features.py:1643-1644). Frame: batter (merge already keyed GamePk+StarterId at 2416). Serving: NO code change — the batter slate path (predict.py:388, **st_feats at 414) already receives both keys from Stores.starter_feats (features.py:4630, 4658-4665), including the no-history NaN fallback. Zero new computation; two names in an existing select + col list.
- evidence: EXISTS: exposed_cols.txt line 2 (batter, 385 cols) has NO p_short_share/p_outs_sd/p_short_heavy/p_gap15/p_ramp60/team_st_outs_pg; both live only in the starts frame (line 4). Nearest batter cols are means-only: p_ip_per_start, ps_BF+p_starts_season, p_np_last/p_np_l3, xpa_pen/xpen_k/hr/h (features.py:2362-2368). p_fbv_sd/p_rel_sd are pitch-level SDs, unrelated. FEATURE_BACKLOG #16 scoped to starter heads; Decline ledger (4 items) has no batter-carry decline — not a bench. LEAKAGE: computed in _starter_asof with shift(1)/cum-minus-self + day-start snap (features.py:556-560, 571-578) — same profile as pc_/ps_/p5_ already selected at 2407-2416. SERVING: predict.py:383-414 batter path spreads **st_feats from Stores.starter_feats, which ALREADY returns both keys (features.py:4658-4665) incl. the NaN fallback (4630) — zero serving code change. DATA: mlb_game_pitching.csv 53,274 GS==1 rows 2015-2026, IP 100% non-null (Outs=ip_to_outs(IP), features.py:230/253); replicated p_short_share 93.5% non-null (mean .176, 23.7% of rows >=0.3), p_outs_sd 95.4% non-null (mean 3.71, p10-p90 2.7-4.6); corr(mean outs, outs SD)=0.146 (n=50,822) — near-orthogonal to the mean; 2018+ short-start prevalence 22.3%.
- notes: Candidate's serving_note was too pessimistic: no per-starter lookup needs adding — the batter serving row already contains both keys via **st_feats; the only reason the model doesn't see them is the training select + batter_feature_cols lists. Value mechanism is real (xpa_pen is a convex clip of starter length, so dispersion shifts E[pen exposure] at fixed mean; corr with mean only 0.146) but it is a second-order refinement of the xpa_/xpen_ family, hence medium not high. Selection vote remains the arbiter per house process; targets note is descriptive only (no pre-declared targets).

</details>

### [12] ctx_neighbor_completion (merged: ctx_ahead_slg + ctx_behind_obp_gidp)  (cost S, value medium)
- **Frames:** batter
- **Heads:** rbi/rbi2/xrbi + hrr2-4/xhrr (ahead-SLG); run/run2/xrun + hrr family (behind-OBP/GIDP)
- **Rationale:** Completes the neighbor-quality matrix in both directions: ahead-SLG = WHICH base the supply runners occupy (a runner scores from 2nd ~2x as often); behind-OBP = chain continuation; behind-GIDP = the rally-kill axis encoded nowhere (the gidp part was proposed by two auditors independently). All intermediates are loop byproducts — near-zero cost, genuinely slot-directional geometry trees cannot reconstruct.
- **Synthesis spec:** features.py after :2887, from intermediates ALREADY merged by the offset loop (:2876-2883): ctx_ahead_slg = mean(_slgp-2,_slgp-1); ctx_ahead_slg_d = mean(_slgp_d-2,_slgp_d-1); ctx_behind_obp = mean(_obpp1,_obpp2); ctx_behind_obp_d = mean(_obpp_d1,_obpp_d2). ctx_behind_gidp: copy the brr_lt shift-merge idiom (:2893-2901) with src c_gidp_pa_sh at offsets (1,2), mean of _gidpp1/2. Expose 5 cols (:3006-3010 / :3084 blocks). Serving: 5 entries in predict.py:476 ctx_maps — ('_slgp',(-2,-1)), ('_slgp_d',(-2,-1)), ('_obpp',(1,2)), ('_obpp_d',(1,2)), ('c_gidp_pa_sh',(1,2)); all srcs verified on serving rows; skipna means.

<details><summary>Verifier record: ctx_ahead_slg (BUILD)</summary>

- refined_spec: features.py, immediately after line 2887: df["ctx_ahead_slg"] = df[["_slgp-2", "_slgp-1"]].mean(axis=1); df["ctx_ahead_slg_d"] = df[["_slgp_d-2", "_slgp_d-1"]].mean(axis=1). Inputs already merged by the existing offset loop (features.py:2876-2882) from Data/mlb_game_batting.csv TB/AB as-of cums (_slgp = c_TB/c_AB career as-of, :2868; _slgp_d = _dk_TB/_dk_AB decayed, :2873) — zero new merges, batter frame only. Serving: add two entries to predict.py:476 ctx_maps: "ctx_ahead_slg": ("_slgp", (-2, -1)) and "ctx_ahead_slg_d": ("_slgp_d", (-2, -1)); per-player _slgp/_slgp_d already produced by the Stores path. Add both cols to the batter feature list alongside ctx_ahead_obp; SHAP selection adjudicates per head (targets rbi, rbi2, xrbi, hrr2/3/4, xhrr).
- evidence: exposed_cols.txt ctx family = ctx_ahead_obp/_d, ctx_ahead_brr, ctx_behind_slg/_d, ctx_run_conv, rbi_opp_obp — no ahead-SLG in any encoding; no lu_/toff_ SLG substitute (lu_* exposed are whiff/chase/K only, toff_hr_pa is team-level non-directional). frames_columns.txt confirms _slgp-1/_slgp-2/_slgp_d-1/_slgp_d-2 already exist (neighbor loop features.py:2876-2882 merges all 4 offsets; only +1/+2 aggregated at :2885/:2887). FEATURE_BACKLOG.md: zero mention of ahead-SLG (not benched, not declined). Serving: per-player _slgp at features.py:4180-4181, _slgp_d at :4212-4214, NaN fallback :4143-4144; predict.py:476-482 ctx_maps dict does the slot-wrap assembly. Data: mlb_game_batting.csv 617,988 rows 2015-2026, TB/AB/BattingOrder 100% non-null.
- notes: Every factual claim in the candidate checked out exactly (line numbers, existing intermediates, serving plumbing). This is a real directional gap — runner base-occupancy (ahead power) vs runner frequency (ahead OBP) — not a re-encoding; GBMs cannot carve slot-directional neighbor SLG from ahead-OBP + team-level toff_hr_pa. Value capped at medium: it is a within-family refinement whose new information is the ahead hitters' ISO residual, and ctx features are secondary-tier. Cost is genuinely two lines train-side + two dict entries serve-side. Not GRAY: the unaggregated -1/-2 SLG intermediates are loop byproducts, not a bench (backlog silent on ahead-SLG).

</details>

<details><summary>Verifier record: ctx_behind_obp_gidp (BUILD)</summary>

- refined_spec: Batter frame, 3 cols. (a) ctx_behind_obp = df[["_obpp1","_obpp2"]].mean(axis=1) and (b) ctx_behind_obp_d = df[["_obpp_d1","_obpp_d2"]].mean(axis=1) — intermediates already merged by the offset loop at features.py:2876-2882; add the two lines beside features.py:2884-2887 and append both names to the exposure lists at features.py:3006-3010. (c) ctx_behind_gidp: copy the brr_lt shift-merge idiom (features.py:2893-2901) with src col c_gidp_pa_sh (already computed, SHRINK gidp_pa prior 0.018 K=200) at offsets (1,2), then mean of _gidpp1/_gidpp2; expose alongside ctx_run_conv (features.py:3084 block). Inputs: mlb_game_batting.csv H/BB/HBP/PA cums (via existing c_*/ _dk_* as-of tables) and GIDP/PA (via existing c_gidp_pa_sh); posted-lineup GamePk/Team/slot. Serving: add three entries to predict.py:476 ctx_maps — {"ctx_behind_obp": ("_obpp",(1,2)), "ctx_behind_obp_d": ("_obpp_d",(1,2)), "ctx_behind_gidp": ("c_gidp_pa_sh",(1,2))} — all three src values verified present per serving row. NaN handling = skipna mean, matching existing ctx cols. Route to run/run2/xrun + hrr2/hrr3/hrr4/xhrr (SHAP selection remains the decider).
- evidence: EXISTS: exposed ctx cols are only ctx_ahead_obp(_d), ctx_behind_slg(_d), ctx_ahead_brr, ctx_run_conv (exposed_cols.txt); no ctx_behind_obp* or neighbor-GIDP in either inventory. _obpp1/_obpp2/_obpp_d1/_obpp_d2 already sit in the built frame as UNEXPOSED side effects of the 4-offset merge loop (features.py:2876-2882) — not a bench: FEATURE_BACKLOG.md has no behind-OBP/neighbor-GIDP entry (grep behind|obp|gidp = unrelated hits only); the features.py:803-811 'flat' note is the deeper-order AHEAD variant. c_gidp_pa_sh/s_gidp_pa_sh exposed = own rate only; nearest cols ctx_behind_slg(_d), run_opp=c_obp*ctx_behind_slg (features.py:2350), mix_gb (2345) are all power-only or own-risk. SERVING: predict.py:476-493 ctx_maps+_nmean loop; ctx_ahead_brr already uses a plain feature col as src, and serving rows already carry _obpp/_obpp_d (features.py:4178/4212) and c_gidp_pa_sh (shrunk_from_sums at 4171 iterates SHRINK incl. gidp_pa) — change = 3 dict entries. DATA: mlb_game_batting.csv 617,988 rows 2015-2026; PA/H/BB/HBP/GIDP 100% non-null every year; league GIDP/PA measured 0.0188 vs coded prior 0.018 (features.py:1598, K=200). LEAKAGE: as-of career/decayed cums + posted-lineup GamePk/Team/slot join, same machinery as shipped ctx cols.
- notes: Genuine geometry gap: a GBM row for slot s has no view of slots s+1/s+2 on-base or GIDP (the behind direction only exists as SLG), and it cannot reconstruct it from ctx_ahead_obp (different rows) or lineup means. Value tempered to medium because ctx features are secondary-tier and OBP/SLG correlate among regulars, so the increment is the decoupled residual plus the (rare-event, hard-shrunk) GIDP axis; heads served (run family) are exactly where the dynamic-priority rule points. Two independent auditors proposing the gidp part is corroborating, not evidence of lift — the paired 2025 CI remains the arbiter.

</details>

### [13] bip_day_sums_extension (merged: bip_oppoair_spray_third_bin + bip_ev105_tail_share)  (cost S, value medium)
- **Frames:** batter (both bip_ and pbip_ sides)
- **Heads:** hit, double, tb2-4, xtb, hr, hrr2-4
- **Rationale:** The spray encoding is one-sided: oppo-air is a stable independent skill (YoY 0.51, corr only -0.46 with pullair) with a park-fence interaction the pull-only encoding cannot form; 13.2% of HRs are oppo. The ev105 rider is the smallest verified edge in the batch (partial r +0.078 controlling brl/hh/ev) — ~6 lines on the same table; if selection drops it, that is the vote working.
- **Synthesis spec:** One dual-path edit of the BIP day-sum table. _spray_flags (:1159): add oppo = Stand-signed angle beyond +/-15 deg opposite-field (HcX.notna() gated) and oppoair = oppo & air. _bip_day_sums (:1170) AND Stores._bip_entity (~:4259-4281): add oppo_n, oppoair_n, ev105_n = (ExitVelo>=105). BIP_SHRINK: 'oppo':(0.282,60,'oppo_n','hc_n'), 'oppoair':(0.161,80,'oppoair_n','hc_n'), 'ev105':(0.084,80,'ev105_n','ev_n') — priors verified on file; 'oppoair' to BIP_DECAYED. Deriveds beside pull_fence (:2210): oppo_fence = hand-mirrored LF/RF; oppo_air_porch = (330-oppo_fence)*bip_oppoair. Append all new bip_/bipd_/pbip_ names to the keep-list (:3032-3038 — required, was missing from the ev105 candidate). Auto-yields both batter and pitcher-allowed sides.

<details><summary>Verifier record: bip_ev105_tail_share (BUILD)</summary>

- refined_spec: Input: Data/mlb_statcast_bip.csv (ExitVelo, Date, BatterId, PitcherId; 98.8% EV coverage 2015-2026). (1) _bip_day_sums (features.py ~:1182, after hh_n): add "ev105_n": (ev >= 105).astype(float). (2) Stores._bip_entity parts dict (~:4263, after hh_n): add "ev105_n": (ev >= 105).to_numpy(dtype="float64"). (3) BIP_SHRINK (~:994): add "ev105": (0.084, 80, "ev105_n", "ev_n") — prior verified 0.0839 on file; K=80 mirrors brl/pullair tail Ks; denominator ev_n matches hh. (4) Optionally append "ev105" to BIP_DECAYED (:1011) for bipd_/pbipd_ variants. (5) Append "bip_ev105", "pbip_ev105" (+ "bipd_ev105", "pbipd_ev105" if decayed) to the MAIN keep-list at :3032-3038 — this step was missing from the candidate spec; it is how ld/pu/flyd entered 07-12. Leakage: standard _cum_decay_table -> merge_asof(allow_exact_matches=False) idiom; serving path filters Date < date. Frames: batter frame both sides (bip_ batter tail power, pbip_ pitcher crushed-rate allowed). Serving: dual-path auto-propagates from the BIP_SHRINK/BIP_DECAYED dicts; parity selftest covers it. Target heads: hr, xtb, tb3/tb4, hrr3/hrr4 — but SHAP selection votes per head as usual.
- evidence: EXISTS: no ev105/105-cutoff column in exposed_cols.txt or frames_columns.txt; nearest = bip_ev/bipd_ev (mean), bip_hh/pbip_hh (EV>=95 share), bip_brl/bipd_brl, bip_flyd, hrq_dist_max; FEATURE_BACKLOG.md has no EV-tail item and no decline-ledger entry. CODE: BIP_SHRINK at features.py:990-1009, _bip_day_sums hh_n at :1182, Stores._bip_entity hh_n at :4263, vectorized exposure loops over BIP_SHRINK dict at :2485-2498, explicit MAIN keep-list at :3032-3038 (must append names there — spec omission). DATA (ran pandas on Data/mlb_statcast_bip.csv): 1,376,160 rows 2015-04-05..2026-07-12; ExitVelo 98.79% non-null (0.966 in 2015 -> ~0.997 from 2021); EV>=105 share of tracked = 0.0839 (proposed prior 0.084 exact); hh share 0.375 (saturated as claimed). VALUE (independent rerun): corr(ev105,brl)=0.783, corr(ev105,hh)=0.808 at player-season grain (3,620 seasons n>=150); partial corr(next-season HR rate, ev105 | brl,hh,ev) = +0.078 on 1,962 matched pairs (proposal claimed +0.10 — same sign/order, slightly lower under my filters); brl partial controlling ev105 = +0.33.
- notes: Honest marginal add: partial-r +0.078 (not the claimed +0.10) after controlling brl/hh/ev, with 0.78-0.81 collinearity to two exposed siblings — the smallest verified edge in this batch, but the cost is ~6 lines in the established idiom and the 105+ tail is a genuinely distinct order statistic (hh saturates at 37.5% of contact, brl is LA-window-gated, mean is mishit-contaminated, hrq_dist_max is HR-censored and 2020+ only). If selection's credit-splitting drops it on most heads, that is the vote working as designed. Risk of harm is minimal (one column per side per encoding); main cost is a keep-list slot contest against its own siblings.

</details>

<details><summary>Verifier record: bip_oppoair_spray_third_bin (BUILD)</summary>

- refined_spec: In _spray_flags (features.py:1159): also return oppo = np.where(Stand=='R', ang>+15, np.where(Stand=='L', ang<-15, False)) & HcX.notna() and oppoair = oppo & air (air already = BBType in {fly_ball,line_drive}); ang = degrees(arctan2(HcX-125.42, 198.27-HcY)) unchanged. In _bip_day_sums (1170) add day sums 'oppo_n': oppo.astype(float), 'oppoair_n': oppoair.astype(float); mirror the same two entries in Stores._bip_entity parts dict (~4259-4281). Add BIP_SHRINK entries 'oppo': (0.282, 60, 'oppo_n', 'hc_n') and 'oppoair': (0.161, 80, 'oppoair_n', 'hc_n') (priors verified on file); append 'oppoair' to BIP_DECAYED (1011). This auto-yields bip_oppo, bip_oppoair, bipd_oppoair, pbip_oppo, pbip_oppoair, pbipd_oppoair via the shared cum/decay table + merge_asof(allow_exact_matches=False). In add_batter_derived (~2210, next to pull_fence): oppo_fence = np.where(eff_hand=='L', LF, np.where(eff_hand=='R', RF, np.nan)); oppo_air_porch = (330.0 - oppo_fence) * bip_oppoair. Expose new cols in the batter-frame keep list (~3033 block, beside bip_pull/bip_pullair) for both batter and pbip_ mirrors; selection votes per head (hr, tb2/3/4, double, hit, xtb, hrr2-4). Inputs: Data/mlb_statcast_bip.csv (HcX, HcY, Stand, BBType, Date, BatterId, PitcherId); LF/RF and eff_hand already in-frame. Serving: flows through Stores.bip_batter/bip_pitcher + add_batter_derived automatically once both paths carry the sums; parity selftest covers it. Requires a frames.joblib rebuild (feature-construction change — cached 12:30 copy not reusable per FINISH_PLAN caveat).
- evidence: EXISTS check: grep of features.py + exposed_cols.txt + frames_columns.txt shows zero oppo columns; only pull-side spray exists (bip_pull, bip_pullair, bipd_pullair, pbip_pull/pullair, lu_pullair) and pull-only geometry deriveds (pull_fence, porch_margin, bat_wind_pull/porch, air_porch, air_fly); park_hand_hr_edge (features.py:2218) is venue-level hand asymmetry, not batter oppo skill. FEATURE_BACKLOG #1-32, Part 1, and Decline ledger contain no spray/oppo item. DATA (Data/mlb_statcast_bip.csv, 1,376,160 rows 2015-2026): HcX non-null 94.0-100% by season (min 2018=94.0, >=99.7% since 2019); measured oppo share = 0.2823 (proposed prior 0.282), oppoair = 0.1609 (proposed 0.161); oppo HR share = 13.2% of 61,139 coord-tracked HRs (claim exact); player-season (n>=100 BIP, 4,472 seasons) corr(oppoair, pullair) = -0.461 (claim -0.49); YoY oppoair r = 0.511 over 3,036 pairs (claim 0.554) vs pullair 0.684. Tempering fact: HR rate per oppo-air ball 0.037 vs 0.184 per pull-air (5x) — value is spread across hit/double/tb heads, not HR-dominant. SERVING: Stores._bip_entity (features.py:4244-4290) shares _spray_flags (1159) and bip_feats with the vectorized path, so adding oppo flags to _spray_flags + the two parts entries + BIP_SHRINK covers both paths; add_batter_derived (2200) is parity-safe for the two derived cols.
- notes: All proposal numbers reproduced within threshold-sensitivity (priors exact to 3 decimals; corr -0.461 vs claimed -0.49; YoY 0.511 vs 0.554). Genuine gap: the non-pull air mass's center/oppo split exists in no column and is not derivable from bip_pull/bip_pullair/bip_gb/bip_pu/bip_ld. Value tempered vs the pitch: oppo-air HR rate is 5x lower than pull-air (0.037 vs 0.184), so expect the lift on hit/double/tb/xtb more than hr; the 13.2% oppo-HR slice plus oppo_fence park interaction is the HR-side upside. Shrinkage Ks (60/80) mirror the pull pair and fit the thinner bin. Same fixed-prior caveat as all BIP_SHRINK entries (documented known limitation, Decline ledger item 4 — not new). Optional lineup-mean team rider explicitly out of scope in this spec, correctly.

</details>

### [14] rbi_lob_conv  (cost S, value medium)
- **Frames:** batter
- **Heads:** rbi, rbi2, xrbi, hrr2-4, run
- **Rationale:** LOB is loaded into the frame with ZERO consumers (grep-verified) — the only column separating realized opportunity from conversion; every existing RBI signal is expectation-side or fused. Exact batter-grain sibling of the shipped hit_luck family; 8-point p10-p90 conversion spread at 1500+ PA; d_rob_pa is the most orthogonal of the three.
- **Synthesis spec:** Add LOB to BAT_STATS (:439) and DECAY_STATS (:51) — cums accrue free, day-start snapped. Derived (near hit_luck :2233): c_rbi_conv_sh = (c_RBI + 40*0.232)/(c_RBI + c_LOB + 40); rbi_conv_gap = decayed conversion (same form, K' at hit_luck-family scale) minus c_rbi_conv_sh; d_rob_pa = (_dk_RBI + _dk_LOB)/_dk_PA. Serving: forward c_RBI/c_LOB/_dk_RBI/_dk_LOB in both the empty-history and sums branches per the '# hit_luck inputs' idiom (:4151/:4202). Document: solo-HR RBIs make conversion partially power-correlated; the with-runners residual is the signal.

<details><summary>Verifier record: rbi_lob_conv (BUILD)</summary>

- refined_spec: (1) Add "LOB" to BAT_STATS (features.py:439) and DECAY_STATS (:51) — c_LOB/s_LOB/_dk_LOB accrue via existing as-of loops, day-start snapped. (2) In the derived block (near hit_luck :2233 / rbi_conv :2349) add: c_rbi_conv_sh = (c_RBI + 40*0.232)/(c_RBI + c_LOB + 40); rbi_conv_gap = (_dk_RBI + K'*0.232)/(_dk_RBI + _dk_LOB + K') - c_rbi_conv_sh (K' by decayed-shrink convention, e.g. match hit_luck-family scale); d_rob_pa = (_dk_RBI + _dk_LOB)/_dk_PA. Constants fixed by convention (BIP_SHRINK precedent), NOT swept. (3) Serving: in batter_feats forward out["c_RBI"], out["c_LOB"], out["_dk_RBI"], out["_dk_LOB"] in both the empty-history branch (:4134/:4150, zeros -> P0 via shrink) and the sums branch (:4158/:4199), mirroring the '# hit_luck inputs' idiom at :4151/:4202. Input: Data/mlb_game_batting.csv cols RBI, LOB, PA (all 0% null, 2015-2026). Frame: batter (~190k rows). Heads: rbi, rbi2, xrbi, hrr2/3/4, run. Caveat to document: RBI includes self-driven HRs (solo HR = +1 RBI, 0 LOB), so conversion is partially power-correlated; c_hr_pa/c_tb_ab_sh absorb that, the with-runners residual is the new signal.
- evidence: EXISTS: grep 'LOB' Model/features.py = 0 matches (zero consumers, verified); exposed_cols.txt has no *lob* column in any frame; nearest RBI cols = c_/s_/r7/r15/r30_rbi_pa_sh, d_rbi_pa_sh (RBI/PA, traffic-x-conversion fused), rbi_opp_obp (features.py:2917, expected traffic tonight), rbi_conv=rbi_opp_obp*c_tb_ab_sh (:2349), xpa_x_rbi — none carry realized traffic; FEATURE_BACKLOG.md: zero LOB mentions, Decline ledger = selection-design items only, #28 per-conversion-chain is the pitcher sibling. DATA (pandas, Data/mlb_game_batting.csv): 617,988 rows 2015-2026; PA/RBI/LOB 0.000% null; LOB/PA=0.3761; RBI/(RBI+LOB)=0.2321 (both match the proposal's claims exactly); yearly conversion stable 0.2238-0.2464; player skill spread at >=1500 PA (n=480): conv p10/p50/p90 = 0.197/0.240/0.274. SERVING: load_raw() reads mlb_game_batting.csv with no usecols (features.py:244); gb_by_player=_LazyGroups(r['gb']) (:3950); batter_feats sums frame[BAT_STATS] (:4158) and DECAY_STATS decayed sums (:4199) — LOB rides both once listed; hit_luck-inputs forwarding pattern at :4151/:4202 is the template for exposing raw _dk_/c_ sums to the derived block. LEAKAGE: c_/s_ cumsum-minus-own-row (:463/:467), _dk_ exp-cumsum-discount (:497-500), _snap_to_day_start doubleheader parity (:448).
- notes: Strong medium: verified-zero-consumer input, exact shipped-template precedent (hit_luck :2233 survived selection), all claimed data numbers reproduce to the digit, and the traffic/conversion decomposition is provably non-derivable from the exposed set (realized traffic exposed nowhere). Discrimination is real (8-point p10-p90 conversion spread at 1500+ PA) but partially power-correlated via solo-HR RBIs, and the fused c_rbi_pa_sh family already carries most of the juice against 385 incumbent batter columns — hence medium, not high. d_rob_pa is the most orthogonal of the three (his realized lineup-context traffic vs tonight's expected traffic from rbi_opp_obp/ctx_ahead_obp).

</details>

### [15] toff_bsr_luck  (cost S, value medium)
- **Frames:** batter
- **Heads:** run, rbi, run2, rbi2, xrun, xrbi, hrr2-4, xhrr
- **Rationale:** The batter run/rbi heads see team run environment only through fused toff_r_pg; the trailing-30-day BaseRuns sequencing-luck residual (#20, kept by selection at team AND winner grain) is the discount term and is uncarveable from exposed cols (needs team H/TB/HBP window sums the frame lacks). Two-line extension of a shipped, paid-for table.
- **Synthesis spec:** In the batter frame's team-merge block (~features.py:2000-2026): merge _team_bsr_table(raw['gb']) (or thread the game-frame instance from :3566) renamed bsr_luck -> toff_bsr_luck on [Team,Date] (pen_fat_tab idiom at :2016). Expose beside toff_* (~:2987). Serving: add 'toff_bsr_luck': s.team_bsr(team,date) to the batter row dict — store exists (:4705-4712), already consumed by team/winner paths. NaN under 10-game windows (6.7%, early season), GBM-native.

<details><summary>Verifier record: toff_bsr_luck (BUILD)</summary>

- refined_spec: Training (features.py, batter frame builder, team-merge block ~lines 2000-2026): bsr_tab = _team_bsr_table(raw["gb"]) (or thread the instance already built for the game frame at line 3566); rows = rows.merge(bsr_tab.rename(columns={"bsr_luck": "toff_bsr_luck"}), on=["Team", "Date"], how="left") — identical ["X","Date"] merge idiom as pen_fat_tab at line 2016. Add "toff_bsr_luck" to the batter feature-column list next to the existing toff_* entries (~line 2987). Semantics: trailing-30-day own-team actual runs minus BaseRuns expectation per game, strictly pre-date, NaN under 10 games. Serving (predict.py batter-row builder ~391-415): add "toff_bsr_luck": s.team_bsr(team, date) to the row dict — reuses the existing Store lookup with its +7-day horizon; no new store, no new scrape. Expose to the full batter frame and let per-head selection vote (no pre-declared targets); expected uptake on run/rbi/run2/rbi2/xrun/xrbi/hrr2-4/xhrr. Opponent-side defensive sibling (opponent pitching-staff bsr_luck-allowed) is a separate follow-up proposal, not included here.
- evidence: EXISTS check: exposed_cols.txt batter line (385 cols) has no bsr/luck-residual column — team env view = toff_hr_pa/toff_r_pg/toff_k_pct/toff_bb_pct/toff_loc_hr_pa/toff_loc_r_pg (season-cum only) + ctx_* neighbor skill (ctx_run_conv = bat_brr_xb*ctx_behind_slg, features.py:2255 — skill, not luck). frames_columns.txt: bsr cols exist only in gf (home/away/d_bsr_luck); batter frame (867 intermediates) never computes it → not a bench. FEATURE_BACKLOG #20 targets "total, winner" only; Decline ledger has no batter-frame entry. Data run (replicated _team_bsr_table exactly): mlb_game_batting.csv 617,988 rows 2015-2026, H/BB/HBP/TB/HR/AB/R all 100.00% non-null; table = 63,179 (Team,Date) rows, mean -0.048, sd 0.341 R/G, p5/p95 [-0.60, +0.52]; batter-grain merge coverage 93.3% non-null (2015 93.9%, 2020 82.9%, 2025 93.8%, 2026 89.7%; NaN = early-season <BSR_MIN_G=10 windows). Leakage: rolling("30D").sum().shift(1) on daily reindex (features.py:741) ends D-1 — same guarantee shipped consumers use (3566/3705/3857). Serving: Store._bsr + team_bsr(team,date) already exist (features.py:4029-4031, 4705-4712), consumed at predict.py:682 (team) and :749 (winner); batter row builder (predict.py:391-415) has s/team/date in scope. Value precedent: off_bsr_luck survived selection into the team frame's exposed 64; d_bsr_luck in winner's 55.
- notes: Extension of shipped #20 to a new frame, not a re-proposal: the batter-frame join was never built, benched, or declined (Decline ledger checked). Genuinely uncarveable from exposed cols — the residual needs team H/TB/HBP rates and a 30-day window, none of which the batter frame exposes. Signal is second-order (sd 0.34 vs ~4.5 R/G mean), hence medium value; cost is a two-line train change + one serving dict entry, so the expose-and-let-selection-vote bar is easily cleared. NaN coverage gaps (6.7%, early-season) match the team frame's existing behavior; GBMs handle natively.

</details>

### [16] wind_carry_starts  (cost S, value medium)
- **Frames:** starts
- **Heads:** pha, per primary; outs weak
- **Rationale:** The starts frame is the ONLY frame holding the raw wind inputs without the signed carry scalar — the can't-carve-a-categorical-into-physics rationale was already accepted twice (#1/#2) and the col is exposed on batter and team frames. One line; carry -> HR/XBH allowed -> ER.
- **Synthesis spec:** Add add_wind_carry(df) (pull=False; helper at :2180, needs only WindDir/WindSpeed — both on sf and the serving row) to add_starter_derived (:3165) beside add_weather_derived, and 'wind_carry' to starts_feature_cols (:3400 list). Serving automatic via the shared helper (predict.py:579). Requires sf rebuild (do not reuse the 12:30 frames.joblib).

<details><summary>Verifier record: wind_carry_starts (BUILD)</summary>

- refined_spec: In features.py add_starter_derived (line 3165), add `add_wind_carry(df)` (pull=False; the helper at 2180 needs only WindDir/WindSpeed, both already on sf and on the serving row) alongside the existing add_weather_derived(df) call, and append "wind_carry" to starts_feature_cols() (line 3400 list). Formula (existing helper, unchanged): wind_carry = sign(WindDir) * WindSpeed with sign = +1 for Out To Lf/Cf/Rf, -1 for In From Lf/Cf/Rf, 0 for L To R / R To L / Varies / Calm / missing. Inputs: WindDir, WindSpeed from mlb_games.csv (already joined onto sf and served via predict.py _weather -> **wx). Frames: starts only (batter and team already have it). Serving path: automatic — predict.py line 579 calls the same add_starter_derived on the assembled serving rows. Requires sf rebuild (feature-construction change: do NOT reuse the 12:30 frames.joblib for sf) + inclusion in the planned selection regen; both are already scheduled in the FINISH_PLAN chain.
- evidence: exposed_cols.txt: starts frame (134 cols) has only raw WindDir+WindSpeed; wind_carry exposed in batter (with bat_wind_pull/bat_wind_porch) and team frames only. frames_columns.txt: built sf (53274x448) has WindDir/WindSpeed, no wind_carry — never computed, so not a bench. features.py: add_wind_carry defined at 2180; called only at 2222 (add_batter_derived) and 3875 (team builder); add_starter_derived (3165-3215) lacks it; starts_feature_cols ends with raw "WindDir" (3464). FEATURE_BACKLOG #2 scoped the carry scalar to team/total/HR-environment only; Decline ledger has no wind item. predict.py: starter serving row includes **wx (_weather lines 340-343 yields title-cased WindDir matching _WIND_SIGN keys + numeric WindSpeed) and calls F.add_starter_derived at line 579 — parity automatic. Data (mlb_games.csv, 26637 games 2015-2026): WindDir 84.0% non-null, WindSpeed 100%; 51.4% of games nonzero carry sign (13691); scalar -29..+29 mph, mean +1.50, std 6.71. train.py: starter head routing (st_exclude) currently nuked (line 565), so the new col reaches k/outs/pbb/pha/per supersets and SHAP selection decides per head.
- notes: Proposal's claims verified in full — genuine cross-frame omission, not a bench or re-encoding: the starts frame is the only frame holding the raw inputs without the physics scalar, and the can't-carve rationale was already accepted twice (backlog #1/#2). Directly on-target for pha/per (carry -> HR/XBH allowed -> ER), weaker for outs. No shrinkage needed (deterministic physics; neutral rows = exact 0). Value capped at medium: weather features are supporting contributors and ~48.6% of rows are zero, but the cost is one line in the shared parity helper. SHAP selection remains the sole keep decider per head.

</details>

### [17] pmilb_opp_starter_prior  (cost S, value medium)
- **Frames:** batter
- **Heads:** all batter heads in rookie/debut-starter matchups (hr, hit, tb2, bk, bb especially)
- **Rationale:** For MLB-debut opposing starters every pitcher main on the batter frame (pc_/ps_/p5_, pd_*, pars_/m_) is NaN by construction — this is the only pitcher description available exactly there (89% coverage on debut starts, 82% non-null overall). The identical table survived selection on the starts frame; one merge, one serving lookup.
- **Synthesis spec:** In build_batter_frame immediately after the st_feats merge (:2416): merge _milb_cols(raw['milb']['pit'],'pmilb_') with PlayerId renamed StarterId, on [StarterId,Season], how='left' — the exact sf idiom (:3379-3384) — with else-branch NaN fill over MILB_PIT_COLS; add *MILB_PIT_COLS to batter_feature_cols (~:3104-3112). Serving: add **s.milb_feats(opp_starter, season, 'pit') to the batter row (helper exists :4489; sentinel -1 -> NaN). 9 cols. Optional team-frame extension deferred (separate plumbing).

<details><summary>Verifier record: pmilb_opp_starter_prior (BUILD)</summary>

- refined_spec: In build_batter_frame (features.py, immediately after the st_feats merge at :2416): `if raw.get("milb") is not None: df = df.merge(_milb_cols(raw["milb"]["pit"], "pmilb_").rename(columns={"PlayerId": "StarterId"}), on=["StarterId", "Season"], how="left")` with the else-branch NaN fill over MILB_PIT_COLS — exactly the sf idiom at features.py:3379-3384. Add `*MILB_PIT_COLS` to batter_feature_cols (the list around :3104-3112, next to *MILB_BAT_COLS). Serving: in predict.py batter row dict (~:411-429), add `**s.milb_feats(opp_starter, season, "pit")` — helper exists (features.py:4489), ServingStore already builds the pmilb_-prefixed pit table (:3975-3981), sentinel opp_starter=-1 yields NaN. Inputs: artifacts/milb_priors.joblib ["pit"]["serve"] (PlayerId, Season, t_K/t_BB/t_HBP/t_1B/t_2B/t_3B/t_HR/t_OUT, n_eff) via raw["milb"]; join key StarterId+Season already on the batter frame. Adds 9 cols: pmilb_k, pmilb_bb, pmilb_hbp, pmilb_b1, pmilb_b2, pmilb_b3, pmilb_hr, pmilb_n (MILB_PIT_COLS is 8 rates + n). Optional team-frame extension (build_game_frame join by away/home starter → opp_ps_milb_k/bb/hr/n in team_game_feature_cols) is separate M-cost plumbing (needs renamed away/home variants); ship the batter-frame primary first. Selection votes per head as usual.
- evidence: EXISTS check: pmilb_* only in starts_feature_cols (exposed_cols.txt line 4) and sf frame; batter frame's milb-ish cols = own milb_* ×8 only; bf (867 cols) has zero pmilb → not computed-but-unexposed. Backlog #14 rider spec (FEATURE_BACKLOG.md:152-157) scopes pit-side join to STARTS frame only; Decline ledger (4 items) has nothing related. Code: batter-frame starter attach at features.py:2403-2416 (StarterId created, pc_/ps_/p5_ merged); sf milb idiom at features.py:3379-3384; MILB_PIT_COLS at :1737; serving helper milb_feats(pid, season, "pit") at features.py:4489-4495, already used at predict.py:550-551; batter row assembly predict.py:383-429 has opp_starter in scope. DATA: milb_priors.joblib pit serve = 45,479 rows, cols [PlayerId, t_K..t_OUT ×8, n_eff, Season], 2015-2026, n_eff 100% non-null. Coverage vs mlb_game_pitching.csv GS==1 (53,274 starts): 81.6% overall (2024 85.5%, 2025 83.7%, 2026 83.3%); 710 MLB-debut starts → 89.3% covered; 3,471 starts with <5 prior MLB apps → 89.9% covered (~6.4k debut batter-rows, ~31k thin-history batter-rows).
- notes: Genuinely new geometry: for MLB-debut/thin-history opposing starters, every pitcher main on the batter frame (pc_/ps_/p5_, pd_*, pars_/m_) is NaN by construction — pmilb_* is the only pitcher-quality description available exactly there, and non-null on ~82% of all rows it also acts as an MLB-sample-independent prior for young starters. Leakage-safe: serve table uses MiLB seasons <= Y-1 only, exact-season join, no in-season aggregates. Shrinkage native to the artifact (translated shrunk rates + n_eff). Precedent: identical table survived selection on the starts frame; batter own milb_* kept in 22/24 heads. Expected value medium: affected slice is real but modest (~1.3% of starts are debuts, ~6.5% thin-history); heads hr/hit/tb2/bk/bb in rookie-starter matchups benefit most. Timing: chain is aborted mid-batch (HANDOFF_0714) — this slots into the pending finish batch as a rider; per-day incremental cost is one merge (negligible).

</details>

### [18] starts_lineup_widening (merged: lu_obp_slg_gap_starts + lu_contact_damage + lu_first_pitch_economy)  (cost S, value medium)
- **Frames:** starts
- **Heads:** pha, per, outs primary; k, pbb secondary
- **Rationale:** The starts frame inherited the K-model's whiff-only lineup view: the k head got its posted-lineup features (lu_mix_k is #1 there) while pha/per/outs see the lineup only through whole-roster season rates that are B-lineup-blind. This ports the proven #19 detector, the contact-DAMAGE half (process xwOBA/brl, faster-stabilizing than outcomes), and pitch-economy aggression (absent in ANY encoding) to the frame whose heads face the posted lineup directly — all in one groupby edit.
- **Synthesis spec:** One extension of the :3337-3347 lu groupby + one _LU_COLS edit. Groupby adds: lu_obp=('_obpp','mean'), lu_slg=('_slgp','mean'), lu_xwcon=('bipd_xwoba','mean'), lu_brl=('bipd_brl','mean'), lu_fpsw=('bd_fpsw_d','mean'). Team norms from vs_ cums already on sf (:3229-3235): vs_obp=(vs_cum_H+BB+HBP)/vs_cum_PA, vs_slg=vs_cum_TB/vs_cum_AB. In add_starter_derived (with the :3176 NaN-guard list): lu_obp_gap = lu_obp - vs_obp; lu_slg_gap = lu_slg - vs_slg; fp_lu_x = (lu_fpsw - 0.301)*(pd_fps_d - 0.609) (PD_SHRINK priors). Expose 8 cols. Serving: extend predict _LU_COLS (:516-520) with the three src maps; vs_obp/vs_slg already arrive via s.team_offense(opp, prefix='vs') at :542; fp_lu_x free via the shared helper.

<details><summary>Verifier record: lu_contact_damage (BUILD)</summary>

- refined_spec: features.py build_starts_frame lu groupby (lines 3337-3346): add two agg lines — lu_xwcon=("bipd_xwoba","mean"), lu_brl=("bipd_brl","mean") — inside the existing `if batter_frame is not None:` block (batter_frame carries both cols; NaN-skipping mean, merged on GamePk/Opponent at 3347-3348, the shipped lu_ idiom). Add "lu_xwcon","lu_brl" to starts_feature_cols() next to "lu_wsw","lu_chase" (~line 3411-3412). predict.py: add "lu_xwcon":"bipd_xwoba", "lu_brl":"bipd_brl" to _LU_COLS (line 516-520) — the existing bdf.loc[mask,src].mean() path serves them with zero new plumbing. Inputs: batter-frame bipd_xwoba/bipd_brl (from Data/mlb_statcast_bip.csv, cols xwOBA/LSA-barrel via BIP_SHRINK). Frame: starts. Heads: pha/per primary (selection/SHAP arbitrates as usual across all 5 starter heads).
- evidence: Starts frame (exposed_cols.txt, 134 cols) has 13 lu_* cols, ALL whiff/K/discipline (lu_k_sh/lu_bb_sh/lu_whiff/lu_vsh_k/lu_wsw/lu_chase/lu_brkwh/lu_offwh/lu_fbwh/lu_mix_*/ump_k_x_lu); zero damage-side lu_ cols; grep of features.py + frames_columns.txt: no lu_xwcon/lu_brl/lu_xwoba anywhere. Nearest cols: vs_hr_pa/vs_r_pg (team-SEASON outcome rates, non-posted players), pbip_xwoba/pbipd_xwoba (starter's OWN contact allowed), off_lu_obp_gap/off_lu_slg_gap/off_lu_pullair + off_xwcon/off_brl_con (team/winner frames only, features.py:3475-3479/3606-3609 — never joined to starts). FEATURE_BACKLOG Part 1 bench + Decline ledger: nothing on lineup damage-to-starts; #19 targeted total/winner only. Inputs bipd_xwoba/bipd_brl are shipped exposed batter cols, EB-shrunk decayed as-of (BIP_SHRINK features.py:990-996: xwoba prior .371 K=60, brl prior .08 K=80). Serving verified: predict.py _LU_COLS (516-520) computes lu_* as means of served batter rows over opposing-lineup mask (566-571); served rows carry bipd_* because add_batter_derived (predict.py:513) builds mix_brl/mix_xwcon from them (features.py:2274-2275, parity-tested). Data: Data/mlb_statcast_bip.csv = 1,376,160 rows, 2015-2026, xwOBA 98.5% non-null, xBA 97.5%.
- notes: Genuine asymmetry: the k head got its posted-lineup features (lu_mix_k is #1 on k per backlog line 253), pha/per never got the damage half — their only lineup view is team-season outcome rates that are B-lineup-blind and outcome-noisy. Shrinkage inherited from per-batter EB priors, so the 9-batter mean is well-behaved. Value capped at medium because hits/ER-allowed are noisier targets than K (defense/sequencing luck sits between xwOBA-on-contact and the label). Optional follow-on in the same idiom: lu_xwcon x pbipd_xwoba collision product (lu_mix template), but the two plain aggregates should ship first and let SHAP selection vote. Must land in the same finish-batch chain (tree currently mid-batch per HANDOFF_0714; frames.joblib cache is invalidated by this feature-construction change).

</details>

<details><summary>Verifier record: lu_first_pitch_economy (BUILD)</summary>

- refined_spec: (a) lu_fpsw: add lu_fpsw=("bd_fpsw_d","mean") to the batter_frame groupby(["GamePk","Team"]) at features.py:3337-3346 (rename Team->Opponent, merge on ["GamePk","Opponent"] — existing block); add "lu_fpsw" to the NaN-guard list in add_starter_derived (features.py:3176-3177). (b) fp_lu_x: in add_starter_derived, df["fp_lu_x"] = (df["lu_fpsw"] - PD_SHRINK["fpsw"][0]) * (df["pd_fps_d"] - PD_SHRINK["fps"][0]) — exact fp_matchup idiom (features.py:2297-2298) at lineup grain; priors 0.301/0.609 already in PD_SHRINK. Serving: add "lu_fpsw": "bd_fpsw_d" to Predictor._LU_COLS (predict.py:516-520); fp_lu_x computes automatically via the shared add_starter_derived call (predict.py:579) — parity by construction, NaN-safe when lineups absent. Expose: append lu_fpsw + fp_lu_x to starts_feature_cols() (features.py:3400+); SHAP selection decides per-head keeps. Inputs: Data/mlb_pitch_daily_batters.csv fp_n/fp_sw (via existing bd_fpsw_d), Data/mlb_pitch_daily_pitchers.csv fp_n/fp_s (via existing pd_fps_d). Frame: starts. Heads: outs, pbb primary; k secondary.
- evidence: EXISTS: starts lu aggregates = lu_k_sh/lu_bb_sh/lu_whiff/lu_vsh_k/lu_wsw/lu_chase/lu_brkwh/lu_offwh/lu_fbwh (features.py:3337-3347; exposed_cols.txt starts line) — no first-pitch column; pd_fps_d is a lone main (features.py:3437); zero 'lu_fpsw'/'fp_lu' hits in either column inventory; fp_matchup is batter-grain only (features.py:2297); FEATURE_BACKLOG v3 entry (L521-528) built lu_{brkwh,offwh,fbwh} but never lifted fpsw — no bench, not in Decline ledger. SERVING: predict.py _LU_COLS (L516-520) + mean-over-posted-lineup loop (L566-571); batter serving rows already carry bd_fpsw_d; product would live in add_starter_derived (features.py:3165-3215), shared by both paths. DATA: mlb_pitch_daily_batters.csv (544,604 rows) and mlb_pitch_daily_pitchers.csv (227,930 rows) fp_n/fp_sw/fp_s 100% non-null 2015-2026; empirical batter fp_sw/fp_n=0.3011 matches PD_SHRINK fpsw prior 0.301 (K100), pitcher fp_s/fp_n=0.6046 matches fps prior 0.609 (K60) (features.py:1040-1041).
- notes: Stronger than the proposal claims in one respect: the lineup's first-pitch aggression is absent from the starts frame in ANY encoding (lu_chase/lu_wsw/lu_bb_sh measure different count states/skills), so the GBM lacks even the main effect, not just the product. Pattern precedent is shipped (lu_mix_k/lu_mix_chase/lu_ars_whiff = same collision idiom; fp_matchup proven at batter grain). Cost is a few lines in three existing blocks, no new tables, no scrape, negligible retrain time. Value capped at medium: single interaction feature aimed at outs/pbb — real gap, but keep decided by SHAP selection + paired CI like every other candidate.

</details>

<details><summary>Verifier record: lu_obp_slg_gap_starts (BUILD)</summary>

- refined_spec: Training (build_starts_frame): (a) at features.py:3232-3235 add st["vs_obp"]=(st["vs_cum_H"]+st["vs_cum_BB"]+st["vs_cum_HBP"])/st["vs_cum_PA"]; st["vs_slg"]=st["vs_cum_TB"]/st["vs_cum_AB"] (cums already in sf); (b) extend the 3337-3347 groupby agg with lu_obp=("_obpp","mean"), lu_slg=("_slgp","mean") (NaN-skipping, same as lineup_aggregates:3476). (c) In add_starter_derived (parity function, called at 3388 and predict.py:579): add lu_obp/lu_slg (and vs_obp/vs_slg) to the missing-col backfill loop at :3176, then lu_obp_gap = lu_obp - vs_obp, lu_slg_gap = lu_slg - vs_slg. (d) Append lu_obp, lu_slg, lu_obp_gap, lu_slg_gap to starts_feature_cols. Serving (predict.py): extend _LU_COLS (516-520) with "lu_obp":"_obpp", "lu_slg":"_slgp" — the existing opp-mask loop at 566-571 does the rest; vs_obp/vs_slg already land on the row via s.team_offense(opp, prefix="vs") at :542, gaps come free from the shared add_starter_derived. Heads: pha, per, outs primary; k/pbb marginal (SHAP selection routes). Inputs: mlb_game_batting.csv only (via built batter frame _obpp/_slgp + _team_offense_table cums).
- evidence: exposed_cols.txt starts (134): lu_* = k_sh/bb_sh/whiff/vsh_k/wsw/chase/brkwh/offwh/fbwh/ars_whiff/mix_* (whiff-discipline only), no OBP/SLG lineup col; frames_columns.txt sf (53274x448) has zero obp/slg columns but already carries vs_cum_PA/AB/H/TB/HBP/BB (unused since #19's team_tab widening, features.py:604-608). lu_obp/lu_slg/gaps exist ONLY in gf (away_/home_/d_lu_obp_gap) — backlog #19 targeted total/winner, never starts; Decline ledger (4 items) unrelated. features.py: lineup block 3336-3348, vs_ merge 3229-3235, lineup_aggregates 3475-3479, _obpp/_slgp as-of at 2867-2868 all as claimed. predict.py: _starter_rows 522-580 already masks opp batter rows for _LU_COLS (516-520) and already calls s.team_offense(opp, prefix="vs") at :542, which returns vs_obp/vs_slg (features.py:4676-4689). Data: mlb_game_batting.csv 617,988 rows 2015→2026-07-12, PA/AB/H/TB/HBP 100% non-null, 2025 league OBP .314/SLG .404 sane.
- notes: Cross-frame port of proven #19 mechanism (kept by selection in team + winner frames) to the frame whose heads face the posted lineup directly — accept-policy explicitly favors applying mechanisms everywhere plausible. Serving is cheaper than the proposal stated: vs_obp/vs_slg already arrive in the starter serving row today. _obpp is unshrunk career ratio (NaN for debuts, NaN-skipping 9-batter mean) — same accepted noise profile as shipped game-frame lu_obp; no new shrinkage machinery. Batchable with any other edit to the same groupby (e.g. the referenced lu_contact_damage candidate).

</details>

### [19] m_kpct_paway  (cost S, value medium)
- **Frames:** batter
- **Heads:** bk/bk2/bk3/xbk primary; hit, tb2, xh secondary
- **Rationale:** The batter-vs-arsenal join carries only contact quality; K%-per-PA is not whiff-per-swing (called strikes, foul-off survival, count management) and PutAway% is type-grain two-strike conversion. Internal precedent: ARS_K_METRICS already treats these as additive to Whiff% pitcher-side. Two dict lines.
- **Synthesis spec:** Add {'K%':'m_kpct','Put Away %':'m_paway'} to ARS_B_METRICS (features.py:1467). That is the ENTIRE change: _matchup_one (:1498-1517) auto-selects/usage-weights per (BatterId,PitcherId,Season); matchup_features applies the 0.7/0.3 two-year blend; batter_feature_cols auto-splices *ARS_B_METRICS.values() (:2998 — no manual exposure edit, correcting the candidate); predict.py:503 serves through the existing call. NaN pre-2018 like all m_* (accepted).

<details><summary>Verifier record: m_kpct_paway (BUILD)</summary>

- refined_spec: Add two entries to ARS_B_METRICS at Model/features.py:1467: {"K%": "m_kpct", "Put Away %": "m_paway"}. That is the ENTIRE change — _matchup_one (features.py:1498-1517) auto-selects/renames/usage-weights all ARS_B_METRICS entries per (BatterId, PitcherId, Season) with sum(usage*metric)/sum(usage where metric non-null); matchup_features (1531-1535) applies the ARS_W1/ARS_W2 = 0.7/0.3 two-year blend with single-year fallback; batter_feature_cols already splices *ARS_B_METRICS.values() (features.py:2998) so NO manual exposure edit is needed (correction to candidate spec); predict.py:503 serves it through the existing matchup_features call. Inputs: Data/mlb_pitch_arsenals_batters.csv (Year, PlayerId, PitchType, K%, Put Away %) x Data/mlb_pitch_arsenals.csv (Year, PlayerId, PitchType, % usage), both loaded/coerced in _ars. Frame: batter. Heads: bk/bk2/bk3/xbk primary; hit/tb2/xh secondary. Requires frame rebuild (feature-construction change — cached frames.joblib not reusable).
- evidence: frames_columns.txt: no m_kpct/m_paway anywhere. exposed_cols.txt batter frame carries only m_xslg/m_xwoba/m_whiff/m_hh/m_coverage from ARS_B_METRICS; pars_kpct/pars_paway exist but ONLY on the starts frame (pitcher's own arsenal, ARS_K_METRICS features.py:1472-1473). Nearest batter-frame encodings all whiff-lens: m_whiff (per-swing), bd_tswh_c/d + ts_matchup (class-agnostic in-season 2-strike whiff), brk/off/fp_matchup. FEATURE_BACKLOG #9 "ALREADY EXISTS" verdict (line 557-558) names only the four contact metrics; Decline ledger = selection-mechanics only. Pandas: mlb_pitch_arsenals_batters.csv 54,995 rows 2017-2026, K% 100.0% non-null (mean 29.27), Put Away % 96.9% (mean 24.48); mlb_pitch_arsenals.csv usage % 100% non-null; both already numeric-coerced in _ars (features.py:282). Serving: predict.py:503 calls F.matchup_features live; exposure automatic via *ARS_B_METRICS.values() splice at features.py:2998.
- notes: Real gap: batter frame has no PA-outcome K vulnerability or 2-strike put-away conversion vs this pitcher's specific mix — every existing encoding is whiff-per-swing or class-agnostic. Internal precedent supports additivity: ARS_K_METRICS already treats K%+PutAway% as additive to Whiff% for the pitcher k head. Correlation with m_whiff caps expected lift at modest-medium; SHAP selection gates it either way, and marginal cost is two dict lines. Coverage limitation: arsenal data 2017+ so features NaN before ~2018 lag-1 — identical to the already-accepted m_* family. No shrinkage machinery needed (matches existing unshrunk m_* convention).

</details>

### [20] pit_bio_age  (cost S, value medium)
- **Frames:** starts + batter
- **Heads:** k, outs, pbb, pha, per; all batter heads via p_age
- **Rationale:** Bat bio is exposed AND selection-kept in 17 heads; the pitcher side has literally nothing but throws. Age is distinct geometry from experience (p_starts_career) and realized velo decline (pd_fbv_tr — noise on thin histories, where the age prior matters most). Serving near-zero: the bio dict is already fetched.
- **Synthesis spec:** (a) Starts: st = _attach_bio(st, raw['rosters'], 'PlayerId', 'pit') in build_starts_frame (:3218+), drop merged B/T leftovers (batter-path pattern :2738) -> pit_age = (Date-DOB)/365.25, pit_height, pit_weight; expose. (b) Batter: rosters DOB merged on StarterId (shape of the ros_p throws merge :2739-2741) -> p_age; expose. NaN where starter absent from current rosters (coverage 10% 2015 -> 95% 2026; identical accepted bat_age limitation). Serving: p_bio dict ALREADY fetched per side at predict.py:404 (only throws consumed today) — add the age/height/weight fields; starter row mirrors bat_* at :443-445. Optional later: add birthDate/height/weight to scrape_handedness.py fields for historical backfill of BOTH sides.

<details><summary>Verifier record: pit_bio_age (BUILD)</summary>

- refined_spec: Training: (a) starts frame — in build_starts_frame (features.py:3218+), add `st = _attach_bio(st, raw["rosters"], "PlayerId", "pit")` then drop the merged-in raw "B"/"T" columns (batter path's rename/drop pattern at line 2738); yields pit_age = (Date−DOB).days/365.25, pit_height (Ht via height_to_inches, already applied at load features.py:258), pit_weight (numeric Wt). Add pit_age/pit_height/pit_weight to the starts feature-col list. (b) batter frame — merge rosters[["PlayerId","DOB"]] renamed PlayerId→StarterId onto df (same shape as the ros_p throws merge at 2739–2741), compute p_age = (Date−DOB).days/365.25, add "p_age" to batter_feature_cols. NaN where starter absent from current rosters (GBM-imputed; identical accepted bat_age limitation, coverage 10%→95% 2015→2026). Leakage: static bio facts, age computed against game Date — no as-of table needed. Serving: predict.py batter row — add "p_age": (date − p_bio["dob"]).days/365.25 if notna else NaN, using the p_bio dict ALREADY fetched at line 404; starter row — same three fields from s.bio(starter_id) mirroring the bat_* pattern at predict.py:443–445. Heads: k/outs/pbb/pha/per (starts) + all batter heads via p_age; selection adjudicates per-head as usual. Optional (not required): scrape_handedness.py already hits statsapi /people for all historical PlayerIds — adding birthDate/height/weight to its fields param would backfill historical coverage for both bat_ and pit_ bio later.
- evidence: EXISTS check: _attach_bio defined features.py:2043, called ONCE ever — line 2737 with prefix 'bat' (batter frame); pitchers get only T→pit_throws (lines 2739–2746). Token search of exposed_cols.txt + frames_columns.txt: only bat_height/bat_weight/bat_age exist; no pit_age/pit_height/pit_weight/p_age in any of the 5 frames. Nearest exposed: p_starts_career (batter #35, starts #391), p_ip_per_start (#53/#410), pd_fbv_tr (starts #426). FEATURE_BACKLOG.md: zero pitcher-bio mentions — not benched, not in Decline ledger. DATA (pandas, read-only): mlb_rosters.csv = 992 rows/992 unique PlayerIds; PlayerId/Ht/Wt/DOB 0% null; DOB 100% parses %m/%d/%Y (the parser features.py:259 already applies); Wt 100% numeric. Starter-row coverage (mlb_game_pitching.csv GS==1, n=53,274): 2015 10.4% → 2019 34.2% → 2022 52.4% → 2024 68.6% → 2025 81.0% → 2026 95.3% — matches proposal's claim. SERVING: predict.py:404 already fetches p_bio = s.bio(opp_starter) per side (only pit_throws consumed today); Stores.bio (features.py:4822) is player-generic and returns height/weight/dob. PRECEDENT: feature_keep.json keeps bat_age+bat_height+bat_weight in 17 heads (hr, hit, tb2, bk, xtb, xbk, xhrr, sb, ...), so bio survives SHAP selection on this model. build_starts_frame(raw,...) at features.py:3218 has raw['rosters'] in scope and st carries PlayerId+Date — the attach is a one-liner in existing idiom.
- notes: Verified real gap with strong shipped precedent: bat bio is exposed AND selection-kept in 17 heads while the pitcher side has literally nothing (only throws). Age is distinct information geometry from p_starts_career (experience ≠ age) and pd_fbv_tr (realized decline ≠ prior expectation of decline; trend is noise on thin histories). Coverage sparsity in early training years (10% in 2015) is the same accepted current-rosters limitation as bat_age; missingness mildly encodes still-active-in-2026 but that pattern already ships on the batter side. Serving is near-zero cost — the starter's bio dict is already fetched per side at predict time and only pit_throws is used. Not GRAY: no bench to overturn, no scrape needed, no policy call — SHAP selection remains the per-head decider.

</details>

### [21] p_chaos_bf  (cost S, value medium)
- **Frames:** starts + batter (+ optional team sibling)
- **Heads:** per, run, run2, xrun, total, team_total (sb/pha weaker)
- **Rationale:** PIT_STATS omits WP/BK/HBP entirely: HBP is a baserunner channel outside BB, and WP/BK advancement is invisible to the bb/h/era decomposition — while the pitcher's MiLB-translated HBP rate IS exposed (confirmed asymmetry). Thin channel (~1.8% of BF) but genuinely absent, concentrated on per and run-scoring heads.
- **Synthesis spec:** Append WP, BK, HBP to PIT_STATS (features.py:442) — pc_/ps_ cums + day-start snap come free (:537-542, :571-576). Rates in _pit_rates (:1916) mirrored in Stores.starter_feats (:4640-4649): p_chaos_bf = shrink(pc_WP+pc_BK, pc_BF; prior 0.0082, K=200) and p_hbp_bf = shrink(pc_HBP, pc_BF; prior 0.0097, K=200) — shrunk per the pvloc/BIP_SHRINK convention (plain pc_* rates are unshrunk; corrected). Recency: append WP/BK/HBP to the rolling-5 lists (:543, :4650) -> p5_chaos_bf (NOT a decayed 'd' col — no such idiom on starts; corrected). Add keys to the no-history NaN dict (:4623). Expose in starts + batter (rides the StarterId merge like pc_k_bf). Optional: pen_chaos_bf via _bullpen_table stats (:620-621).

<details><summary>Verifier record: p_chaos_bf (BUILD)</summary>

- refined_spec: features.py:442 append "WP","BK","HBP" to PIT_STATS — pc_/ps_ cums and day-start snap come free (lines 537-542, 571-576). Rates in _pit_rates (line 1916) and mirrored in Stores.starter_feats (4640-4649): p_chaos_bf = shrink(pc_WP+pc_BK, pc_BF; prior 0.0082, K=200 BF) and p_hbp_bf = shrink(pc_HBP, pc_BF; prior 0.0097, K=200), constants fixed by convention (pvloc_from_sums / BIP_SHRINK precedent — note plain pc_* rates are unshrunk, so shrinkage here follows the pvloc idiom, justified at ~0.8% event rate); ps_ siblings same formula on season cums. CORRECTION to candidate: no decayed idiom exists on the starts frame — replace "p_chaos_d" with a p5 recency sibling by appending "WP","BK","HBP" to the rolling-5 lists at features.py:543 and Stores line 4650 (p5_chaos_bf = raw last-5 sum ratio). Add new keys to the no-history NaN dict at 4623. Expose in the starts feature list (~line 3403 region) and batter frame list (~line 2984); batter frame rides the existing StarterId starter-column merge (same path as pc_k_bf). Optional cheap sibling: append "WP","BK","HBP" to _bullpen_table stats (line 620-621) and add pen_chaos_bf = (pen_cum_WP+pen_cum_BK+pen_cum_HBP)/pen_cum_BF alongside lines 2025-2028; pen serving rides the existing cum-table path. Inputs: Data/mlb_game_pitching.csv cols WP, BK, HBP, BF, GS, PlayerId, Date, GamePk. Target heads: per, run, run2, xrun, total, team_total (sb/pha weaker); SHAP selection decides.
- evidence: features.py:442 PIT_STATS=["BF","HR","SO","BB","Outs","ER","H","Strikes","NP"] — no WP/BK/HBP; grep of exposed_cols.txt (starts 134 + batter 385) finds no chaos/wp/hbp rate in any encoding (only milb_hbp/pmilb_hbp, the MiLB-translated priors — the claimed asymmetry is real); frames_columns.txt shows raw WP/BK/HBP in the starts frame but no pc_/ps_/p5_ derivative; FEATURE_BACKLOG.md has zero wild/balk/hbp/chaos mentions (not benched/declined). Pandas on Data/mlb_game_pitching.csv: 228,069 rows 2015-2026, WP/BK/HBP/BF all int64 zero nulls; starters (53,274 rows): (WP+BK)/BF=0.00817 (proposal ~0.0082), HBP/BF=0.00971 (proposal ~0.0097), every season populated; relief chaos 0.01281, HBP 0.01172. As-of idiom verified at features.py:537-542 (shifted cumsum) + 571-576 (day-start snap, list-driven); serving verified at Stores.starter_feats line 4641 (frame[PIT_STATS].sum() is generic) + NaN path 4623; pen table _bullpen_table line 618-621 takes an explicit stat list.
- notes: Genuine gap, not a re-encoding: HBP is a baserunner channel excluded from BB (pc_bb_bf is walks only), and WP/BK advancement is invisible to the bb/h/era decomposition (pc_era fuses it; pd_zone_d/pd_rel_sd are command proxies, not event rates; psb_* is steals only). The pmilb_hbp-exposed-while-MLB-missing asymmetry is confirmed. Value is real but thin (~1.8% of BF combined; GBM can partially proxy wildness from zone/release-scatter/bb columns), concentrated in per and run-scoring heads — medium rests on the per head plus S cost under the SHAP-selection regime. Rare-event shrinkage is mandatory (K~200 BF); expose pc_BF (already exposed) as support. Two candidate spec errors found and corrected: (1) the "decayed sibling" has no idiom on the starts frame — use the p5 rolling list instead; (2) plain pc_* rates are unshrunk, so the shrunk chaos rates follow the pvloc/BIP_SHRINK convention rather than the raw pc_ idiom.

</details>

### [22] arsenal_dynamics_pack (merged: pars_entropy + pars_yoy_dynamics)  (cost S, value medium)
- **Frames:** starts + batter
- **Heads:** k, outs, per, bk family, xbk; hr/tb2 via trends
- **Rationale:** Mix breadth/unpredictability exists in no encoding — the exposed 3-class shares explain only ~35% of type-grain entropy variance — and the 0.7/0.3 blend hides trajectory entirely (offseason per-pitch stuff deltas, new-pitch adoption = classic breakout markers; all existing trend cols are in-season). Same annual table, one merged build; two required data-hygiene corrections folded in.
- **Synthesis spec:** One per-(PlayerId,Year) pass over mlb_pitch_arsenals.csv. Entropy: p_i = %_i/sum(%_j) (RENORMALIZE — 5.5% of pitcher-years sum <95; corrected from raw %/100), H = -sum p ln p; mask usage-sum<50 rows NaN; pars_entropy = _blend_years 0.7/0.3, wired inside pitcher_arsenal_feats (:1538) + the matchup route to the batter frame. YoY: self-join Y-1 to Y-2 on (PlayerId,PitchType) with Pitches>=50 in BOTH years (guards RV/100 tiny-sample outliers, min -271); pars_rv_trend = sum_shared[usage1*clip(RV100_1-RV100_2,-10,10)]/sum_shared[usage1]; pars_whiff_trend same with clip +/-25; pars_new_usage = share of Y-1 usage with no Y-2 row (NaN when no Y-2 arsenal at all). Expose in starts_feature_cols (+predict.py:563 loop) and batter frame via the pars_ opposing-starter route. NaN for seasons <=2018.

<details><summary>Verifier record: pars_entropy (BUILD)</summary>

- refined_spec: Per (PlayerId, Year) in Data/mlb_pitch_arsenals.csv: p_i = %_i / sum_j(%_j) over that pitcher-year's PitchType rows (RENORMALIZE — 5.5% of pitcher-years sum <95 because Statcast omits rarely-thrown types; do NOT use %/100 raw as originally specced); H_y = -sum_i p_i*ln(p_i). pars_entropy = _blend_years(H_{Season-1}, H_{Season-2}) with existing 0.7/0.3 weights + single-year fallback (features.py:1479). Wire-up: (a) starts frame — compute inside pitcher_arsenal_feats (features.py:1538) by adding the per-(PitcherId,ArsYear) entropy to each lag's `one(lag)` output and to `cols` at line 1564; add 'pars_entropy' to the starts feature list near features.py:3408. (b) batter frame — entropy is batter-independent, so compute once per (PitcherId, ArsYear) and merge inside _matchup_one (features.py:1489) alongside ARS_P_METRICS aggregation; add to batter feature cols. Serving: rides predict.py:560-563 (own-arsenal call) and predict.py:502-506 (opposing-starter matchup call) with no new plumbing. Heads: k, outs, per, bk/bk2/bk3, xbk. Optional free sibling from the same groupby: pars_ntypes = count of types with p_i>=0.02 (corr 0.87 with H; skip if keeping to one column).
- evidence: EXISTS check: grep features.py + exposed_cols.txt + frames_columns.txt — zero entropy/diversity/n_types/per-type-usage columns in any frame. Nearest: p_brk_d/p_off_d/p_fblou_d/p_fbmidu_d/p_fb95u_d (3-class+velo-band shares, features.py:2284-2306), pd_trsame_d (repeat rate, not breadth), pars_whiff/kpct/paway/rv100/cov (usage-weighted quality); pars_xslg/whiff/hh/rv100 on batter frame. FEATURE_BACKLOG #1-32 + Decline ledger (line 568+): no entropy item; 'arsenal_whiff' in frames is the built #32 collision column, unrelated. DATA (pandas run on Data/mlb_pitch_arsenals.csv): cols Year/PlayerId/PitchType/% all present, 32,373 rows, 2017-2026, 0% nulls; 7,974 pitcher-years; median usage sum 100.0 but only 94.5% in [95,105] (1st pct 42.7) -> renormalization required; entropy mean 1.125 sd 0.318 (proposal's numbers confirmed); corr(type-entropy, 3-class entropy)=0.59, residual sd 0.257 -> ~65% of variance invisible to exposed class shares; corr(H, n_types)=0.87. SERVING: predict.py:560-563 runs pitcher_arsenal_feats(s.raw['ars_p']) pre-game; predict.py:502-506 runs matchup_features for opposing-starter pairs — both routes exist today. LEAKAGE: season-lag Y-1/Y-2 join via _blend_years (features.py:1479, 0.7/0.3), identical to shipped pars_*. Coverage caveat: seasons <=2017 NaN (same as existing pars_*).
- notes: Genuinely new geometry: measured 3-class entropy (best-case GBM reconstruction from exposed p_brk_d/p_off_d/velo-band shares) explains only ~35% of type-grain entropy variance (corr 0.59). Two honest caveats: (1) entropy is 0.87-correlated with pitch-type count, so much of the signal is 'breadth' — still unexposed anywhere; (2) the model already carries strong whiff/putaway/tto columns (p_tto_decay, pars_paway, pd_trsame_d), so incremental lift on k/outs is plausible but not guaranteed — one cheap scalar, let SHAP selection adjudicate. 168 one-pitch pitcher-years get H=0 legitimately; no extra shrinkage needed since the input is a full-season Statcast aggregate, but consider masking pitcher-years with raw usage sum <50 as NaN (~1% tail) rather than trusting renormalized shares from heavily truncated rows.

</details>

<details><summary>Verifier record: pars_yoy_dynamics (BUILD)</summary>

- refined_spec: Per (PitcherId, Season): A1 = arsenal rows Year=Season-1, A2 = Year=Season-2, self-join on (PlayerId, PitchType); restrict shared-type set to Pitches>=50 in BOTH years (guards RV/100 tiny-sample outliers). pars_rv_trend = sum_shared[usage1 * clip(RV100_1 - RV100_2, -10, 10)] / sum_shared[usage1]; pars_whiff_trend = same with Whiff % (clip +-25); pars_new_usage = sum[usage1 of A1 types with no A2 row]/100, NaN if pitcher has no A2 arsenal at all; all three NaN when no shared types. Inputs: Data/mlb_pitch_arsenals.csv cols PlayerId, Year, PitchType, RV/100, Whiff %, %, Pitches. Build as a small helper beside pitcher_arsenal_feats reusing its rename/merge idiom (Season=Year+lag prior-season join, leakage-safe); NaN for seasons <=2018. Expose: append to starts_feature_cols (features.py ~L3408) and the predict.py:563 column loop; join to batter frame on (PitcherId=StarterId, Season) alongside the pars_* opposing-starter route. Serving rides the existing live pitcher_arsenal_feats/matchup path — no new store or scrape.
- evidence: features.py: only pars_ cols are ARS_P_METRICS/ARS_K_METRICS levels (pars_xslg/whiff/hh/rv100/kpct/paway/cov); _blend_years (L1479-86) = 0.7*Y1+0.3*Y2 with fallbacks, years not recoverable. exposed_cols.txt: no trend/yoy/new-usage arsenal col in any frame; nearest trends all in-season (p5_k_trend, p5_hr_trend, p_era_trend, pd_fbv_tr, pd_fbv_fade). FEATURE_BACKLOG: #32 = arsenal collision (different); decline ledger = selection mechanics only — no bench. Data (pandas on Data/mlb_pitch_arsenals.csv): 32,373 rows, 2017-2026, PlayerId/PitchType/%/RV-100 100% non-null, Whiff % 99.6%; prev-year type match 67.6% (proposal's 61% conservative); 72.1% of pitcher-years have a Y-2 arsenal; shared types = 95.4% of Y-1 usage; RV/100 outliers exist (min -271.1, tiny-sample types). Serving: predict.py L560 calls F.pitcher_arsenal_feats and L503 F.matchup_features live on slate pairs; col lists at predict.py:563 and features.py starts_feature_cols L3400-08.
- notes: Proposal's why_new claim verified exactly: the 0.7/0.3 blend exposes level only, and all existing trend columns are in-season. Two corrections folded into the spec: (1) RV/100 has extreme thin-sample values (min -271), so min-Pitches>=50 filter + delta clipping is required, not optional; (2) 'pars_entropy' cited in the serving note does not exist in features.py or the frames — it is a sibling candidate, so the 'same-join byproduct' cost claim should not be relied on, but the standalone join on a 32k-row annual file is negligible anyway. Trend cols are all-NaN for 2015-2018 training seasons (arsenals start 2017) — same support profile as existing pars_* levels; GBMs handle NaN natively. Value is concentrated early-season and on starter heads (k, per, pha) plus HR.

</details>

### [23] ump_bb_x_bb  (cost S, value medium)
- **Frames:** batter
- **Heads:** bb, xbb
- **Rationale:** Completes the ump-cross 2x2: ump_k_x_bk gives the K heads the ump-x-BATTER cell, but the walk heads only have ump-x-pitcher. Game-level ump rate is constant across a slate's 18 batters so the tree must carve a rotated boundary — the same argument that shipped the other three cells, one of which (ump_k_x_bk) is selection-kept in several heads.
- **Synthesis spec:** df['ump_bb_x_bb'] = ump_bb_pct * c_bb_pct_sh — one line directly after features.py:2238 (use the SHRUNK rate, matching mix_bb; the shipped K-cell's unshrunk c_k_pct is the noisier choice). Add to batter_feature_cols beside the trio (:3062). Serving zero-cost: shared add_batter_derived, both inputs already on every serving row.

<details><summary>Verifier record: ump_bb_x_bb (BUILD)</summary>

- refined_spec: In add_batter_derived, directly after features.py:2238: df["ump_bb_x_bb"] = df["ump_bb_pct"] * df["c_bb_pct_sh"] (use the shrunk rate, matching mix_bb at 2342; the shipped K-cell's unshrunk c_k_pct is the noisier choice for walks). Add "ump_bb_x_bb" to the batter feature-column list beside the trio at features.py:3062. Inputs: Data/mlb_umpires.csv (GamePk, Date, HpUmpId) + Data/mlb_game_batting.csv (SO/BB/BF per game via _ump_game_totals) through the existing _ump_asof strictly-prior cumsum; batter side from the existing career shrunk as-of c_bb_pct_sh. Frame: batter (~190k rows). Heads: bb, xbb (selection adjudicates the rest). Serving: zero extra work — add_batter_derived is the shared training/serving function (predict.py:513), both inputs already present in the serving row.
- evidence: exposed_cols.txt batter frame contains only ump_k_pct/ump_bb_pct/ump_k_x_pk/ump_k_x_bk/ump_bb_x_pbb — no ump_bb x batter cell in any inventory or feature_keep artifact. features.py:2236-2238 confirms the shipped trio inside add_batter_derived; FEATURE_BACKLOG line 556-557 shows #7 closed with exactly those three cells, #29 added ump_k_x_lu; decline ledger (4 items) has no entry for this cell. Leakage: _ump_asof (features.py:1830-1852) is strictly-prior cumsum + shared _ump_shrink; c_bb_pct_sh is career shrunk as-of. Serving: predict.py:513 calls the same add_batter_derived on the serving frame; ump_bb_pct arrives via s.ump_feats (predict.py:368/413, prior fallback at features.py:4509-4520) and c_bb_pct_sh is provably in the serving row because mix_bb (features.py:2342) consumes it in the same function today. Data (pandas): mlb_umpires.csv 26,637 rows, HpUmpId 100% non-null, 2015-2026, 141 umps, 26,637/26,637 GamePk coverage vs mlb_game_batting.csv; BB/PA 100% non-null over 617,988 rows. Symmetric cell ump_k_x_bk survives selection in multiple heads of current feature_keep.json.
- notes: Genuine un-adjudicated hole in an otherwise-complete 2x2 family (not benched, not declined). Game-level ump_bb_pct is constant across a slate's 18 batters, so the tree must carve a rotated boundary vs per-batter walk rate — same carve-resistance argument that shipped the other three cells, and the symmetric ump_k_x_bk is kept by SHAP selection in several heads. Ceiling limited: one column primarily for bb/xbb. SHAP selection remains the sole decider, so downside is bounded.

</details>

### [24] run_game_layer_starts  (cost S, value medium)
- **Frames:** starts
- **Heads:** per primary; pha, outs secondary
- **Rationale:** The running game is ENTIRELY absent from the starts frame — psb_* exist only batter-side describing the opponent, and the lu_ table has no speed. A steal-vulnerable pitcher facing a running lineup converts identical contact sequences into more runs; genuinely absent geometry since neither marginal exists on the frame. Effect modest (~0.05-0.1 ER in tail matchups); two regime corrections applied.
- **Synthesis spec:** (a) _merge_prior_season(st, _pitcher_season_table(raw['pit_season']), 'PlayerId', ['psb_sb27','psb_stop']) — the batter-frame idiom (:2718-2719) keyed by the starter's OWN PlayerId. (b) lu groupby adds: lu_sb=('d_sb_pa_sh','mean'), lu_sprint=('bat_sprint','mean'); expose all 4. (c) Cross in add_starter_derived: run_game_x = (lu_sb - lg_sb_pa)*(psb_sb27 - lg_sb27_prior) with lg_sb27_prior = PRIOR-SEASON league SB*27/Outs computed per year (NOT a fixed constant — league SB/27 jumped 0.52->0.73 at the 2023 rules; corrected) and lg_sb_pa from the existing _league_env_table joined to starts. Use psb_sb27 in the cross, NOT psb_stop (65% NaN under PSB_MIN_ATT=5; corrected). Serving: self.pitcher_prior(starter_pid, season) (helper exists :4610-4613) + two _LU_COLS entries.

<details><summary>Verifier record: run_game_layer_starts (BUILD)</summary>

- refined_spec: (a) In the starts builder (near the pmilb_ merge, features.py:~3380): st = _merge_prior_season(st, _pitcher_season_table(raw["pit_season"]), "PlayerId", ["psb_sb27", "psb_stop"]) — identical idiom to the batter-frame join at features.py:2718-2719 but keyed by the starter's own PlayerId. (b) Add to the lu groupby (features.py:3337-3346): lu_sb=("d_sb_pa_sh", "mean"), lu_sprint=("bat_sprint", "mean"); add lu_sb, lu_sprint, psb_sb27, psb_stop to starts_feature_cols(). (c) Cross: run_game_x = (lu_sb − lg_sb_pa) × (psb_sb27 − lg_sb27_prior), where lg_sb_pa is the as-of league steal env already computed in _league_env_table (join it to starts alongside lg_k_pa/lg_r_pa/lg_hr_pa) and lg_sb27_prior = PRIOR-SEASON league SB*27/sum(Outs) from mlb_pitching_stats.csv (NOT a fixed constant — league SB/27 jumped 0.52→0.73 at the 2023 rule change; per-year centering keeps the product zero-mean across eras). Compute in add_starter_derived so train/serve parity holds. Serving: starts row calls self.pitcher_prior(starter_pid, season) (helper exists, features.py:4610-4613, currently only called for opp_starter at predict.py:403); add {"lu_sb": "d_sb_pa_sh", "lu_sprint": "bat_sprint"} to predict.py _LU_COLS (516-520). Inputs: Data/mlb_pitching_stats.csv (SB, CS, PK, IP→Outs, prior-season via existing table), batter frame d_sb_pa_sh (as-of decayed), Data/mlb_sprint_speed.csv via bat_sprint (prior-season). Frame: starts. Heads: per primary, pha/outs secondary; SHAP selection decides final exposure.
- evidence: Starts frame 134 exposed cols: zero steal/speed columns (grep psb_|tsb_|sb|sprint|brr = no hits); lu_ aggregates are K/whiff/discipline only (features.py:3338-3346). psb_sb27/psb_stop built in _pitcher_season_table (features.py:1943-1959, SB*27/Outs and (CS+PK)/att with PSB_MIN_ATT=5) but joined only onto the batter frame by StarterId (features.py:2718-2719). Team frame has off_lu_brr/off_lu_brr_xb and winner has d_bsr_luck — other frames, and no pitcher-x-lineup steal interaction anywhere. Not in FEATURE_BACKLOG #15-32 or Decline ledger (checked). Serving: pitcher_prior(pid,season) at features.py:4610-4613 already returns psb_sb27/psb_stop; predict.py _LU_COLS (516-520) is the lu_ serving path over batter serving rows that already carry d_sb_pa_sh and bat_sprint. Data: mlb_pitching_stats.csv 9,712 rows 2015-2026, SB/CS/PK/TBF 100% non-null, Outs derived from IP at load (features.py:298); psb_sb27 mean 0.61 / median 0.40 / p90 1.42, n=9,699; psb_stop non-NaN only ~35% of pitcher-seasons (att>=5). mlb_sprint_speed.csv 7,067 rows 2015-2026, 100% non-null. League SB/27 by year: ~0.47-0.53 (2015-22) then 0.73/0.75/0.72/0.69 (2023-26 rule change) — fixed constant centering is wrong.
- notes: Caveats: (1) psb_stop is NaN for ~65% of pitcher-seasons (PSB_MIN_ATT=5) — thin support; psb_sb27 (100% coverage where Outs>0) should carry the load, and the cross should use psb_sb27 not psb_stop. (2) The proposal's fixed LG_SB27 constant is wrong given the 2023 rules discontinuity (0.52→0.73 SB/27); refined spec centers per-year. (3) Effect size is modest (~0.05-0.1 ER/start in tail matchups) — real but not large; mechanism (steal-vulnerable pitcher x running lineup → same contact line, more runs) is genuinely absent geometry on the starts frame since neither marginal exists there. (4) bat_sprint is Statcast sprint speed, not bat-tracking — no time-gate issue, confirmed in GLOSSARY.md.

</details>

### [25] np_per_out_budget  (cost S, value medium)
- **Frames:** starts
- **Heads:** outs primary; k, pha secondary
- **Rationale:** Trees cannot divide: pc_NP and pc_Outs sit in the frame but no pitch-economy ratio is exposed (pc_strike_pct is per-pitch). Decomposes the leash into budget x efficiency — the GBM-side arithmetic of the queued hazard-v2 — with value concentrated in exactly the ramp/IL-return/opener regimes where the outs head currently errs. Cross-pitcher spread is real (p5-p95 5.11-5.87 = ~1 inning of implied outs at a 95-pitch budget).
- **Synthesis spec:** (a) p_np_per_out = K-shrunk pc_NP/pc_Outs toward the league constant 5.48 (per-year stable 5.34-5.60); (b) p_exp_outs = p_np_l3 / p_np_per_out. SERVING FIX REQUIRED (found in verification — the candidate's claim was wrong): starter_feats emits only rates, so also emit out['pc_NP'] and out['pc_Outs'] inside the existing loop (:4640-4649, sums in scope) and append both to the no-history NaN list (:4623-4630). Compute both cols in the shared add_starter_derived (:3165; parity by construction); expose in starts_feature_cols.

<details><summary>Verifier record: np_per_out_budget (BUILD)</summary>

- refined_spec: (a) p_np_per_out = shrink(pc_NP, pc_Outs) -> K-shrink of the career as-of ratio toward the league constant 5.48 NP/out (per-year stable 5.34-5.60, so a constant prior in the existing SHRINK idiom is sound; handles pc_Outs=0 early-career rows). (b) p_exp_outs = p_np_l3 / p_np_per_out (NaN on first career start via p_np_l3, GBM-safe). Inputs: Data/mlb_game_pitching.csv NP + IP (Outs = ip_to_outs(IP), features.py:230), already flowing as pc_NP/pc_Outs cumsum intermediates on the starts frame (frames_columns.txt, snapped as-of at features.py:571-576) and p_np_l3 (existing exposed main). Build: (1) train side — columns already on st in build_starts_frame; (2) serve side — in starter_feats emit out['pc_NP']=s['NP'] and out['pc_Outs']=s['Outs'] inside the existing pc-loop (features.py:4640-4649, sums already in scope) and append both to the no-history NaN key list (features.py:4623-4630); (3) compute p_np_per_out and p_exp_outs in the shared add_starter_derived (features.py:3165; called by build_starts_frame at 3388 and predict.py at 579 -> parity by construction); (4) append both names to starts_feature_cols() (features.py:3400). Frames: starts only. Heads: outs primary; k/pha secondary via selection vote.
- evidence: EXISTS check: exposed starts list (134 cols) has no pitch-economy ratio — nearest are pc_strike_pct/ps_strike_pct (Strikes/NP, per-pitch, features.py:4648), p_ip_per_start (ps_Outs/3/p_starts_season, features.py:3243 — leash outcome, no NP), p_np_last/p_np_l3 (budget only, features.py:548-550), p_outs_sd/p_short_share/team_st_outs_pg (#16), p_gap15/p_ramp60/p_short_heavy (#17 binary flags). pc_NP/pc_Outs confirmed present-but-unexposed in frames_columns.txt; FEATURE_BACKLOG Part 1 bench inventory = pull_fence/porch_margin/d_ps_xwcon_d/lg_* only, zero backlog/Decline-ledger matches for np-per-out/efficiency/budget — unexposed raw sums are the codebase idiom, not a bench. DATA: Data/mlb_game_pitching.csv 228,069 rows 2015-2026, NP 100% non-null; raw file has IP not Outs (ip_to_outs, features.py:230), IP 100% non-null; 53,274 GS==1 rows (matches ~53k starts frame); league NP/out 5.479, per-year stable 5.34-5.60; 0.068% zero-out starts; cross-pitcher dispersion real (450 pitchers >=30 starts: SD 0.238, p5-p95 5.11-5.87 = ~1+ inning of implied-outs spread at a 95-pitch budget). SERVING correction found: starter_feats (features.py:4640-4649) sums PIT_STATS incl. NP/Outs but emits only rates — pc_NP/pc_Outs are NOT on serving rows today, so the sums (or ratio) must be emitted there too, plus added to the no-history NaN key list (features.py:4623-4630). LEAKAGE: pc_* are as-of cumsums snapped to day start (asof_cols incl. pc_{PIT_STATS}, features.py:571-576); p_np_l3 = shift(1).rolling(3).
- notes: Proposal's serving claim was slightly wrong (serving rows do not already carry pc_NP/pc_Outs — starter_feats emits rates only), but the fix is two lines where the sums are already in scope, so cost stays S. Value is real but regime-concentrated: for stable full-budget starters p_exp_outs ~ 3 x p_ip_per_start (correlated); the new information lives exactly in ramp/IL-return/opener budget-change regimes where the outs head currently errs (hazard v1 did not fix outs, hazard v2 queued) — hence medium not high. This is the GBM-side arithmetic of hazard-v2 and complements, not duplicates, backlog #16/#17 which built shape/policy but no efficiency ratio.

</details>

### [26] own_def_uer_starts  (cost S, value medium)
- **Frames:** starts
- **Heads:** per primary; pha secondary (expect little)
- **Rationale:** per is the ONLY head whose label is defined by the earned/unearned scoring split, and it has zero error signal today — own_def_p_oaa is RANGE, and this codebase already adjudicated error-proneness as a distinct axis when it added opp_def_uer to the batter frame. Honest bound: unearned runs are 7.8% of runs, team spread ~+/-0.09/27 — low end of medium; 4 verbatim-idiom lines.
- **Synthesis spec:** In build_starts_frame: od = _team_defense_table(gp) with cum_* renamed od_cum_*; st = _asof_merge(st, od, by=[Team,Season]); own_def_uer = (od_cum_R - od_cum_ER)*27/od_cum_Outs. Expose near team_st_outs_pg (:3454 region). Serving: 'own_def_uer': s.team_defense(team, season, date) — store and team-generic helper exist (:4024, :4757-4762). Unshrunk, matching opp_def_uer (team cums accrete fast).

<details><summary>Verifier record: own_def_uer_starts (BUILD)</summary>

- refined_spec: Training (features.py build_starts_frame, insert after line 3253, mirroring the lea merge idiom verbatim): od = _team_defense_table(gp); od = od.rename(columns={c: f"od_{c}" for c in od.columns if c.startswith("cum")}); st = _asof_merge(st, od, by=["Team","Season"]); st["own_def_uer"] = (st["od_cum_R"] - st["od_cum_ER"]) * 27 / st["od_cum_Outs"]. Inputs: Data/mlb_game_pitching.csv columns R, ER, Outs (Outs derived from IP at load; 100% coverage 2015-2026) via the already-built _team_defense_table. Add "own_def_uer" to the starts-frame feature superset (the list region near features.py:3454 where team_st_outs_pg lives). Serving (predict.py _starter_rows, ~line 557 row dict): "own_def_uer": s.team_defense(team, season, date) — Stores.def_tab and the team-generic helper already exist; no new scrape. Frames: starts only. SHAP selection decides keeps per head; primary target per (only head whose label depends on the earned/unearned split), secondary pha. No shrinkage, matching the existing unshrunk opp_def_uer (team-level daily cum accretes fast; early-season NaN/noise handled identically to the batter-frame version).
- evidence: EXISTS check: starts exposed list (134 cols) has only own_def_p_oaa/if/of for defense — no uer/error column; opp_def_uer exposed only on batter + team frames (exposed_cols.txt lines 2, 10); sf frame (448 cols, frames_columns.txt line 5) has no uer column even unexposed; FEATURE_BACKLOG mentions opp_def_uer only as the batter-frame add (line 514), Decline ledger has nothing related. LEAKAGE: _team_defense_table (features.py:655) = _daily_cum as-of idiom, already vetted at features.py:2857 (batter) and :3641 (game); build_starts_frame already uses the identical ["Team","Season"] _asof_merge at line 3252. SERVING: Stores.def_tab built at features.py:4024, team-generic Stores.team_defense at features.py:4757-4762, called in predict.py:402/655; _starter_rows (predict.py:522) has `team` in scope at line 557. DATA (pandas): mlb_game_pitching.csv 228,069 rows 2015-2026, R/ER/IP 100% non-null; team-season uer27 mean 0.358, std 0.087, p5-p95 0.225-0.52; unearned share of runs 7.78%, 360 team-seasons.
- notes: Honest magnitude bound: unearned runs are 7.8% of all runs and team spread is ~±0.09 runs/27 outs, so this is the low end of medium — the case rests on (a) per being the ONLY head whose label definition depends on the earned/unearned attribution split while having zero error signal today, (b) the codebase's own documented adjudication that error-proneness is an axis OAA range misses, and (c) a genuinely verbatim 4-line build + 1-line serve reusing tables already constructed in both paths. Effect direction on per is nonmonotone (errors shift R to unearned, depressing ER attribution, but extend innings/baserunners) — GBM-carvable given the input, not hand-signable. pha lift is second-order; do not expect movement there.

</details>

### [27] raw_pitch_schema_batch_v6_v7 (merged: pd_stretch_vdrop_d + pd_effprem_d + pd_relsep_d + pd_brkmov_d + bd_twostrike_class_split)  (cost M, value medium)
- **Frames:** starts + batter (opposing-starter joins)
- **Heads:** per, pha, outs, k (starts); bk family, hit/tb, traffic-dependent rbi/hrr heads (batter)
- **Rationale:** The five verified-new raw-archive axes, each a stable trait no tree can reach from exposed sums: base-out stretch delta (YoY r=0.69; base-out state appears NOWHERE in the pipeline), perceived-velo premium (r=0.95, orthogonal to release velo at r=0.09 — a full mph-equivalent axis the model cannot see), release-slot separation (r=0.74, only 0.34 corr with the confounded pooled scatter), breaking movement cause (reliability 0.945 vs 0.578 for its whiff outcome — the pd_ivb_d argument), and the two-strike-x-class putaway cell. M cost because of the re-agg, paid once for all five; ranked below the S-cost mediums per the tie rule only.
- **Synthesis spec:** ONE scrape_pitches.py aggregate() schema wave + ONE --from-raw re-agg (~4 min, 2015-2026 parquets on disk) + the :368-377 NaN schema-guards, amortized across five features. PITCHER file: (1) fbstr_n/fbstr_v (FF/SI, runners on any base) -> pd_stretch_vdrop_d = (stretch_v - windup_v)*n/(n+300), windup complement from existing fb_n/fb_v; join batter-side as p_stretch_d; sign = stretch minus windup (mean +0.08 — consider 'vdelta' naming). (2) fbe_n/fbe_sum (effective_speed - release_speed on FF/SI) -> PD_SHRINK 'effprem' (+0.15, K=200) -> pd_effprem_d + p_effprem_d; document the tracking-era drift of the league premium (-0.56 in 2015 -> +0.30 in 2024) beside the fixed prior; optional percv_matchup collision gated on mains voting in. (3) rpf_n/x/z + rpb_n/x/z (per-class release centroids: fastball-remainder class vs BREAKING set) -> pd_relsep_d = hypot of centroid deltas, NaN unless both n>=30, shrunk toward 0.165 ft; + p_relsep_d; optional sumsqs -> pd_relsd_w_d ADDED alongside (never replacing) pooled pd_rel_sd. (4) brkmov_n/brkmov_sum (12*hypot(pfx_x,pfx_z) over BREAKING; pfx-based, NOT spin_axis) -> PD_SHRINK 'brkmov' (~10.7, K=100, n>=30 gate) -> pd_brkmov_d + p_brkmov_d; collision via the CONTACT channel — centered(p_brkmov_d)*centered(bipd_xwoba) — NOT the proposed whiff cross (empirically inverted: low-movement quartile whiffs most). SHARED agg list: (5) ts_brk_n/sw/wh (two-strike x breaking class) -> PD_SHRINK 'tsbrkwh' (~0.30 measured, K=80) -> bd_tsbrkwh_c/d + ts_brk_matchup = centered(bd_tsbrkwh_d)*centered(p_brk_d). All ride existing PD_SHRINK/PD_BATTER/pdo_ plumbing and the standard daily-CSV serving path — zero new serving inputs; daily scrape carries the cols forward automatically.

<details><summary>Verifier record: bd_twostrike_class_split (BUILD)</summary>

- refined_spec: Scrape side (Scrapers/scrape_pitches.py, _aggregate ~line 205-279): add three columns using existing masks — ts_brk_n=(two_k & is_brk), ts_brk_sw=(two_k & is_brk & swing), ts_brk_wh=(two_k & is_brk & whiff), appended to the `shared` list (:266-272) so both daily files carry them (batter is the consumer). Optional same-cost mirror: ts_off_* via is_off. One-time backfill: python scrape_pitches.py --from-raw over Data/raw_pitches/pitches_{2015..2026}.parquet (~4 min precedent), then the 06:00 daily scrape carries it forward automatically. Model side (Model/features.py): (1) PD_SHRINK["tsbrkwh"] = (measured prior ~0.30, K=80, "ts_brk_wh", "ts_brk_sw") near :1054; (2) add "tsbrkwh" to the PD_BATTER tuple (:1122-1127) -> bd_tsbrkwh_c/bd_tsbrkwh_d via existing shrunk cum/decayed as-of path; (3) collision near :2290: df["ts_brk_matchup"] = (df["bd_tsbrkwh_d"] - PD_SHRINK["tsbrkwh"][0]) * (df["p_brk_d"] - PD_SHRINK["brk"][0]) — both factors serving-computable today; (4) append bd_tsbrkwh_c, bd_tsbrkwh_d, ts_brk_matchup to the batter feature-col list (:3117-3129 block). Frames: batter only. Serving: zero new inputs; identical path to every bd_/p_*_d feature. Upgrade path (mirror pd_ahsp into the batter frame's pdo_dk store for a sharper pitcher factor) stays optional, not required.
- evidence: EXISTS check: 0 hits for ts_brk/tsbrk in features.py, exposed_cols.txt, frames_columns.txt; nearest = bd_tswh_c/d + bd_brkwh_c/d (marginals), ts_matchup (features.py:2310), brk_matchup (:2290), pd_ahsp_d (:1066/:3214, starts frame only — candidate's claims all accurate). Backlog: #25 = pitcher half only; batter ts×class cross absent from built items and Decline ledger; 'per-count leverage splits' still listed as remaining raw-pitch ideas (line 530). Plumbing verified: PD_SHRINK entries brkwh(0.321,K100)/tswh(0.225,K100)/brk(0.286,K150) at features.py:1034-1066; PD_BATTER tuple :1122-1127 auto-generates bd_*_c/_d; p_brk_d built+exposed (:2604,:2639,:3119). Scraper: shared agg list scrape_pitches.py:266-272 has ts_* and brk_* separately, no cross; --from-raw path exists (:368-390), v4-v6 bump precedent documented (~4-min re-agg, backlog log line 539-548). Data (pitches_2023.parquet, 720,684 rows; 2015-2026 all on disk): balls/strikes 100% non-null, pitch_type 99.58%; ts_brk cell 76,995 pitches/season, per-batter median 103.5 pitches / 62 swings (p25=35); whiff-per-swing ts 0.250 vs brk 0.329 vs ts_brk 0.302 — joint cell at its own level, not recoverable from marginals. Batter daily CSV header confirmed to lack ts_brk_* (ends at f32_wh).
- notes: Genuine gap: the two-strike x breaking-class cell is the putaway cell for the K heads and cannot be carved from the bd_tswh/bd_brkwh marginals by a GBM. Value tempered by (a) two-strike mix already ~36% breakers so bd_tswh partially contains it, (b) thin per-batter support (median 62 swings/season vs K=80) — expect the shrunk collision ts_brk_matchup to survive selection more readily than the raw bd_ pair. needs_scrape=false is honest: schema bump to an existing scraper + one free --from-raw re-agg, established v4->v6 precedent, no new serving-side input (catcher-framing failure mode does not apply). Timing only: FINISH_PLAN's active directive freezes the current one-batch finish chain, so this is a post-finale add unless the user reopens the batch — a scheduling call, not a feature objection.

</details>

<details><summary>Verifier record: pd_brkmov_d (BUILD)</summary>

- refined_spec: Schema add to Data/mlb_pitch_daily_pitchers.csv in Scrapers/scrape_pitches.py aggregate() (mirror ivb_n/ivb_sum at lines 263-264): mask = raw.pitch_type.isin(BREAKING) & pfx_x.notna() & pfx_z.notna(); brkmov_n = mask.astype(float); brkmov_sum = (12*sqrt(pfx_x^2+pfx_z^2)).where(mask).fillna(0). Backfill history via the existing --from-raw re-agg of Data/raw_pitches/pitches_2015..2026.parquet (fold into the same single re-agg as any other raw-archive adds). features.py: add brkmov_n/brkmov_sum to the v6 NaN schema-guard list (~line 375); PD_SHRINK entry "brkmov": (prior~10.7 measured on the full re-agg, K=100, "brkmov_sum", "brkmov_n"); append "brkmov" to PD_PITCHER_D (~line 1131); NaN-gate at dk_brkmov_n >= 30 (idiom of FBSD_MIN_N); join opposing starter into the batter frame as p_brkmov_d via the pdo_ rename/shrink block (features.py 2544-2642, mirror p_ivb_d). Frames: starts (pd_brkmov_d) + batter (p_brkmov_d). Collision: SKIP the proposed brkmov_matchup with bd_brkwh_d — verified pitcher-level corr(mov, whiff)=-0.14 and low-movement quartile whiffs MOST, so a whiff collision is physically misaligned; if any collision, use the contact channel instead: brkmov_contact = centered(p_brkmov_d, 10.7) * centered(bipd_xwoba) (evidence: xwOBA-on-contact falls 0.367->0.351 across movement quartiles). Serving: unchanged pd_/p_ daily-CSV path shared with Stores via PD_PITCHER_D.
- evidence: EXISTS check: no brkmov/movement column in exposed_cols.txt or frames_columns.txt; nearest are pd_ivb_d/p_ivb_d (FF-only vertical break, backlog #27), pd_brk_d/p_brk_d (usage), pd_brkwh_d/bd_brkwh_c,d/lu_brkwh/brk_matchup (whiff outcomes), pars_* (arsenal outcomes, no movement); Decline ledger and Part 1 have no movement bench. DATA: raw_pitches parquets carry pfx_x/pfx_z all years; pfx-notna on breaking-class pitches = 100.00% (2015/2016/2020/2023/2026), 99.98% (2025); brk share 24.7-31.0% of pitches; league brkmov mean 10.24-11.30 in, between-pitcher sd 4.16 in. VALUE: 2025 split-half per-pitcher reliability r=0.945 (movement) vs r=0.578 (brk whiff/swing) at n>=30/half — fast-stabilizing cause confirmed; pitcher-level corr(mov, brk whiff rate) = -0.139 — orthogonal to existing outcome columns, not a re-encoding; xwOBA-on-contact by movement quartile 0.367->0.351 (monotone, n~8.9k/quartile) — contact-suppression channel real; per-pitch whiff-given-swing HIGHEST in low quartile (0.327 vs 0.304) — whiff channel absent. IDIOM: scrape_pitches.py aggregate() ivb at lines 157, 202-204, 263-264; features.py schema NaN-guard 370-377, PD_SHRINK "ivb" 1070, PD_PITCHER_D 1124-1131, batter pdo_ join 2544-2642, collisions 2379-2381; PD_PITCHER_D comment confirms frame/serving Stores share the list. Daily scrape pulls full Savant details CSV (pfx_x included) and both daily and --from-raw route through the same aggregate(raw), so needs_scrape=false is accurate.
- notes: Genuine gap: breaking-class movement CAUSE exists nowhere (only usage + whiff outcomes), and it is near-orthogonal to those outcomes (r=-0.14), so this passes the new-information-geometry bar rather than re-encoding pd_brkwh_d. Value routed through contact suppression (pha/per/tb-type heads) and thin-history starters (n>=30 gate fills in ~1.5 starts; reliability 0.945 vs 0.578 for whiff), NOT through k/bk as the proposal claims — per accept policy no pre-declared targets, selection votes per head, so this only changes expectations, not buildability. Downgraded value to medium: effect size on contact quality is modest (~16 pts xwOBA across quartiles) and whiff channel is empirically absent. Cost M assumes it rides an already-planned --from-raw re-agg; alone it would still be M (one re-agg pass + ~6 touch points in the shipped ivb idiom).

</details>

<details><summary>Verifier record: pd_effprem_d (BUILD)</summary>

- refined_spec: Scraper schema add (scrape_pitches.py aggregate(), matching the fb_v idiom at lines 245-246): fbe_ok = is_fb & effective_speed.notna() & release_speed.notna(); fbe_n = fbe_ok count; fbe_sum = (effective_speed - release_speed).where(fbe_ok).fillna(0) sum — pitcher file only. Backfill history via the existing --from-raw re-agg against Data/raw_pitches/pitches_{2015..2026}.parquet (~4 min). features.py: add PD_SHRINK["effprem"] = (+0.15, 200, "fbe_sum", "fbe_n") and "effprem" to PD_D → pd_effprem_d = (pd_dk_fbe_sum*wdn_pd + k*prior)/(pd_dk_fbe_n*wdn_pd + k) in starts frame (idiom of features.py:3305-3308); batter frame join via the pdo_ block (idiom of p_fbv_d, features.py:2588-2590): p_effprem_d = (pdo_dk_fbe_sum*wdn2 + k*prior)/(pdo_dk_fbe_n*wdn2 + k). Optional second-stage collision after mains vote in: percv_matchup = bd_fb95wh_d * ((p_fbv_d - PD_SHRINK["fbv"][0]) + (p_effprem_d - 0.15)) — perceived-velo sibling of bat_velo_matchup (features.py:2251). Serving: same daily-CSV pd_/pdo_ path, no new scrape. Document the era drift of the league premium (-0.56 in 2015 → +0.30 in 2024 → +0.16 in 2025; tracking-system regime shifts) next to the fixed prior.
- evidence: EXISTS check: 0 matches for effective_speed/release_extension/effprem/percv in Model/features.py, exposed_cols.txt (all 5 frames), frames_columns.txt; only velo source in pipeline is release_speed (scrape_pitches.py:159); nearest cols pd_fbv_c/d/tr/sd/fade, p_fbv_d, fb95/fbmid/fblo bands, bat_velo_matchup (features.py:2251) are ALL release-speed-based. FEATURE_BACKLOG #27 (built) = movement pfx/IVB only, extension listed merely as available, effective_speed never mentioned; Decline ledger has no perceived-velo item. DATA (pyarrow on Data/raw_pitches/pitches_2015..2026.parquet, all 12 present): effective_speed+release_speed exist; FF/SI joint coverage 97.9% (2015), 98.5% (2016), >=99.8% (2017-2026). Cross-pitcher premium SD 0.746 mph (2024, n=409 >=300 FB) / 0.740 (2025, n=401); YoY r=0.950 (n=260); corr(premium, release velo)=0.089 (near-orthogonal — claim confirmed). SERVING: daily scrape fetches full Savant type=details CSV (effective_speed already in every chunk; aggregate() just ignores it); --from-raw backfill flag exists (scrape_pitches.py:368-390); pd_ construction idiom at features.py:3305-3308, pdo_ batter join at 2588-2590, serving helper at 4559.
- notes: All proposal numbers reproduced exactly (SD 0.75, YoY r=0.95 n=260, prior +0.15 matches 2025 mean +0.16). Genuinely new axis: effective_speed feeds nothing today and the premium is orthogonal to the whole release-velo family (r=0.089 with pitcher mean velo) while being the most stable trait tested. One caveat the proposal omitted: league-mean premium drifts by tracking era (-0.56 in 2015 to +0.30 in 2024), so the fixed +0.15 prior fits only the Hawk-Eye era — minor since K=200 barely binds at starter support (~450+ FB/season) and trees absorb slow level shifts, but it belongs in the schema comment. 2015-16 coverage slightly lower (97.9%/98.5%) — the fbe_ok notna mask already handles it. Cost is M (scraper schema add + one shared --from-raw re-agg), not S. Expected value medium, not high: single column on an already dense velo axis; the collision is gated on mains voting in, per the proposal.

</details>

<details><summary>Verifier record: pd_relsep_d (BUILD)</summary>

- refined_spec: Schema bump to mlb_pitch_daily_pitchers (scrape_pitches.py per-day agg, pitcher file only): rpf_n/rpf_x/rpf_z = count/sum(release_pos_x)/sum(release_pos_z) over class-0 pitches (NOT in BREAKING, NOT in OFFSPEED — the existing fastball-remainder class) with release_pos_x & release_pos_z notna; rpb_n/rpb_x/rpb_z = same over the existing BREAKING set {SL,ST,SV,CU,KC,CS,SC,KN}. Backfill via --from-raw over Data/raw_pitches/pitches_2015..2026.parquet (columns: pitcher, pitch_type, release_pos_x, release_pos_z; 99.6-99.9% populated). features.py, four touch points, all existing idioms: (1) from-daily NaN-gating list at features.py:368-377 — add the 6 cols (NaN under pre-bump file rows); (2) vectorized starts frame next to pd_rel_sd (features.py:3315): pd_relsep_d = hypot(pd_dk_rpb_x/pd_dk_rpb_n - pd_dk_rpf_x/pd_dk_rpf_n, pd_dk_rpb_z/pd_dk_rpb_n - pd_dk_rpf_z/pd_dk_rpf_n), NaN unless dk_rpf_n>=30 AND dk_rpb_n>=30 (centroid gate, deliberately lower than RELSD_MIN_N=100 since means need less n than SDs), then K-shrunk toward league-mean separation ~0.165 ft; (3) batter frame p_relsep_d for the opposing starter via the pdo_ rename map (features.py:2543-2561) + derivation next to p_rel_sd (features.py:2625); (4) serving Stores: pd_pitcher_feats reads dk['rpf_*']/dk['rpb_*'] from _pd_sums (same path as pd_rel_sd at features.py:4563), batter-store mirror for p_relsep_d. Expose pd_relsep_d in the starts feature list (features.py:~3445) and p_relsep_d in the batter list (features.py:~3130). Enters the superset; SHAP selection votes per head as usual (no pre-declared targets; direction empirical — deception vs tipping). Optional same-batch refinement: also emit rpf_x2/rpf_z2/rpb_x2/rpb_z2 sumsqs and ADD pd_relsd_w_d (within-class scatter = per-class release_scatter_from_sums, usage-weighted) — ADD alongside pd_rel_sd, do NOT replace the shipped pooled column (baseline comparability).
- evidence: EXISTS check: frames_columns.txt has only pooled release sums (pd_cum/dk_rp_n/x/x2/z/z2; batter-side pdo_dk_rpn/rpx/rpx2/rpz/rpz2) feeding pd_rel_sd (features.py:3315-3318) and p_rel_sd (features.py:2625) via release_scatter_from_sums (features.py:1106); no per-class release column in either inventory. Nearest cols: pd_rel_sd/p_rel_sd (pooled, confounded), pd_trsame_d/pd_trfbbrk_d (sequencing frequency), pd_ivb_d (movement). FEATURE_BACKLOG #25/#26/#27 and the Decline ledger contain nothing on per-class release geometry; the v5 log's 'release-point consistency' idea shipped as the POOLED scatter. Scrape check: scrape_pitches.py:92 BREAKING={SL,ST,SV,CU,KC,CS,SC,KN} matches the candidate's class exactly; class-0 remainder = fastball class per line 91 comment; rp_ sums (lines 247-251) confirmed pooled over all types. DATA (read-only pandas on Data/raw_pitches): release_pos_x/z+pitch_type nonnull 99.9%/99.7%/99.6% in 2015/2020/2025; fb/brk shares ~.55-.63/.25-.31. FB-vs-BRK centroid separation (min 100/class): mean 0.165 ft, cross-pitcher sd 0.114-0.118 ft (claim 0.105 confirmed); YoY 2024->2025 r=0.743 n=342 (claim 0.70 confirmed). Redundancy: separation vs pooled-scatter r=0.337 — mostly orthogonal, unrecoverable from pooled sums. SERVING: pd_pitcher_feats (features.py:4540-4568) already derives pd_rel_sd from decayed _pd_sums; NaN-under-pre-schema gating idiom at features.py:368-377; --from-raw re-agg precedent ~4 min.
- notes: Every claimed statistic reproduced or exceeded on the actual archive (sd 0.114 vs claimed 0.105; YoY r=0.743 vs claimed 0.70). The decisive number is the r=0.337 correlation with pooled pd_rel_sd: this trait is ~89% orthogonal to the only existing release feature, and per-class centroids cannot be carved from pooled sums by any tree — genuinely new information geometry, not a re-encoding. Stable mechanical trait (YoY 0.74) with dense support on the starter frame. Cautions: (a) keep the refinement as an ADD, not a replacement of shipped pd_rel_sd; (b) timing is a user call — the finish batch #15-32 is already built and the FINISH_PLAN chain is aborted mid-sequence with an all-at-once directive, so this either joins that batch pre-chain or queues behind it; a schema bump forces a --from-raw re-agg + frames rebuild (frames.joblib cache invalidated). No new scrape; odds untouched.

</details>

<details><summary>Verifier record: pd_stretch_vdrop_d (BUILD)</summary>

- refined_spec: Scrape (scrape_pitches.py aggregate, v6 batch): runners = raw.on_1b.notna() | raw.on_2b.notna() | raw.on_3b.notna(); fbv_ok = is_fb & velo.notna() (is_fb = FASTBALLS={FF,SI}, identical to existing fb_n gate at line 244); add to base DataFrame: fbstr_n = (fbv_ok & runners).astype(float), fbstr_v = velo.where(fbv_ok & runners).fillna(0.0); append both to pit_stats (line 273). Backfill 2015+ via --from-raw (~4 min), batchable with the other v6 schema adds; daily scrape adds them incrementally for free. Feature (Model/features.py, existing dk idiom used for pd_fbv_d): stretch_v = dk_fbstr_v/dk_fbstr_n; windup_v = (dk_fb_v - dk_fbstr_v)/(dk_fb_n - dk_fbstr_n); pd_stretch_vdrop_d = (stretch_v - windup_v) * dk_fbstr_n/(dk_fbstr_n + K), K=300 (convention, not swept; shrinks toward 0 delta). Guard den>0 on both splits, NaN otherwise (matches pd_fbv_sd NaN pattern at features.py:3328). Frames: starts frame as pd_stretch_vdrop_d; join opposing starter's value into batter frame as p_stretch_d (same join used for p_fbv_d at features.py:2589). Serving: mirror in the serving helper block (features.py:4546-4566) from the same dk sums — no new store, no new scrape. Heads: per, pha, outs, k; batter rbi/rbi2, hrr2/3/4, tb2/tb3; team_total. Naming note: mean delta is +0.08 (slightly HARDER from stretch on average) — sign is stretch minus windup; consider name pd_stretch_vdelta_d to avoid the "drop" misread.
- evidence: EXISTS: zero grep hits for on_1b/stretch/windup/fbstr in Model/features.py, Scrapers/scrape_pitches.py, exposed_cols.txt, frames_columns.txt; nearest cols = pd_fbv_c/d/tr/sd/fade (starts), p_fbv_d (batter), rbi_conv (features.py:2349) and #28 per-conversion chain (features.py:3198) — all overall-velo or traffic-x-overall-damage, none base-out-conditioned; FEATURE_BACKLOG remaining-ideas list (lines 529-530, 547) and Decline ledger have nothing related. DATA (pyarrow, read-only): all 12 parquets 2015-2026 have on_1b/2b/3b, pitch_type, release_speed; velo notna 99.6-100%, FF/SI share 47-57%, runners-on 42-43%, joint qualifying 19.6-23.6% of pitches (~140k+/season). Claimed stats reproduced: cross-pitcher delta sd 0.377 (2024) / 0.359 (2025) vs claimed 0.38; YoY r=+0.692, n=302 overlap at min-100-per-split (claimed +0.70/n=217, consistent with a higher min-n). 2025 delta p5..p95 = -0.52..+0.66 mph, mean +0.08. SPEC: FASTBALLS={FF,SI} (scrape_pitches.py:88) matches fbv_ok, so the windup complement subtracts like from like against existing fb_n/fb_v (lines 244-246). SERVING: daily fetch is type=details (line 108, all Savant cols — 2026 parquet from same fetches has the base cols at full coverage); serving-side pd_fbv_* helper computes from the same dk sums (features.py:4546-4566); --from-raw re-agg is an established ~4-min path (backlog line 227).
- notes: Real gap: base-out state appears nowhere in the pipeline, so no GBM can carve this from existing mains (pd_fbv_sd confounds the delta with general variability; pd_fbv_fade is within-game trajectory; #28 chain is traffic x overall damage). Trait is stable (YoY r=+0.69 reproduced) but small-amplitude (~0.36 mph cross-pitcher sd), hence medium not high value — it is a second-order conditional-stuff effect concentrated on run-scoring states, most plausible on per/pha and traffic-dependent batter heads. Proposer's discipline is good: whiff-version (r=+0.11) correctly excluded. Cost is M because it requires the --from-raw re-agg, though marginal code is a few lines in existing idioms; batch it with the other v6 schema adds so the re-agg is paid once.

</details>

### [28] milb_priors_extension (merged: milb_sb_prior v1 + pmilb_outs_ps)  (cost M, value medium)
- **Frames:** batter (steal axis) + starts (workload)
- **Heads:** sb, triple, run, run2 (bat); outs, k, pha, per at cold start (pit)
- **Rationale:** The last uncovered axes of the MiLB translation: rookie speed (every existing speed column is NaN/empty exactly at debut) and demonstrated stamina/role pedigree (all workload mains MLB-history-only; 6.5% of starts are a pitcher's first 3, with 81.5% MiLB coverage there). Riders already proved out (milb_n #8 in the PA gate, 22/24 heads kept MiLB cols). Value narrow-but-targeted; the role-filter bug fix is load-bearing and must not ship as originally specced.
- **Synthesis spec:** One milb_priors.py extension + ONE artifacts/milb_priors.joblib rebuild. BAT side (fit-free v1): carry SB, CS, OPP = (H-2B-3B-HR).clip(0)+BB+HBP per (PlayerId,Season,Level); over kept levels (AAA/AA/A+) accumulate SERVE_DECAY-weighted count sums Y-3..Y-1; serve milb_att = (sb_w+cs_w + 40*0.145)/(opp_w+40) and milb_sb_succ = (sb_w + 20*0.72)/(sb_w+cs_w+20) — constants measured from the file; NO level offsets (success flat 0.720-0.723 across kept levels — empirical license). v2 logit-offset translation DEFERRED pending v1 selection survival (needs MLB SB plumbing not in the PA frame). PIT side: pmilb_outs_ps over rows with GS>=1 AND GS>=0.6*G (MANDATORY role filter — season IP includes relief innings; the proposed naive 3*IP/GS yields impossible >30-out values on 17.3% of rows), outs_ps = IP-thirds/GS, GS-decayed pooled window; pmilb_gs_share = decayed sum(GS)/sum(G) over ALL kept rows (pure-reliever 0 is signal). Add the 4 names to MILB_BAT_COLS/MILB_PIT_COLS as explicit passthroughs; frames merge (:2708, :3380) and serving milb_feats (:4489) pick them up with zero further plumbing.

<details><summary>Verifier record: milb_sb_prior (BUILD)</summary>

- refined_spec: V1 (build this): in Model/milb_priors.py _milb_counts("bat"), also carry SB, CS (to_numeric, fillna 0) and OPP = (H-2B-3B-HR).clip(0) + BB + HBP into the per-(PlayerId,Season,Level) row. In build_serving, restrict to fitted levels as today (AAA/AA/A+), and inside the season-Y loop accumulate decayed COUNT sums sb_w = sum(SB * 0.6^(y-1-Season)), cs_w, opp_w over seasons Y-3..Y-1 (decay weights only — counts are already mass, do not multiply by n). Serve two new columns: att = (sb_w + cs_w + 40*0.145) / (opp_w + 40/0.145 ... ) — concretely: milb_att = (sb_w + cs_w + K_A*LG_ATT)/(opp_w + K_A) with K_A=40, LG_ATT=0.145 (measured league MiLB attempt rate); milb_sb_succ = (sb_w + K_S*LG_SUCC)/(sb_w + cs_w + K_S) with K_S=20, LG_SUCC=0.72 (measured kept-level rate; matches house SB_SUCC_K=20 idiom at features.py:1748). No level offsets (measured success is flat 0.720-0.723 across kept levels; attempt-rate level drift is absorbed by GBM ordering — document as convention). In features.py: append "milb_att","milb_sb_succ" to MILB_BAT_COLS and pass them through _milb_cols (they are not t_* renames — add explicit passthrough); NaN where no kept-level history. Batter frame only (no pmilb mirror). Rebuild artifacts/milb_priors.joblib, retrain. Heads: sb (repair priority), triple, run, run2. V2 (defer, separate item): per-level logit offsets on mover pairs for SB/(1B+BB+HBP) needs MLB-side SB counts NOT in the PA frame (CLASSES has no SB) — requires mlb_batting_stats.csv or game-log plumbing, fit MLB seasons <=2024; only worth it if v1 columns survive selection.
- evidence: EXISTS check: exposed_cols.txt + frames_columns.txt milb cols are exactly milb_{k,bb,hbp,b1,b2,b3,hr,n} / pmilb_* — no steal axis anywhere, not benched (absent from frames entirely); milb_priors.joblib["bat"]["serve"] verified = PlayerId, t_K..t_OUT, n_eff, Season only; _milb_counts reads H/2B/3B/HR/BB/HBP/SO and drops SB/CS. Nearest speed cols all fail at debut: bat_sprint/bat_hp1b/bat_brr/bat_brr_xb (prior-season MLB), c_sb_succ + c/s/d/r7/r15/r30_sb_pa_sh (MLB as-of cum, empty until MLB games), psb_*/tsb_* (opponent env). FEATURE_BACKLOG: #14 = 8-class translation as built; no MiLB-SB item in #15-32, Part 1, or Decline ledger. DATA (pandas): milb_batting.csv 94,725 rows 2010-2026, SB/CS/H/2B/3B/HR/BB/HBP/PA/Level/Year/PlayerId all 100% non-null; league attempt (SB+CS)/OPP=0.145, success=0.713; kept levels AAA/AA/A+ success 0.723/0.720/0.722 (flat — vindicates fit-free v1), attempt 0.104/0.129/0.143. SERVING: features.py:2708 batch merge + Predictor.milb_feats features.py:4489 exact-(pid,season) lookup both driven by _milb_cols/MILB_BAT_COLS (features.py:1734-1745) — two added columns flow through both paths. 2,915 players in 2025 serve slice.
- notes: Genuinely the last uncovered axis of the MiLB translation: every existing speed column is NaN/empty exactly where this has support (debuts/thin history). Value is real but narrow — differentiates only the rookie slice of a ~6.5%-base sb head plus triple/run/run2; medium, not high. V1 is fit-free so no circularity with 2025/2026; v2's offset fitting is the only part with plumbing cost (M) and should be gated on v1 surviving SHAP selection. Convention constants (K_A=40, LG_ATT=0.145, K_S=20, LG_SUCC=0.72) are measured from the file, not guessed; success-rate flatness across levels (0.720-0.723) is the empirical license for skipping offsets.

</details>

<details><summary>Verifier record: pmilb_outs_ps (BUILD)</summary>

- refined_spec: Extend milb_priors.py pit-side serving with two workload columns (no translation fit). Input: Data/milb_pitching.csv cols PlayerId, Year, Level, G, GS, IP (+ optional TBF), Level in artifact kept set {AAA, AA, A+}. Per season-row: thirds = floor(IP)*3 + round((IP mod 1)*10). (a) pmilb_outs_ps: computed ONLY over rows with GS>=1 AND GS >= 0.6*G (REQUIRED role filter — IP is season-total, relief innings contaminate mixed-role rows; 17.3% of unfiltered rows are impossible >30), per-row outs_ps = thirds/GS; serve-season Y pools rows with Season in [Y-3, Y-1] (SERVE_LOOKBACK) weighted w = GS * SERVE_DECAY^(Y-1-Season) (GS-exposure decay; the proposal's TBF weight also works but GS is the natural per-start exposure); NaN if no qualifying rows. (b) pmilb_gs_share: over ALL kept-level rows in the same decayed window (including GS=0 reliever seasons — a pure-reliever pedigree of 0 is the signal), = decayed sum(GS)/decayed sum(G); NaN if no rows. Emit both into out['pit']['serve'] in build_serving; add the two names to MILB_PIT_COLS (features.py:1737) and pass them through _milb_cols (features.py:1740, they need no MILB_REN rename — just include in the cols list); frames merge at features.py:3380 and serving milb_feats at features.py:4489 then pick them up with zero further plumbing. Frame: starts. Join: exact (PlayerId, Season), same as existing pmilb_*. Heads: read all starter heads per no-pre-declared-targets policy; the identified hole is outs/k/pha/per at cold start (first ~3 MLB starts = 6.5% of frame, 81.5% MiLB coverage there). Leakage: seasons <= Y-1 only, identical window to existing build_serving; no FIT_MAX_MLB_SEASON dependency since nothing is fit. Requires one milb_priors.joblib --rebuild.
- evidence: EXISTS check: exposed pmilb_* = {pmilb_k, pmilb_bb, pmilb_hbp, pmilb_b1, pmilb_b2, pmilb_b3, pmilb_hr, pmilb_n} only (exposed_cols.txt + frames_columns.txt; features.py:1734-1745 MILB_REN/_milb_cols emits rates+n_eff, nothing else). Nearest existing workload cols all MLB-history: p_ip_per_start, p_np_last, p_np_l3, p_outs_sd, p_short_share, p_gap15, p_ramp60, team_st_outs_pg (all present in starts exposed list). FEATURE_BACKLOG #14 = translated rates only; Decline ledger + Part 1 have no MiLB-workload item. DATA: milb_pitching.csv 101,984 rows, Year 2010-2026, IP/GS/G/TBF/Level/Year/PlayerId all 100% non-null; IP fractional parts strictly {0,1,2} (thirds notation confirmed); artifact milb_priors.joblib pit kept levels = ['A+','AA','AAA']; 24,504 GS>=1 kept-level rows, 7,114 pitchers, ~1,500 rows/yr. BUG FOUND in proposed formula: IP is season-total incl. relief innings — naive 3*thirds/GS gives outs_ps>30 on 17.3% of GS>=1 rows (max 279); with GS>=0.6*G role filter: mean 14.91, sd 3.54, max 28 (sane). Population claims verified: 6.53% of 2015+ MLB starts (n=53,274) are a pitcher's first 3 (proposal said 6.5%); 81.5% of 1,319 debut starters have a kept-level GS>=1 MiLB season in Y-3..Y-1. SERVING: features.py:3380 (frames merge) and features.py:4489 milb_feats (exact-(PlayerId,Season) serving lookup) are both generic over MILB_PIT_COLS — extending the serve table + column list serves automatically.
- notes: Genuinely new geometry: outing-length/role pedigree exists nowhere (all workload mains are MLB-history and NaN at debut; pmilb_* rates carry quality, not stamina). Value is concentrated — meaningful only on the ~6.5% early-career slice, but that is precisely where the outs head's top feature family is missing, and coverage there is 81.5%. The proposal's naive 3*IP/GS formula MUST NOT ship as written (season-total IP includes relief innings; 17.3% of rows produce impossible values) — the GS>=0.6*G role filter in the refined spec fixes it and was verified to yield a physically sane distribution (mean ~14.9 outs/start, max 28). Thin-support shrinkage is inherent in the existing decay/NaN-impute idiom (GBMs impute NaN); no extra K-shrinkage needed since these are workload means, not rare-event rates.

</details>

### [29] ump_r_g_batter  (cost S, value low)
- **Frames:** batter
- **Heads:** run, run2, rbi, rbi2, hrr2-4, xrun, xrbi
- **Rationale:** Exposure-scope artifact of #21 (which targeted total/per only) — not a bench or decline. Honest value grade: the run family already keeps the ump zone products, so the increment is only the non-zone-mediated run tilt, heavily shrunk and diluted over 9 batters. But the identical quantity was adjudicated BUILD-worthy at game grain and the cost is one string.
- **Synthesis spec:** Add 'ump_r_g' to batter_feature_cols (~:3048, beside ump_k_pct/ump_bb_pct). Nothing else: the column is already computed on the batter frame by _merge_ump (:2920) and already on every serving row via **ump (Stores.ump_feats :4503-4520). Do NOT add it to train.py _UMP (:305) — that exact-name list is the zone-only exclusion; leaving it out routes ump_r_g to the run-production family by default. No frame rebuild needed; takes effect at the next selection regen + chain.

<details><summary>Verifier record: ump_r_g_batter (BUILD)</summary>

- refined_spec: One-line exposure: add "ump_r_g" to batter_feature_cols in Model/features.py, next to "ump_k_pct", "ump_bb_pct" (~line 3048-3049). Nothing else to build — the column is already computed on the batter frame by _merge_ump (features.py:2920, from _ump_asof at 1830-1852 with _ump_rg_shrink K=40/prior=9.0 R/G), and predict.py _batter_rows already spreads **ump (all three keys from Stores.ump_feats, features.py:4503-4520) onto every serving row. Do NOT add ump_r_g to train.py _UMP (line 305) — that exact-name list is the zone-only exclusion that routes ump_k/bb away from non-K/BB props; leaving ump_r_g out of it means the run-production family (run, run2, rbi, rbi2, hrr2/3/4, xrun, xrbi) sees it by default and SHAP selection (sole feature decider) votes per head at the next feature_select regen. No frame-construction change, so the cached frames.joblib remains valid; requires only the already-planned selection regen + two-train chain to take effect. Inputs: Data/mlb_umpires.csv (GamePk, Date, HpUmpId; 2015-2026, 100% coverage) joined to game run totals from the game logs via _ump_game_totals.
- evidence: EXISTS check: exposed_cols.txt batter list (line 2, 385 cols) has ump_k_pct, ump_bb_pct, ump_k_x_pk, ump_k_x_bk, ump_bb_x_pbb but NO ump_r_g; starts (line 4) and team (line 10) both expose ump_r_g; frames_columns.txt batter frame (line 3) carries ump_r_g — computed-but-unexposed exactly as claimed. features.py: _merge_ump applied to batter frame at line 2920 (attaches all 3 ump cols); batter_feature_cols at ~3048 lists only "ump_k_pct", "ump_bb_pct" with comment "routed to the K/BB props only"; starts cols include "ump_r_g" (3456); team cols include it (3913). FEATURE_BACKLOG Part 1 bench inventory (lines 36-42, 5 items: pull_fence, porch_margin, batter fatigue, d_ps_xwcon_d, lg_*) does NOT contain ump_r_g; Decline ledger (4 items, selection-design + shrinkage-priors) does not either; #21 (lines 207-209) says "Targets: total, per" — batter exposure was never scoped, never benched, never declined. LEAKAGE: _ump_asof (features.py:1830-1852) = groupby(HpUmpId) cumsum minus current row (strictly prior games; comment notes an ump never works the plate twice in one day, so date-clean); _ump_rg_shrink (1824-1827) = (runs + 40*9.0)/(games + 40) using UMP_RG_PRIOR/UMP_RG_K from line 1629. SERVING: predict.py _batter_rows line 369 calls s.ump_feats() (features.py:4503-4520, filters Date < date, returns all 3 keys incl. ump_r_g) and spreads **ump into every batter row — the value is already on every serving row today. ROUTING: train.py:305 _UMP = ["ump_k_pct", "ump_bb_pct"] is an exact-name list, so ump_r_g is NOT caught by the run/rbi/hrr/sb PROP_EXCLUDE entries (train.py:343-400, comment "ump is zone-only") and would reach the run-production family by default. Keep-list check: artifacts/feature_keep.json has ump_r_g on no head — consistent with the hand-restored pre-batch 24-head snapshot (aborted chain; #21 selection regen not yet run), so no adjudicated verdict against it exists. DATA: Data/mlb_umpires.csv = 26,637 rows, 2015-2026, HpUmpId 100% non-null, 141 umps, ~2,430 games/season (2020=898); per-ump R/G over 105 umps with 100+ games: p10=8.64, p50=8.94, p90=9.38 (raw p90-p10 spread 0.74 R/G before shrinkage).
- notes: The candidate's factual claims all verified: genuine exposure-scope artifact of #21 (targeted total/per only), not a recorded bench or decline, zero serving plumbing. Value graded honestly low rather than the proposal's implied medium: the run/rbi family is not fully ump-blind today — it keeps the zone interaction products (ump_k_x_pk, ump_k_x_bk, ump_bb_x_pbb are not in _UMP, and feature_keep.json shows run/rbi kept them) — so the orthogonal increment of ump_r_g is only the non-zone-mediated run effect, a small game-level tilt (heavily shrunk, K=40) diluted across 9 batters. That said, BUILD is decisive because the cost is literally one string in a list, the identical quantity was already adjudicated BUILD-worthy at game grain (#21, total/per), and house policy (no pre-declared targets, expose everywhere plausible, selection prunes) makes broad exposure the default idiom. Shrinkage need is already handled by _ump_rg_shrink; zero-history umps land on the 9.0 R/G league prior in both paths identically.

</details>

### [30] precip_batter  (cost S, value low)
- **Frames:** batter
- **Heads:** count means (xh/xtb/xbk...) and all exposure-sensitive batter heads
- **Rationale:** Rain-truncation cuts every counting stat identically to the rationale that exposed Precip on 2 of the 3 frames that compute it; Condition captures only 4.1% of Precip>0 games so the signal is not recoverable. Value low (heavy precip ~7% of games, roofs dilute) but this is the cleanest possible build.
- **Synthesis spec:** Append 'Precip' to batter_feature_cols. The column is already on the built batter frame (_attach_context :1988-1995) and already on every batter serving row (**wx at predict.py:437, forecast wired 07-14 for 1E) — a feature-cols change, not construction, so the cached frames.joblib remains valid per HANDOFF rules. Run predict.py --selftest parity after.

<details><summary>Verifier record: precip_batter (BUILD)</summary>

- refined_spec: Append "Precip" to batter_feature_cols() in Model/features.py (alongside the existing weather cols). No frame rebuild needed: the built batter frame already carries the column from _attach_context's mlb_weather.csv merge on GamePk (features.py:1988-1995), so this is a feature-cols change, not feature construction — the cached frames.joblib remains valid per HANDOFF_0714 rules. Serving is already complete: predict.py _batter_rows includes **wx (line 437) whose _weather() dict carries "Precip" (line 346) from the 1_get_todays_games.py Open-Meteo start-hour forecast (wired 07-14 for 1E). Rides the next superset train; per-head SHAP selection votes it. Run predict.py --selftest parity after the change as standard.
- evidence: exposed_cols.txt: Precip in starts (line 4, #131) + team (line 10, #64) lists, ABSENT from batter list (line 2); batter weather exposure = Temp/WindSpeed/Condition/WindDir/hum_eff/air_dens/wind_carry/bat_wind_pull/bat_wind_porch only. frames_columns.txt: built batter frame (479,466x867) already contains 'Precip' (col 182) via _attach_context features.py:1988-1995. batter_feature_cols() (features.py:2976+) lacks it; starts list has it at features.py:3463; team at 3915. Serving: 1_get_todays_games.py forecast_weather() lines 319-373 fetches Open-Meteo precipitation at start hour; predict.py _weather():346 -> "Precip"; _batter_rows spreads **wx at predict.py:437 — batter serving rows already carry it. Not benched: FINISH_PLAN.md 1E (lines 83-86) scoped Precip to outs/total only; Decline ledger = 1F trio + shrinkage priors, no precip. Data: mlb_weather.csv 26,635 rows, Precip 100% non-null 2015-2026, 21.97% of games >0mm (mean 0.325, p95 1.8, max 34.7), 7.4% >1mm. Condition categorical captures only 4.1% of Precip>0 games (238/5,853 Rain/Drizzle) — signal not recoverable from exposed columns.
- notes: Cleanest possible build: one identifier added to a list, everything else (training column, serving forecast, GUI field, parity harness) already exists from the 07-14 1E close-out. Value graded low-modest per head — heavy precip is ~7% of games and roofed parks dilute it — but Condition roof states are batter-exposed so the GBM can carve the interaction, and the count means (xh/xtb/xbk...) plus the xpa_ exposure family are exactly exposure-truncation-shaped. Cross-frame consistency argument is sound: same mechanism, same construction, already accepted on 2 of 3 frames. Not a bench, not declined; "expose and let selection vote" is the repo's stated default disposition.

</details>

### [31] pitch_around_protection  (cost S, value low)
- **Frames:** batter
- **Heads:** bb, xbb
- **Rationale:** Two corrections applied to the candidate: bb's keep-list ALREADY holds both mains (this is a hand-the-product play, chase_matchup precedent, not a fill-invisible-signal play), and the causal story was empirically inverted. Statistically clear but small (~0.1-0.6pp per-PA vs an 8% base); one shared-path line.
- **Synthesis spec:** One line beside run_opp (features.py:2350): df['pitch_around'] = (c_iso_sh - 0.165)*(ctx_behind_slg_d - 0.41). NO CLIP — the measured interaction (t=+5.09, n=448k, mains controlled) is POSITIVE, i.e. protection modulates walks mostly for NON-sluggers, and the candidate's proposed clip would zero the live surplus-side region (spec corrected). Constants: SHRINK iso prior 0.165; frame mean of ctx_behind_slg_d 0.41. Expose ~:3107; serving free via shared add_batter_derived.

<details><summary>Verifier record: pitch_around_protection (BUILD)</summary>

- refined_spec: One line in features.add_batter_derived next to run_opp (features.py:2350): df["pitch_around"] = (df["c_iso_sh"] - 0.165) * (df["ctx_behind_slg_d"] - 0.41). NO CLIP (the t=+5.09 structure includes the protection-surplus side; clipping at 0 destroys it). Constants: 0.165 = SHRINK["iso"] prior (features.py:1589); 0.41 = league-scale SLG center (frame mean of ctx_behind_slg_d = .4117). Inputs already in the frame at that point (ctx_behind_slg_d built at features.py:2887 vectorized, predict.py:476-482 serving; c_iso_sh via _shrink). Expose by adding "pitch_around" to the batter feature list near rbi_conv/run_opp (features.py:~3107); enters the superset, SHAP selection votes per head (expected traction: bb/xbb, but no pre-declared target per accept policy). Serving is free — predict.py:513 calls the same shared add_batter_derived. Sign note for eval: expect POSITIVE learned direction (pitch-around walk modulation is weaker for sluggers, whose feared-hitter walks c_ibb_pa/c_bb_pct_sh already price) — GBMs learn the sign; do not encode the folk clipped-deficit version.
- evidence: EXISTS: no iso-x-protection product anywhere (features.py greps: only ctx products are run_opp=c_obp*ctx_behind_slg at 2350 and ctx_run_conv=bat_brr_xb*ctx_behind_slg at 2255; c_ibb_pa at 2834). BUT proposal premise stale: feature_keep.json bb head (261 cols) ALREADY keeps ctx_behind_slg, ctx_behind_slg_d, run_opp, c_ibb_pa, c_iso_sh — SHAP selection overrode train.py:372 _CTX routing, so both mains are live on bb and only the product is missing. Not in FEATURE_BACKLOG #28-32 composite cluster or Decline ledger (4 items, none ctx/bb). DATA (frames.joblib bf, 479,466 rows): c_iso_sh 100% non-null (mean .169; SHRINK iso prior 0.165/K=160 at features.py:1589), ctx_behind_slg_d 99.9% non-null all seasons 2015-2026 (mean .412, std .087). VALUE probe (2015-2025, n=448k, bb/PA residualized on career BB + c_ibb_pa + toff_r_pg + toff_bb_pct + slot): protection main effect = classic pitch-around (weak protection -> more walks) but STRONGEST for low-ISO hitters (slope -0.037) and ~flat for sluggers (-0.006); centered interaction coef +0.327, t=+5.09 — SIGNIFICANT beyond both mains but POSITIVE sign, i.e. the proposal's slugger-concentrated deficit story is inverted, and its optional clip would zero the live (surplus-side) region. SERVING: predict.py:476-482 builds ctx_*_d from posted lineup; predict.py:513 calls shared F.add_batter_derived (where run_opp lives) — one line serves both paths with parity by construction.
- notes: Two corrections to the candidate as-submitted: (1) its "uncrossed for BB / ctx earns keep on rbi-side" claim is factually stale — bb's active keep-list already contains both mains plus run_opp, so this is a hand-the-product play (chase_matchup/ts_matchup precedent), not a fill-an-invisible-signal play; (2) its causal story and optional clip are empirically backwards — the significant interaction (t=+5.09, n=448k, mains controlled) is positive, meaning protection modulates walks mostly for NON-sluggers. Effect size is small (~0.1-0.6pp per-PA modulation vs ~8% base), hence expected_value low despite statistical clarity; cost is one shared-path line, so the repo's standard superset->selection->paired-CI flow is the right adjudicator.

</details>

### [32] park_vmr_disp  (cost S, value low)
- **Frames:** game/team intermediate + pricing layer (total lines)
- **Heads:** total tails (P(over)); team_total only via fallback
- **Rationale:** Nothing anywhere encodes a second moment of runs, and the venue dispersion trait is real (VMR 1.91-2.65 vs the 2.28 constant; extremes persistent in sign across independent halves) but weak (split-half 0.15) — expect ~0.5-1.5 pts of tail probability at ~5-6 venues. Build the column; the pricing substitution on the adjudicated NB surface is the user's call (gray item 3).
- **Synthesis spec:** Feature: _park_table (:762) add per-game R2 sumsq -> park_cum_R2; in build_game_frame after the park merge (:3729-3736): park_vmr = EB-shrunk venue variance-to-mean ratio with K~900 (NOT the proposed 300 — measured noise/signal implies ~3x more shrinkage) toward VMR0=2.278, gated park_cum_n>=30; identical derivation mirrored in Stores.park (:4804-4820). Consumption (the payoff — gated on gray item 3): predict.total_over (:173-174) disp_g = total_disp*(park_vmr/VMR0)^a, exponent a FIT ON 2025 ONLY (phase rule; do not assume a=1 — mu already absorbs wind-driven mean variation), clip [1.6,3.0], fail-safe to the constant on missing. team_over reaches NB only on calibrator-miss fallback — do not claim team_total value without a separate shoot-out. Optionally expose park_vmr to team_game_feature_cols (expect near-zero for mean-objective GBMs). Winner and NB-per adjudication untouched.

<details><summary>Verifier record: park_vmr_disp (BUILD)</summary>

- refined_spec: Build: in _park_table (features.py:762) after per_game = gb.groupby("GamePk")[stats].sum(), add per_game["R2"] = per_game["R"]**2 and pass stats+["R2"] to _daily_cum(gv, ["Venue"], ...) -> park_cum_R2. In build_game_frame after the park as-of merge (features.py:3731-3736): m = park_cum_R/park_cum_n; v = park_cum_R2/park_cum_n - m**2; vmr_raw = v/m; park_vmr = (park_cum_n*vmr_raw + K*VMR0)/(park_cum_n + K), gated on park_cum_n >= 30, with VMR0 = pooled league VMR computed the same way (measured 2.278) and K set by empirical Bayes ~900 (measured noise/signal ratio; the proposed K=300 under-shrinks ~3x) or fit on 2025. Serving: add the identical derivation in Stores.park (features.py:4804-4820) from row["cum_R2"]/row["cum_R"]/row["cum_n"]. Consumption (primary): predict.total_over (predict.py:173-174) disp_g = art["total_disp"] * (park_vmr/VMR0)**a, clip disp_g to [1.6, 3.0], with exponent a fit on 2025 ONLY (phase rule) — do NOT assume a=1: unconditional venue VMR partly reflects mean variation (Wrigley wind) that mu already captures via WindDir/wind_carry, so a<1 is likely; fail-safe a=0/missing park_vmr -> constant. team_over: as wired the venue disp only reaches calibrator-miss fallback lines (team_line_cals take precedence, predict.py:185-187) — do not claim team_total value without a separate pricing shoot-out, which is a user call. Optionally expose park_vmr to team_game_feature_cols for selection to vote (expect near-zero for mean-objective GBMs). Inputs: Data/mlb_game_batting.csv (R per GamePk) + Data/mlb_games.csv (GamePk/Venue/Date) — no new data, no scrape. Winner untouched; NB-per adjudication untouched.
- evidence: EXISTS check: frames_columns.txt has NO venue/team second-moment column — only pitcher SDs (p_outs_sd, p_fbv_sd, p_rel_sd, pd_fbv_sd, pd_rel_sd); park_r_pg is the venue MEAN (features.py:3736); total_disp/team_total_disp are single cal-year scalars (train.py:1068-1096); FEATURE_BACKLOG has no VMR/dispersion item in #15-32, Part 1, or Decline ledger. WIRING: total_runs IS in NB_PRICED_TARGETS (predict.py:110, adjudicated 07-13 NB>calibrators) so predict.total_over:173-174 lives on the constant disp — the substitution is immediately live; team_over prefers team_line_cals (predict.py:185-187), NB is fallback-only for team lines. DATA (pandas, 26,637 games 2015-2026, 100% R coverage): league VMR 2.278 (claim 2.28 exact); T-Mobile 1.913, Wrigley 2.653, Tropicana 1.970 (claims exact); corr(venue VMR, venue mean)=0.191 (claim said 0.33; direction holds). SIGNAL STRENGTH: cross-venue VMR SD 0.177 vs shuffled-null p99 0.171 — real but barely exceeds sampling noise; split-half (even/odd seasons) venue corr 0.151, YoY 0.093; extremes persistent in sign (Wrigley 2.46/2.75 halves, T-Mobile 1.63/2.02, Coors 2.24/2.24); implied empirical-Bayes K≈900 games, not the proposed 300. SERVING: Stores.park (features.py:4804-4820) already serves as-of cum park rows via self._cum; adding cum_R2 is one column in the shipped idiom.
- notes: Verdict is BUILD (genuine gap — nothing anywhere encodes a second moment of runs; construction/serving/leakage all in shipped idioms; cost is a few lines) but with honest value expectations: the venue dispersion trait is real (spread exceeds shuffled-null p99, extremes persistent in sign across independent multi-season halves) yet weak (split-half 0.15, YoY 0.09), so after proper shrinkage the effect is ~0.5-1.5 pts of tail probability at ~5-6 venues on the total surface only. Plausible-but-not-likely to be CI-clear alone on the 2025 paired gate; cheap enough to bundle and let the standard eval adjudicate (flat = revert per accept policy). Two corrections applied to the original spec: K 300->~900 (or fit), and a fitted attenuation exponent instead of assumed linear scaling. The team_total claim was overstated given team_line_cals precedence — flagged, not fatal. This is a pricing-layer change on an adjudicated surface (NB totals); it extends the winning NB path rather than reversing any verdict, but ship/no-ship remains the user's call per the no-unilateral-decisions policy.

</details>

### [33] off_lu_vsh_edge (denominator-fixed)  (cost S, value low)
- **Frames:** team
- **Heads:** team_total, total
- **Rationale:** The encoding gap is real — the team/game frames carry ZERO handedness information — but the measured value evidence is weak: after shrinkage and lineup averaging the clean platoon edges show ~zero marginal correlation with runs (|r|<0.013, hr edge wrong-signed), and SIM_BLEND already carries partial platoon signal. Built under the repo's expose-and-let-selection-vote default; selection dropping it would be informative, not failure.
- **Synthesis spec:** DO NOT ship as proposed — the original edge mixes TB/PA-shrunk (vsh) with TB/AB-shrunk (career), making it half a walk-rate proxy (r=-0.51 with lineup BB). Fixed: (1) lu_vsh_kedge = posted-lineup mean of (vsh_k_pct_sh - c_k_pct_sh) — denominator-clean, zero new batter-path code; (2) optionally lu_vsh_edge with BOTH terms TB/PA-shrunk (baseline = _shrink(c_TB, c_PA, 'tb_ab'); needs one intermediate added to the served batter row). Compute in lineup_aggregates (:3467), map per side via the lagg_tab block (:3680-3690), expose on team_game_feature_cols; serving via predict._lineup_agg (:700) from already-served cols. Winner untouched; if only one column, ship kedge.

<details><summary>Verifier record: off_lu_vsh_edge (BUILD)</summary>

- refined_spec: DO NOT ship the formula as written (TB/PA-minus-TB/AB denominator mismatch = BB-rate contamination, r=-0.51). Fixed spec, exact #19/#32 idiom: (1) in features.lineup_aggregates (features.py:3467) compute per-row edges from the built bf then mean: lu_vsh_edge = mean(vsh_tb_ab_sh - _shrink(bf.c_TB, bf.c_PA, 'tb_ab')) [c_TB/c_PA are existing as-of cols in bf — this makes both terms TB/PA-shrunk, same prior 0.410/K=320] and lu_vsh_kedge = mean(vsh_k_pct_sh - c_k_pct_sh) [already denominator-consistent, zero new batter-path code]. (2) Map per side in build_game_frame via the existing lagg_tab rename block (features.py:3680-3690), expose off_lu_vsh_edge + off_lu_vsh_kedge in team_game_feature_cols (features.py:3881). (3) Serving: predict._lineup_agg (predict.py:700) adds kedge = (bdf.loc[mask,'vsh_k_pct_sh'] - bdf.loc[mask,'c_k_pct_sh']).mean() from already-served exposed cols; the TB edge baseline needs one intermediate col (c TB/PA shrunk) added to the served batter row where career sums are in scope (same place shrunk_from_sums(sums,'c') runs) — one line each path, parity selftest covers it. (4) _team_rows (predict.py:645) passes both through like off_lu_arswh. Frames: team frame only (total + team_total); winner untouched. Leakage: inherits #19's convention (train = appeared batters, serve = posted lineup). Cache note: game-frame construction change invalidates frames.joblib gf — fine, the final chain rebuilds anyway.
- evidence: EXISTS: team frame (team_game_feature_cols, features.py:3881-3915, 64 cols) and gf (262 cols) have ZERO handedness columns — grep of exposed_cols.txt/frames_columns.txt for vsh/hand/platoon hits only batter frame (vsh_PA/vsh_tb_ab_sh/same_hand/eff_hand) and starts frame (lu_vsh_k, a LEVEL not edge, K-head only, features.py:3341). BACKLOG: #10 'covered by vsh_*/same_hand' (line 559) was batter-grain only; #19/#32 built the lineup_aggregates plumbing (features.py:3467-3479) this rides; no Decline-ledger conflict. DATA: mlb_handedness.csv Bats/Throws 100% non-null (4,185 players); starter Throws known 53,274/53,274 starts 2015-2026; built bf (frames.joblib, 479,466x867): vsh_tb_ab_sh, c_tb_ab_sh, vsh_k_pct_sh, c_k_pct_sh, vsh_hr_pa_sh, c_hr_pa_sh all 100.0% non-null. BUG FOUND: as-proposed edge mixes denominators — vsh_tb_ab_sh is TB/PA-shrunk (VSH_STATS has no AB, features.py:443,2755) but c_tb_ab_sh is TB/AB-shrunk (SHRINK['tb_ab'] den='AB', :1585); measured edge mean -0.0258, corr -0.507 with lineup BB rate = half walk-rate proxy. Clean pairs (k: SO/PA both; hr: HR/PA both) have mean -0.001/+0.0003. VALUE: clean edges near-orthogonal to existing team cols (|r|<=0.28 vs lu_obp_gap/lu_slg_gap/lu_arswh/lu_pullair) = new axis, BUT univariate corr with runs on 26,400 games: k edge -0.009, hr edge -0.012 (wrong sign), opp-hand share +0.008 — null band ±0.012 (shipped off_lu_slg_gap benchmark: -0.016). SERVING: predict._team_rows already consumes bdf via _lineup_agg (predict.py:615,700-713,839); both inputs are exposed batter feature cols so present in served rows; scalar vsh path at features.py:4073-4083.
- notes: Verdict is BUILD on the repo's own disposition ('expose it and let selection vote', FEATURE_BACKLOG line 10) because the encoding gap is real (team frame verifiably carries zero hand information), cost is a few lines in the shipped #19/#32 idiom, and leakage/serving are clean. But the value evidence is weak and the proposal's 'first-order run signal' claim is overstated: after K-shrinkage and lineup averaging, the clean platoon edges show ~zero marginal correlation with runs scored (|r|<0.013, n=26,400; hr edge even wrong-signed), and the only variant that correlated with anything was the denominator-bugged one riding lineup walk rate. Expect selection to possibly drop both columns — that outcome would be informative, not a failure. If the user wants only one column, ship lu_vsh_kedge (zero new batter-path code, fully from exposed cols); the TB edge requires the small baseline-intermediate addition to stay uncontaminated. Winner frame deliberately untouched (small-n overfit caveat); SIM_BLEND already carries partial platoon signal into game heads at weights .35/.20/.30, further capping upside.

</details>


## Gray items (user-adjudicated 07-14)

- **opp_pen_bridge (the only formal GRAY verdict)** — Build the 2-column bridge-pen decomposition (opp_pen_br_era, opp_penhl_share) despite the measured redundancy? Verification found the core claim largely fails: with the HL-outs share nearly constant (sd 0.042), bridge ERA is R^2=0.95 linearly recoverable from the two exposed mains (pen_era + pen_hl_era); the genuinely new content is only the share itself, which is low-variance and contaminated by the GF==1 game-finisher convention; and the claimed meltdown-cross partner (opp_ps_len) only exists if opp_starter_len_pen_split (rank 9) ships. Options: build both / build share-only / skip.
  - Recommendation given: If rank 9 ships, add opp_penhl_share ONLY as a ~2-line rider (it is the actually-missing mixture weight) and drop opp_pen_br_era as a 95% re-encoding; if rank 9 does not ship, skip entirely. Refined spec with exact line anchors is in the adjudication should you elect to build.
- **Batch timing and scope (applies to all 33 build items)** — The finish chain is ABORTED mid-sequence with your active one-all-at-once directive, and nearly every item here is a feature-construction change that invalidates frames.joblib and needs the full two-train chain + selection regen. Do these builds join the pending finish batch pre-chain — including the two heavier waves (raw-pitch v6/v7 schema bump + one ~4-min --from-raw re-agg, rank 27; milb_priors.joblib rebuild, rank 28) — or queue as a post-finale second wave? And where do you cut the list (all 33, top-N, or drop the rank 29-33 low tail)?
  - Recommendation given: Fold ranks 1-28 into the current batch: the chain, frame rebuild, and selection regen are already being paid once, you have already accepted the attribution risk for the all-at-once finish, and the two schema waves are cheap enough (~4 min re-agg + one artifact rebuild) that deferring them buys nothing. Ranks 29-33 are near-free list edits that can ride along or queue with zero loss — your call on whether the keep-list slot contest is worth it this pass. You decide the cut.
- **park_vmr_disp pricing consumption** — The park_vmr COLUMN build (rank 32) is uncontroversial, but its payoff is a pricing-layer substitution on the adjudicated NB-totals surface: disp_g = total_disp scaled by (park_vmr/VMR0)^a with a fit on 2025. Land the column only, or authorize the pricing change (fit a on 2025, ship behind the standard paired gate with fail-safe fallback to the constant)?
  - Recommendation given: Land the column and run the 2025-only fit + paired evaluation of the disp scaling with the fail-safe in place; ship only on a clear paired read. It extends the winning NB path rather than reversing any verdict, but a pricing-surface change is yours to authorize per the no-unilateral-decisions policy.
- **Backlog #22 reconciliation (premise mismatch — verified fresh)** — The heads-game lens flagged that #22 (opponent high-leverage arm availability) was never actually built despite this audit's all-built-through-#32 premise. I re-verified: FEATURE_BACKLOG.md:211 carries a BUILD verdict for it, but features.py has zero availability columns (only pen_np_l3 aggregate fatigue + pen_hl_* quality) and FINISH_PLAN's Phase-1 build list omits it. Re-open #22 and build it in this batch, or re-log it as open backlog?
  - Recommendation given: Re-open it in FEATURE_BACKLOG either way so the ledger is truthful. On the merits it is a natural sibling of own_pen_leash_state (rank 6) and the #15 exposure family — an as-of arm-specific table (which of the opponent's top SV/HLD arms threw both of the last two days, quality-weighted) at S/M cost from data already scraped — so building it alongside rank 6 in this batch is coherent; scheduling is your call.

User decisions (07-14 evening): all 33 items build this batch; #22 rebuilt this batch;
park_vmr column + gated pricing fit authorized (fit on 2025 ONLY, ship only on a clear
paired read, fail-safe fallback to constant dispersion); opp_pen_bridge = share-only
rider (opp_penhl_share) riding with rank 9; bridge-ERA column dropped (95% re-encoding).

## GRAY/REJECT verifier records (for the Decline ledger)

### rbi_traffic_vsp (REJECT)
- evidence: EXISTS check: the exact products do not exist. features.py batter-frame products are all OWN-skill×pitcher (mix_k/bb/hr/hit at 2341-2346, m_*/pars_) or supply-without-pitcher (rbi_conv=rbi_opp_obp×c_tb_ab_sh at 2349, run_opp at 2350, ctx_run_conv at 2255); the only lineup×pitcher collisions (lu_mix_k/whiff/chase, lu_ars_whiff at 3180-3189) live on the STARTS frame. Not in FEATURE_BACKLOG #28-32 composite cluster (verified: #28=starter-frame p_conv_c/p_conv_r at 3204-3205, #29 ump, #30 air, #31 form-exposure, #32 arsenal), not in the Decline ledger (selection-mechanics only), not benched. All 4 inputs are exposed batter mains per exposed_cols.txt. DATA check (frames.joblib bf, 479,466 rows, 2015-2026): ctx_ahead_obp/rbi_opp_obp 99.94% non-null, ps_h_bf/ps_bb_bf 92.24% (early-season starter gaps), products 92.24%; onb-allowed mean .3015 sd .054. VALUE check (the killer): residualized each product on its two factors (linear), measured partial corr with target — PROPOSED: p1 vs y_rbi +0.0005, p2 vs y_rbi -0.0011, p1 vs y_rbi2 +0.0010, p1 vs y_hrr2 -0.0006 (all within noise, SE~0.0015 at n=442k; residual quintiles U-shaped: [0.299,.2927,.2908,.2974,.3027]). SHIPPED CONTROLS on the identical probe: rbi_conv +0.0070 (monotone .2882→.3098), run_opp +0.0257 (monotone .3369→.401), xpa_x_rbi +0.0093 — the probe has power and the shipped products pass it; this candidate fails it. Marginal corr confirms re-encoding: p1 +0.0483 vs ctx_ahead_obp alone +0.0467; p2 +0.0016 (near-zero). Product resid_std 0.0022 vs xpa_x_rbi 0.0074 — both factors have tiny coefficients of variation, so the product is ~99% linearly reconstructable from mains the trees already see, and the residual curvature is target-uncorrelated.
- refined_spec: As proposed (buildable but rejected on measured value): bf["rbi_traffic_vsp"] = bf["ctx_ahead_obp"] * (bf["ps_h_bf"] + bf["ps_bb_bf"]); bf["rbi_traffic_deep"] = bf["rbi_opp_obp"] * (bf["ps_h_bf"] + bf["ps_bb_bf"]) — one line each in the features.py:2338-2350 product block, batter frame, all 4 inputs already exposed and served. Construction is trivially correct; the interaction term it would add is empirically empty.
- notes: Reject basis is NO-VALUE, not existence/leakage/serving: leakage-safe by inheritance (all four factors are as-of cums: ctx from teammate career cums at features.py:2867-2884, rbi_opp_obp at 2917, ps_* from shifted season cums via _pit_rates:1916), and serving is free (all factors in the batter serving path). The why_new physics is falsified by direct measurement: traffic = supply×leak has almost no multiplicative curvature over MLB's narrow rate ranges, so the exposed additive mains carry the whole signal — unlike xpa_x_* where xpa_slot's large variance creates real curvature. Also mild credit-splitting risk (p1 corr 0.81 with the onb sum, 0.58 with ctx_ahead_obp). Proposal's base-rate claim was off: measured y_rbi=29.7%, y_rbi2=10.4%, y_hrr2=45.9% on the built frame. Per no-unilateral-declines policy this verdict is a recommendation for the user's adjudication; the counterargument (2 cols cost ~nothing, selection votes) is real but the same probe that validated three shipped products returns flat-zero here on every claimed target head.

### xpa_team_turnover (REJECT)
- evidence: EXISTS scan: no team-PA/G column exposed (batter frame exposes toff_hr_pa/toff_r_pg/toff_k_pct/toff_bb_pct only; toff_cum_PA/toff_cum_n are unexposed intermediates in frames_columns.txt line 3; FEATURE_BACKLOG Part 1 bench list and Decline ledger contain nothing turnover/exposure-related). xpa_bat (features.py:2815) = decayed personal PA/G, K=10 shrinkage — embeds actual team turnover. Home is an exposed batter main. Code verified: _slot_pa_table features.py:2087-2107 pools home/away as claimed; serving Stores.team_offense features.py:4676-4689 already holds cum_PA/cum_n; Stores.xpa_slot features.py:4101-4111. DATA (mlb_game_batting.csv, 617,988 rows, 2015-2026, PA/Home/BattingOrder 100% non-null, 53,274 team-games): between-team full-season PA/G SD = +/-1.5-1.6% (2023 sd 0.60 on mean 37.88; 2024 sd 0.60; 2025 sd 0.56) — the proposal's "+/-4%" is the extreme min-max range, overstating the usable spread ~2.5x. corr(team PA/G, team R/G) = 0.848/0.862/0.901 (2023/24/25) and toff_r_pg is EXPOSED; residual PA/G sd after removing R/G = 0.303 PA/G = +/-0.8% of exposure. Home/away: away 38.58 vs home 37.04 team PA/G, but per-slot starter diff is a CONSTANT 0.158-0.175 PA/G across slots 1-8 (slot 9: 0.202) — additive, not multiplicative, hence carveable from exposed xpa_slot + binary Home mains.
- refined_spec: Not recommended for build. If the user overrides, the minimal salvage is step (a) only: expose team_pa_pg = toff_cum_PA/toff_cum_n as one main (one line in _attach_context features.py:2021-2024 idiom + one line in Stores.team_offense features.py:4682-4689), skip the ratio, the xpa_to products, and the (slot,Home,Date) sibling table — the products modulate an existing #1-family feature by a factor whose SD is 1.6% (0.8% orthogonal to the exposed toff_r_pg), and the H/A sibling encodes a constant-offset-by-binary already carveable from exposed mains.
- notes: REJECT is on value (constraint 4), not feasibility — leakage and serving are clean and cost is S. The quantitative premise fails on measurement: (1) team-turnover spread is +/-1.6% SD not +/-4%, and 72-81% of its between-team variance (r^2 of corr 0.85-0.90) is already carried by the exposed toff_r_pg, leaving a +/-0.8% exposure modulation (~0.004 runs on the run head) — below selection's noise floor; toff_bb_pct/toff_k_pct mechanically drive the remainder, and xpa_bat already embeds realized team turnover. (2) The home/away slot-table fix targets a real pooling bias (1.54 team PA/G) but the measured per-slot correction is a uniform ~0.17 PA additive offset at every slot — additively separable from the exposed (xpa_slot, Home) pair, i.e. the easiest binary-x-continuous carve for GBMs on a 190k-row frame, not new geometry. Contrast with the shipped xpa_x_* / #31 precedent: there the composed factor varies +/-50-100% across batters; here +/-1.6%. The proposal's own why_new flagged itself lower-confidence; the data confirms it. Per the decline-ledger policy this REJECT is a recommendation for user adjudication, with the salvage crumb documented in refined_spec.

### opp_pen_bridge (GRAY)
- evidence: EXISTS check: no bridge ERA or hl-outs share in any exposed list (team frame pen cols = opp_pen_era/opp_pen_hl_era/opp_pen_np_l3/opp_pen_xwcon); the four cum intermediates away/home_pen_cum_ER/Outs + penhl_cum_ER/Outs confirmed in the built game frame (features.py:3597-3600, 3628-3633) as standard unexposed intermediates, not a bench (no backlog/decline-ledger item; #15=xpen exposure and #22=HL availability are different signals). LEAKAGE: both tables are _daily_cum as-of (features.py:618-631); HL subset strict-subset of pen rows so bridge outs >=0 (verified 0 negative/0 zero over 360 team-seasons). DATA: mlb_game_pitching.csv 228,069 rows 2015-2026, SV/HLD/GF/ER/IP 100% non-null, Outs derived at load (ip_to_outs, features.py:253); HL=46.0% of relief apps, 44.5% of relief outs; bridge ERA 5.23 vs HL 2.70 (real 2.53-run separation). VALUE (decisive): outs share w is nearly constant (mean 0.447, std 0.042), so br_era ~ pen_era + pen_hl_era linear R^2 = 0.95, corr(br_era, pen_era) = 0.908 — the 'algebraically unrecoverable' claim is ~95% empirically recoverable from two already-exposed mains; genuinely new content = only the share's small variation, whose meaning is diluted by GF==1 (every reliever-finished game counts as hl). Claimed cross partner opp_ps_len does NOT exist on the team frame (team_game_feature_cols, features.py:3885-3915). SERVING: Stores.bullpen (4721)/bullpen_hl (4731) exist but return ratios only, not cum_ER/Outs — helpers need a few extra lines, then predict.py _team_rows (615-697) consumes them in-idiom.
- refined_spec: If the user elects to build: (1) build_game_frame, after features.py:3633: den = g[f"{side}_pen_cum_Outs"] - g[f"{side}_penhl_cum_Outs"]; g[f"{side}_pen_br_era"] = ((g[f"{side}_pen_cum_ER"] - g[f"{side}_penhl_cum_ER"]) * 27 / den).where(den >= 90); g[f"{side}_penhl_share"] = g[f"{side}_penhl_cum_Outs"] / g[f"{side}_pen_cum_Outs"]. (2) build_team_game_frame dict (~features.py:3824): "opp_pen_br_era": gf[f"{opp}_pen_br_era"], "opp_penhl_share": gf[f"{opp}_penhl_share"]; append both names to team_game_feature_cols (features.py:3915). (3) Serving: extend Stores.bullpen/bullpen_hl (features.py:4721/4731) to also return cum_ER/cum_Outs (or add a Stores.bullpen_bridge computing both derived values via self._cum on pen_tab + pen_hl_tab), then compute the two values in predict.py _team_rows (~line 653). Inputs: Data/mlb_game_pitching.csv cols GS, SV, HLD, GF, ER, IP(->Outs) — all 100% non-null 2015-2026. Priority if built: opp_penhl_share is the actually-missing quantity (the mixture weight); opp_pen_br_era is ~95% linearly recoverable without it. Frames: team frame only (total + team_total); winner untouched.
- notes: GRAY on the explicit marginal-value criterion, not feasibility: everything checks out mechanically (leakage-safe by construction, servable with a few extra helper lines, S build cost, inputs 100% covered), but the proposal's core why_new claim was tested and largely failed — with the hl-outs share nearly constant across teams (std 0.042), the bridge ERA is R^2=0.95 linearly recoverable from the two exposed mains, so this is ~95% a re-encoding. The 5% residual (the share itself) is genuinely unexposed information but is a low-variance signal contaminated by the GF==1 game-finisher convention (tracks mop-up volume as much as trust), and the claimed meltdown interaction partner (opp_ps_len) does not exist on the team frame. Under the project's superset+SHAP-selection policy a near-free column pair can still ride and let selection vote — that trade-off (S cost vs mostly-redundant signal) is the user's call per the no-unilateral-decisions policy.


## Reject summary + existence confirmations (synthesis, verbatim)

Two candidates carry REJECT verdicts, both on measured NO-VALUE grounds (leakage, serving, and cost were clean on each) — per the no-unilateral-declines policy these are recommendations for your adjudication and belong in the FEATURE_BACKLOG Decline ledger for a future re-evaluation pass. (1) rbi_traffic_vsp (teammate-supply x tonight's-starter-leak products): the residualized-interaction probe that VALIDATES three shipped products (rbi_conv +0.0070, run_opp +0.0257, xpa_x_rbi +0.0093 partial corr) returns flat zero here on every claimed head (+/-0.001, within noise, U-shaped quintiles) — MLB rate ranges are too narrow for supply-x-leak curvature, so the product is ~99% linearly reconstructable from mains the trees already see; the two lines would cost nothing but the interaction term is empirically empty. (2) xpa_team_turnover (team PA/G exposure adjustment + home/away slot table): the premise overstates the spread ~2.5x (measured +/-1.6% SD, not +/-4%), 72-81% of it is already carried by exposed toff_r_pg (residual +/-0.8% of exposure, below selection's noise floor, with xpa_bat already embedding realized turnover), and the real home/away pooling bias resolves to a uniform ~0.17-PA additive offset per slot — additively separable from the exposed (xpa_slot, Home) pair, the easiest possible GBM carve. A salvage crumb (expose plain team_pa_pg as one main) is documented if you override. Separately, the finder lenses pre-killed and documented — with numbers — a long dead-end list so none of it gets re-proposed: stretch whiff delta (YoY r=0.11), daily delta-run-exp RV (r=0.29), pitcher heart share (r=0.88 with pd_zone_d), shadow/waste chase splits (r=0.97/0.94 with plain chase), miss_distance (2027 bat-tracking gate), spin_axis (0% 2015-16; pfx carries it), fielding alignment (re-encodes spray/GB), sz_top/bot (bat_height proxy), n_thruorder (p_tto_decay), inning-usage (#16), bat_score_diff (in-game), catcher framing (shelved), pitcher XBH-allowed profile (y2y r=0.101), LA-distribution moments (carved by the 4-bin histogram), EV-SD (re-encoding), HR-log ROB/Inning/Outs fields (lineup noise), team boom/bust VMR (split-half ~-0.05), and the ENTIRE linescores lens (all six inning-grain aggregates fail persistence: starter first-inning bleed r=-0.001, settle-in dead after shrinkage, comes-out-hot r=0.038, Fano dispersion r=-0.019, late-leakiness dominated by pen_* logs, inning-1/home-half constants absorbed by intercepts) — linescores stay a grading + sim-timing-validation asset, not a feature source.

Items flagged by finders that resolved as already-built or already-covered, confirmed during adjudication: pickoffs are consumed inside psb_stop = (CS+PK)/att (features.py:1956) — not a hole; Condition is a raw categorical in all frames; PH/PR bench-role is exactly xpa_bat's design intent; switch hitters are carried by bat_hand/eff_hand/vsh_; GO/AO exists both sides; the batter's own GIDP is exposed (c_gidp_pa_sh) with a mix_gb collision; Strikes/NP are in PIT_STATS (pc_strike_pct etc.); dome wind needs no gating (boxscores report ~0 wind indoors, 41/4330 dome games); FRP is a re-encoding of OAA (corr 0.992); the Precip serving FORECAST is fully wired (1_get_todays_games.py:312-370) with Precip already exposed on starts+team; MiLB power shape (milb_b2/b3) is exposed; bat_brr_xb already uses Opportunities as its denominator; backlog #9's arsenal contact metrics exist (m_xslg/m_xwoba/m_whiff/m_hh — only the K-dimension was missing, now rank 19); pd_ivb_d covers FF movement (#27); the outcome side of breaking balls is covered (pd_brkwh/bd_brkwh/lu_brkwh) — only the movement CAUSE was absent (rank 27); and hrr2/hrr3 threshold histories exist and survived selection, which is precisely the template rank 2 extends. Additionally, several near-miss existence claims in candidate specs were corrected rather than confirmed during verification and are folded into the ranked specs: batter serving rows already carry p_short_share/p_outs_sd (rank 11's build is lists-only), s.team_st_outs and the pen fatigue/quality stores already exist for ranks 6/9, ARS_B_METRICS auto-splices exposure (rank 19), and the winner frame's missing park/weather/travel/ump context is a PRINCIPLED documented 10k-row-overfit decision — no auditor proposed touching it, correctly.
