# Data/ CSVs are tracked (G6 decision 2026-07-14), with two exceptions:
# raw_pitches/ (~1 GB parquet archive, regenerable via --from-raw source data)
# and mlb_statcast_bip.csv (146 MB — over GitHub's 100 MB hard push limit)
Data/raw_pitches/
Data/mlb_statcast_bip.csv
cache
backups
todays_games.json
!Data/GLOSSARY.md
Logs/
*.log
Predictions/

# trained model binaries are large and regenerable (Model/train.py);
# the small JSONs (metrics.json, eval baselines) stay committed as
# evidence of results
Model/artifacts/*
!Model/artifacts/*.json
*.joblib
*.pkl
*.pickle
*.pt
*.h5
*.onnx

# python
__pycache__/
*.pyc

# local/env files
.venv/
venv/
.env
.odds_api_key

# local tooling
.claude/
cache/
.vscode
